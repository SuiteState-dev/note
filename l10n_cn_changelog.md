# l10n_cn 项目档 · 版本沿革（冷档）

**版本**：c-1（2026-08-19 建档。**内容整节迁自 `l10n_cn_localization_project.md v44 §10`，一字未改。**）
**仓库**：note
**职责**：**只放历史**。`项目档 v41` 及更早的逐版沿革条目全部在此。

---

## 🔴 为什么单独一档（读之前先看这段）

**这份是冷档 —— 它不参与任何决策。**

- **决策前要看的 = 热**（口径、规则、作废清单、当前状态）⇒ 在 `项目档` / `status` / `background`。
- **出事后回溯要看的 = 冷**（某条结论是哪一轮定的、当时为什么那么定）⇒ 在本档。

⇒ 🔴 **新窗口交接时【不要】读这份。** 施工单的「先读四份档」不包含本档。只有在这两种情况下才来查：
1. 想知道**某条结论的来历**（哪一轮、什么证据、当时的替代方案是什么）；
2. 怀疑**某条现行结论已被推翻而没销号** —— 但那种情况**先查 `项目档 §6.7 已作废清单`**，那份是热的。

**迁出理由（2026-08-19 量过）**：迁出前 `项目档` 为 182,771 字符（≈114k tokens），其中 §10 沿革占 **39,039 字符 = 21%**；而四份主档合计约 **236k tokens，已超过单个上下文窗口** —— 「先读四份档」那条指令事实上已不可执行。本次迁出是**最小拆分**的第一步：只搬冷的，不动热的。

## 规矩

1. **本档只增不改。** 已写下的沿革条目**不重写、不订正措辞** —— 它记录的是「当时怎么想的」，改了就失去价值。若某条当时的结论后来错了，**订正写在 `项目档 §6.7`，不回来改这里**。
2. **项目档 §10 只留最近 3 轮**，更早的推入本档。每次回写项目档时顺手推一行。
3. 引用本档一律带版本号：`changelog c-1 §v39`。

---

## 版本沿革（v41 及更早）

| 版本 | 日期 | 内容 |
|---|---|---|
| **v41** | 2026-08-18 | **材料轮（第二批）**，无代码交付。输入 = 星辰再导 **5 套 11 份**打印模板 + **报表公式界面截图两张** + **星辰 ASSBE 资产负债表导出件 xlsx** + **note 仓库 17号附录 PDF 直读**。① 🔴 **新增 §4.5.12 十（唯一归宿）：会小企01表官方行次与列序** —— **列序 = 期末余额在前 / 年初余额在后**；货币资金 = **行次 1**，资产侧 1–30（含固定资产原价 18 / 减：累计折旧 19 / 固定资产账面价值 20 / 流动资产合计 15 / 非流动资产合计 29 / **资产总计 30**），负债权益侧 31–53（**短期借款 = 31** 起，至**负债和所有者权益（或股东权益）总计 53**）；节标题行不占行次。② 🟢 **R45「资产总计」悬案结案** —— **准则原文即「资产总计」**，我方选择获原文级背书；「税局月季报表样写『资产合计』」确属表样与准则不一致，PIN 断言保留、证据级上调。③ 🔴 **金蝶演示账套不得作行次 / 列序 / 行集锚** —— 该 xlsx 出自「通用行业（旗舰版）」演示账套且**被人改过**：表不平（资产总计 − 负债权益总计 = **869,459.00**，年初期末同一差额）、BS 公式里「**短期借款**」被配进「**货币资金**」、导出件**负债侧无「短期借款」行**致行次整体 +1、**列序与准则相反**。⇒ 判读规则立 `l10n_cn_kingdee_material.md v4 §0.1`（形态 / 行为可引，行集 / 行次 / 列序 / 金额不可引）。④ 🟢 **金蝶不平照出、不拦截**（导出件上无任何提示）⇒ 我方 `_CN_CROSSFOOT` 双通道曝光是**金蝶没有的能力**，差异化点获原件支撑。⑤ 🟢 **星辰取数元模型入档**：报表项目 → N 条「**科目 · 运算符号(加/减) · 取数规则**」三元组，取数规则是**枚举**（BS「余额」/ PL「损益发生额」），**用科目名称不用编码、无前缀匹配、无方向后缀** ⇒ 客户自建科目**必须手工加进公式**，我方前缀取数自动覆盖，**结构性优势**。⑥ 🟢 **第二批模板解析**：多栏账 = KZ-J107 **别名确认**；数量金额总账两版（借贷两侧无单价栏）；核算项目明细账两版（`balancedc` 又一处）；核算项目余额表三档（后两档带 `ytddebit`/`ytdcredit` **八栏**）；日记账三份（横纵内容相同、**仅五栏无对方科目栏无日结月结**，⚠️ 第三份挂 `ca_journal_rpt` 下实为「现金支票付款凭证」，**`formId` 相同 ≠ 同一用途**）。⑦ **方向栏出现面扩到六种账表**，全部是「一个方向列 + 一个余额列」，印证 §4.5.22。⑧ 🔴 **`2221` 父行那一格仍未答** —— 本轮截图是**科目表**（只有属性列、无金额），不是余额表 / 总账，**不构成对该格的回答**。配套 `status s-27` / `background v28` / `l10n_cn_kingdee_material.md v4` |
| **v40** | 2026-08-18 | 🔴 **订正轮，无交付**。输入 = 金蝶**总账导出件**（万佳设计集团深圳分公司 2026年第7期）+ **8 份打印模板元数据 zip 原件**（Safi 从星辰导出，`prt_metedata/*.xml` 内嵌 JSON 直读）。① 🔴 **§4.5.22 整节推翻重写，R49 的 Q1 裁决作废** —— 正确规则是**双栏表与单栏表共用一条**：`余额==0 → 方向「平」、余额栏空`；`≠0 → 方向 = 科目余额方向属性`（不随符号迁移），**符号留在余额金额里**（可为负）。原件：`3103` 总账显示「方向贷 / 余额 −31,989.46」，与同账套科目余额表「贷栏 −31,989.46」**完全一致**；`1122`/`2202`/`2221`/`5001` 零余额全部「平」。⇒ v39 那句「两表表现不同、语义一致」**一并作废**（进 §6.7）。② 🔴 **错因入档** —— v39 从 §60(七)「无余额的写『平』」推出「方向必是余额的函数」，**第三步是跳跃**：「平」只证明零值要单独处理，不排除非零时取属性。**法源没错，错的是我方从法源推出的那句「因此」**，且它被写得像法源本身在说话。⇒ `background v27` 立**惯例 33**。③ 🔴 **载体差（惯例 29 第五条实例）** —— 金蝶**打印模板的列集 ≠ 导出件的列集**，本轮在总账（科目在表头 vs 表内两列；`month`+`day` vs 「期间」一列）、数量金额明细账（余额组无方向列 vs 有）、科目余额表（六栏 vs 八栏）**各出现一次**。⇒ 引用列集必须写明载体；**我方锚定导出件**。④ 🟢 **明细账版式锚闭合两种**：三栏式（模板 8 列与导出件逐列一致，含 `vchnum` 凭证字号）、数量金额式 KZ-J105（表头逐字「借方（收入）/ 贷方（付出））/ 余额（结存）」+ 两行表头合并结构原值；⚠️ 原件「付出））」多一右括号，**我方不抄**）。多栏式 KZ-J107 **借贷两个独立模板坐实**，分析栏 66 个 `DataColumn` 全无绑定、运行时生成，5 个并存网格列位 6/13/13/13/13（**分档依据模板内无声明，`unknown`**）。⇒ **多栏式与数量金额式不是二选一的版式争议，是两条产品线**，可分轮做。⑤ 🟢 **§58 装订与签章拿到逐字原文**（明细账封面模板）：记账 / 复核 / 装订 / 财务负责人 / 移交人 / 接交人**六个角色** + 「账薄页数：从…到…共…页」「第…册，共…册」。法定下限是**记账人员 + 会计机构负责人两个**，我方三栏位不违规；**装订册元信息形态照此做**（M3b 解冻时）。⑥ 🟢 **封面 / 目录 / 正文三份模板 `formId` 同为 `gl_rpt_subledger`** ⇒ §7.2「明细账是装订册」得到机制层证据。⑦ 🔴 **本批模板内无取数公式**（带 `=` 的绑定只有四个打印期系统函数；表头那个「公式」是静态标签）—— 要取数公式须另导报表项目类导出件。⑧ **总账两条形态偏离登记**：导出件每科目每期**两行**（本期合计 + 本年累计），R49 是每月一行 + 段末一次累计；导出件用「期间」列（`202607`）而非月份列。配套 `status s-26` / `background v27` / `l10n_cn_kingdee_material.md` v3 |
| **v39** | 2026-08-17 | **R49 中式总分类账（月汇总形态）全绿交付**（`suite_cn_statement` 1.22.0 → **1.23.0**，63 tests 0 failed = 49 旧 + 14 新，无回归；打桩自检 `_cn_gl_direction`→4 红 / `_cn_gl_query`→12 红 / `_cn_gl_compute`→12 红、空绿残留 0；被测库 `latest_version` coa 19.0.3.0.3 / statement 19.0.1.23.0 / voucher 1.3.1，消耗一次性起点 = 无、全 rollback）。**中式账簿三件已交付两件**（余额表 R47 / 总分类账 R49），只剩明细账。① 🟢 **架构（惯例 31 第二次实战）**：新建 `cn_general_ledger` + 自写 `suite.cn.general.ledger.report.handler`，**官方 GL 记录 / 官方 `account_report.py` / 官方任何 handler 三个都没碰**（正面陈述）。行 = 月份不吃列式 `date_scope` ⇒ 改用 `_dynamic_lines_generator`（官方 GL 同款机制）；取数**复用 R47 roll-up + R48 地基，只多 `date_trunc('month')` 一层**；跨年期初经 `to_beginning_of_fiscalyear` 进来（复用 R47-T2）。② 🔴 **Q1 方向栏裁决 —— note 侧初判错、开发侧裁对**：施工单倾向照抄 R48 的「属性落栏」，实测讲不通，裁定 `signed > 0 → 借 / < 0 → 贷 / == 0 → 平`、余额栏恒取正数。**决定性证据有两条、当时都没被引用**：§60(七) 的「**无余额的写「平」**」（「平」是余额状态、不可能是科目属性）与金蝶 `KZ-J101` 的 `balancedc` **计算列**。⇒ 规则边界立**§4.5.22（唯一归宿）**：**双栏表用属性、单栏表用实际符号**，R48 规则适用范围到此划清。③ 🔴 **本轮最重要的一条订正 —— 「无实务版式锚」是错的**：施工单与 design draft-48 都写「总分类账无实务版式锚」，而 `l10n_cn_kingdee_material.md` **§3.6 早有 `KZ-J101` 逐字段绑定**、**§7.1 早有「方向栏是计算列、余额侧恒有借/贷/平」**。**材料在库里、下单方与执行方都没查**，据此还挂了一条「向二姐要总分类账样本」的待办。⇒ `background v26` 立**惯例 32**（**「我方没有 X」与「我方有 X」同为断言，同样要举证**；与惯例 28 互为镜像）。**正确的诚实边界 = 有列集锚、无行措辞锚**（见 §4.5.22 三）。④ 🔴 **判据 4 措辞下调（惯例 14 第二实例）**：原写「**我方第一次两张自建报表互证**」，但两表**共用同一套取数**、交付方自己也写了「**构造必等**」⇒ 只能抓月层聚合新引入的错、抓不到共用上游的错（共模失效）。**改称「同源一致性检验」**；真互证须另开一条独立取数路径（直接从 `account.move.line` 聚合），列为候选、不排期。⑤ 🟢 **Q2 裁 B（连续性定义随之定死）**：只出有发生额的月 + 恒出期初行与本年累计行；连续性 = **本月期初 == 上一个【已印月】的期末**（非日历相邻月）；全年全空且无期初的科目不出段；**只有期初余额、全年无发生额的科目出段**（有余额就是账），其条数单列 `only_opening_count`（实测 = 1）。⑥ 🟢 **受众判断（惯例 29）**：总分类账 = **账簿**，受众 = 客户自己的会计 + 审计，**非报送件** ⇒ 表内口径提示允许，与 R48 撤下 BS 报送件页脚**不冲突**（那是报送件、这不是）；§58 打印连续页码 + 三签章栏位**只留栏位、不做流程**（M3b 冻结）。⑦ 🟢 **两条渲染路径都出**：通用 `_get_lines`（**扁平行**避框架 `totals_below_sections` 噪声合计行 ⇒ `background v26 §7 B-79`）+ 中式版式 XLSX（按钮直下载）。⑧ 🟢 **B-78 入库**：`_init_currency_table` 显式幂等，XLSX 与测试直调路径可各自自建币表。🔴 **两条补课挂 `status s-25 §3`**：**(i)** 判据 1/2/3/4 只给了哨兵 `>0`、**未给实数**（惯例 17，R47 是给了的 ⇒ 表述退化）；**(ii)** 「平」态**无用例钉着**，而它是法源与原件都明写的第三态。配套 `status s-25` / `background v26` / `design draft-48` |
| **v38** | 2026-08-17 | **R48 三件全绿交付**（`suite_cn_statement` 1.21.0 → **1.22.0**，49 tests 0 failed = 37 旧 + 12 新，无回归；打桩自检五处全变红、空绿残留 0）。**本轮全部输入来自同一件事：拿到了实务原件与执业会计口径**（二姐客户真实账套的科目余额表截图 + 三条答复）—— 这是我方**第一份余额表版式锚**，此前该表的合计措辞与行位置只是「按中式惯例」、无原件对位。① 🔴 **T1 余额方向落栏**：实务规则 = **余额落哪一栏由科目自身的「余额方向」属性决定、不随数值符号迁移；实际方向与属性相反时在原栏写负数**（二姐原话「属于贷方科目，亏损了就是负数」「借贷方不会自动挪」；金蝶截图 `3103 本年利润` 净额在借却显示贷栏 `−31,989.46`，note 侧用合计行验算坐实**负数参与合计**：期初贷 `32,478.42+31,956.43+(−31,989.46)+(−6,027.54)=26,417.85` 与合计行一致）。⚠️ **我方 R47 旧规则是「净额 > 0 落借、< 0 落贷」，两种规则合计都能平** —— 所以 R47 判据 A 组全绿**发现不了这个差异**，但每一行的数不同、会计与金蝶对账会全线对不上。落地：`account.account.cn_balance_direction`（Selection 借/贷），**在 `statement` 定义**（运行时载体 / 客户自建覆盖层）。② 🔴 **值来源分三层，主源是我方发行件**：`coa ASSBE_CHART` 方向列 = **唯一 home**（基值 = 准则大类映射 + 逐条例外）；**星辰 CSV 仅制备期底稿与交叉核对、不上运行时路径**（它是**一个客户账套**不是准则，做主源会把该账套的用法抄进来）；`account_type` 仅兜底客户自建。③ 🔴 **`account_type` 兜底实测翻车（两数分列）**：我方发行件（100 条一级）**3 条**（`1602` 累计折旧 / `1622` 生产性生物资产累计折旧 / `1702` 累计摊销 —— `asset_non_current` 但方向 = 贷）；星辰实务表（247 保留）**21 条**（3 备抵 + 12 增值税借方子目 + 6 利润分配借方子目）。⇒ **异质性**：我方只含一级看着只翻 3 条，真实客户表含明细翻 21 条 —— **`account_type` 推导在带子目的真实账套上大面积失真，只能兜底不能主源**；翻车全在**方向与父科目大类相反的子目**。④ 🟢 **交叉核对 16 条不一致，逐条有因由**：真例外 **13 条**（3 备抵 + 10 增值税借方专栏/预交类）已纳入我方发行件；**4 条不纳入** —— 教育费附加 / 地方教育费附加 / 应付利润 / 以前年度损益调整，星辰记借而准则均贷，那是**该客户账套的用法**（附加税费与应付利润是应付项、以前年度损益调整期末结平）。🔴 **裁语：不一致 ≠ 我方错，我方从准则、不随一家账套走。** ⑤ 🟢 **无属性科目三层兜底 + 告警**：字段有值 → 直读；无值 → `account_type` 推导；**同时随件出条件告警**（给条数 + 编码 + 去哪改，**不写「见日志」**·惯例 5）。**不静默留空**（整行看着像没数据）、**不静默推导**（备抵会默默摆错栏且不留痕）。⚠️ 告警是**条件告警**不是恒真声明，与 `§4.5.7.1` 那条口径声明的载体规范不同。⑥ 🟡 **判据 4 = 本单唯一无原件对位的一格**：父子方向不一致时，设计为「父行 = Σ子行 signed 余额，再按**父行自身**方向属性落栏、反向写负数」，实测 `D221` 组自洽（父净 −400 落贷栏、子行各保留自身方向、`父(借−贷) == Σ子(借−贷)` 成立）。**与实务规则同构，但截图里 `2221` 本期与期末恰好为空、未暴露该情形** ⇒ **登记待验**，下次要样本时**指名要这一格**（请二姐展开 `2221` 级次，看父行在子目有借有贷时显示什么）。⑦ 🟢 **T2 毛额不取绝对值**：符号截断处**确认为 0**（`SUM(debit)`/`SUM(credit)` 路径无 `abs`/`GREATEST`；`account_report.py` 的 `max(0.0, net)` 是**资产负债表 `sum_if_pos` 机制**、不在 TB 发生额路径上 —— 排除做得准）。红冲后 `T603` 本期借 `−4.16` / 贷 `−4.16`（贴合金蝶 `5603` 形态），四组仍平。🔴 **口径依赖（须写在明处）**：手工负 `debit` 会被 ORM 翻正成 `credit`，**红冲负毛额只有启用 `storno` 的库才保留** ⇒ **我方与金蝶在红冲呈现上的一致性，前提是客户启用了 storno**；未启用时两边形态本就不同，**不是我方缺陷**。⑧ 🔴 **T3 撤下中式版式页脚**，见 `§4.5.7.1` 的 v38 记录。配套 `status s-23` / `background v25` / `design draft-47` |
| **v37** | 2026-08-16 | **R47 两件全绿交付**（`suite_cn_statement` 1.20.0 → **1.21.0**，37 tests 0 failed，23 旧 + 14 新，无回归）。① 🔴 **T1 触发本单自带门槛并正确停下** —— 工单「不加 custom handler」与判据 A#2 / B#5 自相矛盾：中式余额表的「本期 / 本年累计发生额·借 / 贷」要的是**毛额**，而 `account_codes`（`SUM(balance)`）与 `domain`（同）两个引擎都只出**净额**，**数学上一个方程两个未知数、从余额推不出借贷两个毛额**；全 Odoo 唯一分借贷毛额的是 `custom_handler`，官方试算平衡表本身即是。⇒ **裁决：禁的对象是官方机件，不是 handler 这个手段**（`§4.5.7.1` 已加锚，`background v24` 立**惯例 31**）。② 🟢 **严格六 / 八栏科目余额表落地**：新建 `cn_trial_balance`（挂官方 TB 变体）+ 自带 `suite.cn.trial.balance.report.handler`，取数 SQL 与官方 `_report_custom_engine_trial_balance` **逐字同款范式**，仅在我方 handler 内加派生键 `bal_debit`/`bal_credit`。**正面陈述：官方 TB 记录 / 官方 `account_report.py` / 官方任何 handler 三个都没碰。** 判据全绿：三 / 四组合计借 == 贷（期初 500/500、本期 600/600、本年累计 1100/1100、期末 1100/1100）；逐行勾稽**参与 5 行 / 不成立 0 行**；一级 roll-up **参与 2 一级行 / 不成立 0 行 / 未归入任何一级的叶子 0 行**。③ 🟢 **一级 roll-up 走「单动态行 + 我方后处理器」**（否掉「枚举一级行」：那是「叶子必须动态」同一个封闭世界假设搬到上一层，客户自建号段要靠 catch-all 兜，而兜底行本身就是缺陷的可见面）。roll-up 键 = 有点取首段 / 无点取前 4 位 ⇒ **混合编码实测归一**：`2221` + `2221.01` + `2221.02` + `2221001`（点分 + 连号）四形态同归一键。④ 🟢 **六 / 八栏是一张报表两列集**，开关控「本年累计」两列显隐，不建两个 report 记录。⑤ 🟢 **T2 期初口径五问全 `verified`**：`bal_begin` 是**年初**（`to_beginning_of_fiscalyear`）**不是期初 ⇒ 不动它**，TB 期初用新列 `to_beginning_of_period`；本年累计 = `from_fiscalyear`；🔴 **诚实边界：Odoo 跨年期初 = 全历史 move line 实时累计、不读上年结转凭证**（与中式「期初 = 上年结转」不同实现、数值等价；客户问结转凭证在哪 = 没有且不需要）；期初是否含未过账受 `all_entries` 控制、默认仅已过账（`observed`，未专测）。⑥ 🟢 **验收复核三条全按「改」处理**：单叶子一级组**折叠成一行**（默认行为非开关；边界精确 —— 单明细但编码 ≠ 一级键的不折叠）；**一级父行名无锚时查我方标准名**再回落前缀（**命中金蝶迁入的连号客户人群**：实测连号 `2211001`、库无 `2211` 锚 → 父行名取「应付职工薪酬」而非光秃「2211」；惰性导入加守卫，保住 `§11.2` 两模块分立）；中式版式渲染器整链 `with_context(lang='zh_CN')`，**法定件不出英文科目名**（与 R44 页脚 `translate=False` 同类同解）。⑦ 🟢 **两条渲染路径各贴一次**（惯例 29）：通用 `_get_lines` 八栏、中式版式 XLSX 八栏 6632 B / 六栏 6562 B（六栏列头正确抽掉「本年累计」两列），表头带单位 / 所属期间 / 金额单位、页脚「第 P 页 / 共 N 页」连续编号、末尾三签章栏位（§58/§60，**只留栏位不做流程**）。⑧ 🟢 **惯例 30 两半都上**：每断言前置「参与行数 > 0」哨兵，平衡 / 勾稽 / 一级三类校验各带**注入 → 检出**（Odoo 禁不平分录 ⇒ 数据级不平**构造性关闭**·惯例 24，改在校验边界注入篡改一格断言确出）；打桩自检 handler 三核心方法逐一换 `return` ⇒ 对应判据 **11 / 5 / 2 条变红，空绿 0**。⑨ 🔴 **明细账与总分类账本轮未做**（连接口都不预留：预留出来的接口没有第二个消费方验证，是空设计）。配套 `status s-22` / `background v24` / `design draft-46` |
| **v36** | 2026-08-16 | **R46 空绿排查结单（本轮不推进任何功能，只查已有绿灯是不是真的）。**① 🟢 **全量 77 用例逐条打桩**（`coa` 25 / `statement` 23 / `voucher` 20 / `cashflow` 9），判据＝把被测方法体换成 `return`/`[]`/`None` 后用例还绿不绿。**空绿 4 条（`确认为 4`，非「没查到」）**，修后再打桩 **0**；全量重跑 77 tests 0 failed。② 🔴 **4 条全部落在「断言形态」这一类** —— 断言「不存在 / 为 0 / 无残留」的用例，在**什么都没发生**时天然成立。门控类零新增（该病灶 R45 已拔）。③ 🔴 **两条历史结论因此降级，入 §6.7 第二十四 / 二十五条**：R34「发行科目 formula 还原后留库」`verified`→`observed`（背书测试空绿**且被测对象选错**——用官方 `1122` 测我方留库，而 `restore` 从不碰 `account.account`）；R23-T2/C4-3「聚合表达式无悬空」降级（`assertFalse(dangling)` 在检测器空转时恒成立，**检测器正确性从未正向验证**）。④ 🟢 **另两条结论仍立**：#1 幂等/认领不覆盖、#4 取号不污染 `name`，**另有真证件承载**（`test_claim_writes_ledger`、取号系列），空绿测试属冗余弱牙。⑤ 🟢 **R45 那 8 条发行空绿复核：两半已齐** —— R45 补的是「前置」半（`setUpClass` 激活 `zh_CN`），R46 补的是「哨兵」半（`created==6`/`claimed==15` 正向断言，打桩 `_publish` 即红）。⑥ 🆕 `background` 升 **v23**：**惯例 30 补子形态 (a)（断言"不存在"是重灾区）与 (b)（检测器 / 哨兵类测试必须自带注入-检出半）**——(b) 是**第三次撞同一堵墙**：R43 撞号哨兵做到了、R44 悬空检测没做到、R44-T1 存量告警也没做到（R45 才补）。⇒ **凡交付「会报警的东西」，判据恒为两条：该静时静、该响时响。**⑦ 🔴 **`suite_cn_cashflow_statement` 零测试用例** —— 一个模块没有测试是 100% 空绿，连打桩都不必做，入 `status §6` 收尾。⑧ 🔴 **T3-5 订正（Safi）**：「税局上传外部验证」被 `status §6` 长期记为未做，实际 **R33-A 已做且导入通过**（`.xlsx` 被接受、逐格落位全中、唯一问题是日期字面格式），见 §4.5.21；残留只剩「行19 改填正数后是否被导入器接受」一条。版本：`coa` 3.0.2 / `statement` 1.20.1 / `voucher` 1.3.1（均为测试基建补丁，**零业务逻辑改动**）。配套 `status s-20` / `background v23` / `design draft-45` |
| **v35** | 2026-08-15 | **R45 三件全绿交付**（`coa` 3.0.0→**3.0.1** / `statement` 1.19.0→**1.20.0**；25 + 23 tests 0 failed）。① 🟢 **T1 连号检测判据 `或`→`且` 收窄**（`verified` live 两半）：前提变了 —— **上线前无任何客户库** ⇒ 「装过 2.x 的存量客户」人群为空，原定「验它能响」是验一件不会发生的事；而**中国实务主流编码就是连号**，拿金蝶星辰真实科目表实测**34 条命中旧判据第一分支** ⇒ 唯一会被打到的是**正常的连号客户**。收窄后：无台账**不触发**、有台账**触发**、原文实捕、**不删不改**。形式论证：真·残留必由 2.x `create` 建、`create` 必写台账（缺陷 #3 只损认领痕、不损新建痕）⇒ `A∧B = A`，丢掉的只有「客户自有连号」这类假阳。② 🟢 **T2 披露删「显示为 0」整句**：把 8 条与碰撞/错位表对上，该句**对 `148` 合同资产 / `1504` 债权投资两条是假的**（会显示错的非零数、不是 0）⇒ 定稿只述取数口径、**一切结果预测删除**；两载体一字同改、两导出新串双命中旧串双 0 命中。③ 🟢 **T3 行名核修**：疑似活缺陷**不存在**（我方行24 早已正确，构造性关闭·惯例 24）；全表逐行对位挖出**两处新分歧**，**均由财政部原文裁定** —— 行30「资产总计」我方对（税局第二处笔误）、PL 行19/20/21/30/32 引号字形**我方错**（原文与表样均为全角弯引号）已改。⇒ `§4.5.7.3` 立**例外条款**：行名以财政部原文为准，表样笔误不照搬，登记偏离 + 钉断言。④ 🟢 **`4401` `unknown` 销号**（`§4.5.21` 补 ASSBE 侧结论，星辰名录层实证）。⑤ 🔴 **顺带发现（价值高于三件任务）**：`TestCoaPublish` **8 条用例长期空绿** —— `zh_CN` 未激活 ⇒ 语言门在 `_publish` 入口整批拒绝 ⇒ 一条科目没发过、断言全落空集。⇒ `background v22` 立**惯例 30**（测试通过 ≠ 被测路径执行过；断言前须有正向哨兵）。⑥ 🔴 **两条 note 侧订正**：`§材料表` 删 `科目表ASBE_企业会计准则.xlsx` **死指针**（仓库 404，惯例 28 第三次实例）、条数 263→**260**；`index.md` 立取用规矩第 4 条（**`raw` 会缓存 404**，刚推上去拉不到 ≠ 推失败）。⑦ 🆕 `金蝶材料档` 新增 **§2.1b 星辰名录层**，并记元教训：**科目表材料至少提炼两层 —— 模型层与名录层**，只提模型层会让名录层问题空挂数轮。配套 `status s-19` / `background v22` / `design draft-44` |
| **v34** | 2026-08-15 | **R44 结单回写（T2 交付 / T1 交接待跑 / T3 撤下）。**① 🟢 **T2 十三项取 0 提示交付**（`suite_cn_statement` 1.18.0 → **1.19.0**，`verified` live，22 tests 0 failed）：8 条前缀直取项的静态口径披露落地，`-u` 无误。② 🔴 **载体由单载体扩为双载体（Safi 追认，非照单执行）** —— 跑 XLSX 那一半抓到这张表有**两条渲染路径**：通用路径走 `_get_lines` → `account.report.line`；**中式版式 XLSX（法定主力导出件）走 form 行、不含 `account.report.line`** ⇒ 只加报表行，披露在**客户真正拿去用的那份文件上直接消失**，而屏幕正常、测试全绿。故 (a) `account.report.line`（seq 9000、无表达式）+ (b) `cn_form_asbe_bs` 页脚 `footer_note`（`translate=False`、**中性格式非 `dc_notice` 红字**），**两处文案一字一致**，两个导出各真导一次、定稿串同时命中、被否的旧版两者均 0 命中。⇒ `§4.5.7.1 (iv)`。③ 🟢 **定稿文案改动追认**：工单原措辞被否 —— **不替 0 下结论**，取 0 有第三种合法可能（科目建了、编码对、业务也做了，期末余额恰为 0）。④ 🟢 **xmlid 归属查清（判据 5 的完整答案）**：官方报表 = `l10n_cn_reports.l10n_cn_asbe_bs`（`account.report` id 27），我方披露行 = `suite_cn_statement.cn_asbe_bs_prefix_disclosure`（`account.report.line` id 303）⇒ 我方记录落共享表、FK 挂官方报表，**未改写官方记录任何字段**；官方 `-u` 的 stale-cleanup 按 `ir_model_data.module` 键 ⇒ 不删不改。同 `bal_begin` 模式（R15/R16 已验 survive）。**唯一耦合 = FK 指向 27，属既有依赖非新增风险面。** ⇒ `background v21 §7` **B-77**。⑤ 🔴 **T1 交接待跑，不判未通过** —— 卡点是环境权限（`odoosh-push` 硬锁只推 `develop-6`，容器内建不出 2.4.1 分支），须 Safi 平台侧从 commit `59234dc` 建 throwaway build；地基已核（`observed`，源码/git：`59234dc` 的 `2221` 明细为纯连号、`_detect_legacy_lianhao` 判据对连号码必命中）⇒ **判据 3/5「响 + 不动」标「交接待跑」，不挂 `observed` 待验**（区别于惯例 24 的构造性关闭：那是永远造不出来，这是造得出来但本轮没条件造）。⑥ 🟢 **T3 整件撤下**（ASBE 未执行版 form 判定不做，v32 已定；随单材料从未存在，惯例 28）。⑦ 🔴 **通用 PDF 未字面导出**（同走 `_get_lines`，`observed`，不并入 `verified`、不再跑）。⑧ 🆕 `background` 升 **v21**：新增 **B-77**（我方记录挂官方共享表，官方 `-u` 不删不改）、**惯例 29 补第三条实例**（同一份数据两条渲染路径，只加一条等于没送到、且测试全绿；自查三问 → 四问）。配套 `status s-18` / `background v21` / `design draft-43` |
| **v33** | 2026-08-14 | **R44-T2 开工前的两处口径订正（开发侧逐条推真实报表 XML 后提出，note 侧采纳并拍板）。**① 🔴 **前缀直取实为 8 条、非 4 条**：原表漏 `148` 合同资产（裸正号单前缀），并把 `- 2204`/`- 2245`/`- 251` 三条排除在外。**判据是「取 0 是否等价于缺科目」，不是「公式长得干不干净」** —— 这三条均为单前缀，负号只表列报方向，语义与裸正号相同；且只做资产侧会造成**资产项有声明、负债项没有**的不对称。⇒ 落地 8 条：`1101`/`148`/`1504`/`1505`/`1513`/`- 2204`/`- 2245`/`- 251`。② 🔴 **「口径声明」≠「告警」，原工单措辞自相矛盾**（写了「口径声明」又跟「双通道曝光」）。声明恒真、不依赖当期数值、无误报漏报概念 ⇒ **不走双通道、不配日志**；「静态、不区分是否真为 0」**是特性不是缺陷**。③ 🔴 **载体定为 ASBE BS 报表尾部静态披露行**，**不挂发行回执** —— `l10n_cn_asbe_bs` 的 `chart_template = cn_large_bis`，而发行 wizard 的 `_supported_companies` 只对 `chart_template='cn'` 跑、明确拒绝 `cn_large_bis`，**两拨公司互斥**，回执对大企业根本不出现。否掉「独立诊断按钮」（不知道有问题的人不会去点）与「custom handler 注入」（为恒真声明覆盖官方 generic 引擎，与「不改写官方逻辑」冲突，留作后手）。④ 🆕 `background` 升 **v20**，立**惯例 29**：告警/提示/披露类交付，动手前须确认**载体的可见人群与问题的相关人群相交**；两次实例（十三项人群互斥、T4 归档科目界面不显示）。**与惯例 1 分工：1 管用户找不找得到入口，29 管这个入口下有没有他要的人；先例可复用的是规范，不是那段代码所在的位置。** 配套 `status s-16` / `background v20` |
| **v32** | 2026-08-14 | **三份新准则修订通知入库 + `§4.5.21` 立。**① 🟢 **三项新准则（财会〔2017〕7号 / 22号 / 财会〔2018〕35号）对非上市 ASBE 企业均自 2021-01-01 施行**（原文级 `observed`）⇒ 财会〔2019〕6号**附件1 适用面≈0**，**ASBE 未执行版 form 判定不做**；`standard_version` 保留枚举值、不提供 form，解冻条件＝遇到真实未执行客户；连带 `l10n_cn_ASBE_unexecuted_rowset.md` **不再需要制备**。⚠️ 与 §4.5.20 不冲突（那句「零影响照排」讲的是 CAS 30 对它没影响，非「它有客户」）。② 🔴 **CAS 15《建造合同》已明文废止**（财会〔2017〕22号：执行新收入准则者不再执行财会〔2006〕3号中的《第14号——收入》和《**第15号——建造合同**》）⇒ `5401 工程施工`/`5402 工程结算` 是**废止准则下的科目**，`status §8` 第15项的 `unknown` **销号**；与二姐口径原文级吻合。🔴 `nonneg` 仍不撤；ASSBE 侧 `4401` 仍未核，两准则不得合并判断。③ 🟢 **已核安全**：财会〔2006〕18号 仅被废止《〈第14号——收入〉应用指南》一篇，我方引用的**间接法现金流量部分完好**，显式记录以防重查。④ 🔴 **取材教训入档**：施行日只在**通知**里、不在**附件**里 —— 判断适用面必须取通知。⑤ 新增 **§4.5.0 法规主题速查表**（十二条指针），立表原因如实记：v31 一轮内 note 侧两次把**已落档结论**当未核前置提出（GB/T 24589、CAS 30），**惯例 13 落了档而没有入口＝等于没落**。⑥ 🔴 **`status §5` 订正**：「行集平表材料已到位」不成立 —— 产物在仓库 404、`index.md` 无此条；判定该文件**从未作为文件存在过**（分析在早前对话里完成、结论被写成「已制备」）⇒ `background` 立**惯例 28**。配套 `status s-15` / `background v19` / `design draft-42` |
| **v31** | 2026-08-14 | **R43 骨架修正轮，全绿交付**（commit `6ed1bd2..b6e57ef`，14 文件；`coa` **3.0.0** / `voucher` **1.3.0**；45 tests 0 failed）。① 🟢 **T0 结案：门控 `verified` 无恙**（活体四公司 dry_run + wizard `action_run` + 三入口走查汇流到 `_publish` 逐公司复核，无法绕过）。🔴 **成因二次订正** —— 原疑证真因不是开发侧推断的「R35 pre-gate 残留」，而是「**staging 推了代码但没执行 upgrade apps ⇒ 跑的仍是旧代码、门控未加载**」（Safi 重跑给出）；升级后 `cn_large_bis` 两档纳税人身份均 REJECTED、290→290。⇒ `background v18 §7` **B-76** + **惯例 26**（取证须带 `latest_version`）。**惯例 23 第二次兑现**：推断成因因规规矩矩标 `observed`，一次重跑即翻掉、成本为零。② 🟢 **缺陷 #7 消除，编码统一点分**：两档**新建均 6**、**认领 15（小规模）/ 25（一般纳税人）**、整库 **159→166**、连号残留 0、撞号 0。映射硬编码逐条对照、**判据只用 `code` 不写同义**；我方独有 6 条走 **`2221.51`–`.56`**，号段写死 design + 常量；**撞号哨兵两半都跑**（合成撞号能触发 = 构成证据那一半）。🔴 **T1-d 裁定:不写合并 migration** —— 决定性理由是它与 manifest「发行的科目留库」这条**保护客户数据的既有承诺冲突**，而服务对象是 0 个客户；改为 `_detect_legacy_lianhao` 告警不删不改 + 版本跳 **3.0** 让断裂显式 + 自有库 rebuild 清。🔴 **措辞边界入档:点分 = 统一到 Odoo 形态，不是国标，对客户不得说「按国标」**。③ 🟢 **三件口径**：`1012` **选 A**（认领仅告警、新建才写 `asset_cash`；裁定理由「`observed` 级证据不配自动改客户数据、且 `type` 驱动 CF 会无声改变已出报表数字」入 design，属惯例 21 精神）／缺陷 **#5 销号**（零代码）／缺陷 **#8 修**（枚举剥除 + 三夹具含防误剥）。④ 🟢 **凭证呈现骨架**：「记-N」上 form+list、可排序过滤；**批量打印按凭证号正序重排 `docs`**（乱序输入断言递增）；`binding_view_types`→`list,form`。⑤ 🟢 **T4 6 条地方税种 `active=False`**，并**补齐「归档=隐形」的可发现性**（`_publish` 报告加 `archived` 清单 + wizard 回执明写编码与取消归档路径 + 专项测试）—— 若不披露则等同没发，客户照样自建、指标 3 一分不省。⑥ 🔴 **note 侧两处自我订正**：**(i)** 重复条数的驱动变量是**纳税人档**、不是 chart/准则（原按库分行写成「ASSBE 15 / ASBE 25」错，两变量在样本里共变）；**(ii)** 「159 基线含我方 88」是**假机制** —— 官方原生实装本就是 159/290（子模板 71/202 + 父模板 `cn_common` 88），而我方通用发行件**恰好也是 88 条**，纯属数字撞车，被「发行台账 0 行」当场证伪。⇒ 立**计数陷阱**条目：凡引用科目条数须写明「哪一层」。⑦ 🆕 **新增待评估:纳税人身份分档实效重估** —— 两档终态同为 166，分档对最终科目集**零影响**（官方 `cn` chart 无条件发全套 `2221.01.0x`）；倾向「小规模档时归档官方那 11 条三级明细」，**但涉归档官方科目 ⇒ 与第一梯队 #4 合并评估**。⑧ `background` 升 **v18**（B-73 表格标签订正、B-76 落定、新增 88 计数陷阱、新增**惯例 26/27**）。配套 `status s-14` / `background v18` / `design draft-42` |
| **v30** | 2026-08-14 | **R42 交付 + 二姐两批反馈 + 两库全量科目表实测，note 侧回写。**① 🟢 **R42 交付**（design `draft-41`；`coa` 2.4.0→2.4.1）：**场景 5 反序复现 `verified` live**（挂四轮的惯例 20 待验项销号）—— 官方 chart **激活后回填中文、不永久英文**；🔴 **我方账户「激活前存在」态不可达**（R38-T3 门控使然、**设计使然非漏做**），改可达对照组，免责向结论按惯例 21 留 `observed`、不进对客户表述；顺带 R38-T3 门控由打桩 `observed` 升 `verified`。两件小修（转义序列 / wizard 回执去「见日志」）。② 🟢 **缺陷 #6（`1012`）结案**：编现金流量表时**算现金及现金等价物、受限保证金不剔除** ⇒ 整户 `asset_cash`、**不做明细级区分**；原「可能需明细级区分」疑虑撤销；我方 wizard 告警「应为 `asset_cash`」**内容正确**。🔴 残留张力：与 CAS 31「可随时支付」文义有张力，单人样本 `observed`，遇大额受限保证金客户须复核，**不作对外权威表述**。③ 🟢 **缺陷 #5（负数大写）定案销号**：「红字是手工登记账的标识／现在没有手工帐啦／没红字」⇒ **取绝对值、方向由借贷栏承载、不做红字**，与 R36 实现一致、**零代码改动**；连带 `background §7`「借方红字存成贷方蓝字」降为**不适用**。④ 🟢 **建造合同列报拿到方向**：老制度工程施工/工程结算入存货；**新收入准则改用合同履约成本、合同结算，借方合同资产、贷方合同负债**。实测 `cn_large_bis` 有 `1481`/`2204`、**无合同履约成本与合同结算**，印证「可选自设科目、财政部未给编码」。⇒ `5401`–`5403` 由「存货漏计的已知偏离」降为「**历史科目、现行准则客户不适用**」，解冻条件改为「先核客户执行哪版收入准则」。🔴 时间轴推论标 `unknown`；🔴 **`nonneg` 不撤、ASSBE 侧（`4401`/工程结算）未核，两准则不得合并判断**。⑤ 🔴 **编码风格翻转（`status §8` 第 2 项）：连号 → 统一点分**。依据:成本 4:1（官方点分 85/108 vs 我方连号 21/31）、格式统一与重复消除是同一动作、点分天然可读（连号需分段补救）。代价:号段撞车须约定 `.51`+ 配哨兵、`code` 改写 21/31 条（纯 ORM 无 xmlid 迁移、**零客户的现在是最后窗口**）、192 表达式定长依赖必测。🔴 **措辞边界:统一到点分 = 统一到 Odoo 形态，非中国标准形态，对客户不得说「按国标」**。🟢 原提「国标导出编码格式」拍板前置**无效**——GB/T 24589 非本线对象、早已归档（§4.5.13 / §6.7 第十条）。⑥ 🟢 **凭证存储架构维持现状并获从业者背书**：「凭证不都是按月份打印或者单独打印哪一张吗」⇒ 不需独立凭证对象，R22 否决「第二条记账路径」的判断成立。**同时给出功能规格:按月份批量打印 + 单张打印。**⑦ 🔴 **两条新缺陷**：**#7 发行同义重复**（我方与官方在 `2221` 下同义重复 15 条 ASSBE / 25 条 ASBE，根因 `background v17 §7 B-73` 认领按 `code` 匹配、明细层形态不一致故**结构性失效**；由统一点分一并解决）；**#8 凭证 path 前缀重复**（官方明细名自带父名前缀，与档 1 拼接后重复，**仅官方点分科目触发**故 dev 库测不出）。⑧ 🔴 **一条未答的阻断项**：我方科目出现在 `cn_large_bis` 公司上，与 R37-T4 门控判据及 R42-T1 实证冲突，**未答之前不得对该库做任何 `code` 迁移**。⑨ 🟢 **发行范围重估**：`2221` 税费档净贡献由「补一套」收缩为「**补 6 条（官方缺的地方税种，应默认 `active=False`）+ 补 1 条（进项税加计抵减）+ 重 15~25 条（将由格式统一消除）**」。⑩ `background` 升 **v17**（新增 **B-72**～**B-75**、补编 **惯例 23**、新增 **惯例 24/25**）。配套 `status s-13` / `background v17` / `design draft-41` |
| **v29** | 2026-08-13 | **R38–R41 四轮收口，note 侧回写。**① 🟢 **R38 阶段 B 交付**（`coa` 2.4.0 / `statement` 1.18.0）：三处成对摘全部落地、偏离 5→8（未超封顶 9）、四处同步齐改 + 新增 `19.0.2.4.0/post-migration.py`；验收按绝对终态（存货=0/营业成本=0/`30=53` 差 0/`nonneg` 无破口），对照组 `Dr1403/Cr2202` 存货 +6000。② 🟢 **R39 清 design 五轮债** → `draft-40`（含新增 §17.5）。🔴 **note 侧自我订正一条**：`draft-40` 这个版本号是 v25（R34）时 note 侧**自己填进 §8 索引的**，当时开发侧从未写过 —— **把「应该有」写成了「已经有」**，此后四轮引用一路指向虚构版本；比不带版本号更坏（看起来是对得上的）。落 §9.3 漂移。③ 🔴 **`draft-36 四处落档缺陷` 销号**（挂六轮）：四项内容在本档 / status / background / design / 历轮对话中**从未枚举**，开发侧两次回报"全文搜不到"是正确回答；且 §17.4 的落点建议 B-53…B-65 **已在 `background` §7 全部有 home**。⇒ 立 **惯例 22**（待办条目必须自带可执行内容）。④ 🔴 **R41 存量库活体轮（`verified`，全新 staging，2.1.0 → 2.4.0）—— 四件挂账一次清完**：**B-67 全链升 `verified`**（八条公式同步到位、无半应用、漂移台账 0、覆盖台账 8）；**R38-T1-b「真实升级路径未实测」的 `unknown` 销号**（跨三个 migration）；**R37-T3「要等第一个客户装过、发过第二版」诚实边界解除**；**R35 污染重建坐实**（43 新建 + 56 认领、台账只记 43 条新建）。⑤ 🔴 **两条已入档结论被实测推翻**：**(i) R35 成因记错** —— `post_init` 走 `_publish_all_companies`，源码 `coa_publisher.py:37-38` 只搜 `chart_template='cn'`，**天然不跨公司**；那 43 条来自 **wizard 手动填入非 `cn` 公司**（wizard 文案写死「小企业会计准则 / chart cn」却允许填 `cn_large_bis` 且不拦不警告 = **文案是门控、代码不是**）。⇒ 缺陷 #1 **仍须修（R37-T4 已修）但严重度降一档**：需人工误操作触发、**非默认即错**（默认公司字段为空、不带入当前公司）。对客户不得说「装我们模块会污染别的公司」。**(ii) B-68 暴露面要按路径拆** —— `post_init` 落**单键**（中文写进 `en_US`、`zh_CN` 空）、wizard 落**双键**（两键均中文），差别在 `env.lang` 有无 UI 语境，**源码读不出来**。⑥ 🔴 **§4.5.7.1 全表订正**：五处公式与官方实际不符、判定一处翻转（持有待售负债 Odoo 实取 `2245`，**与金蝶一致**，原表那个 `223+2241+225+226` 是其他应付款的公式抄错行）；**「十四行」作废，改称「十三项」**；新增「取到 0 的语义按公式形态分三类」。⑦ 🟢 **缺陷 #6（`1012` type）拿到实证**（跨准则发行时 wizard 告警原文），🔴 但该告警写「见日志」**违惯例 5**，须查现版本。⑧ 🆕 **新增待办：`report_override.py` `SyntaxWarning: invalid escape sequence '\('`** —— Python 3.13 起变语法错误。⑨ `background` 升 **v16**（B-68 补活体段、新增 **B-71** Odoo.sh staging 推 commit = 保留库重建容器、新增**惯例 21/22**）。配套 `status s-12` / `background v16` / `design draft-40` |
| **v28** | 2026-08-13 | **R37 ASBE backport 测量 + 三件修复交付，note 侧回写。**（推送 `1376a7e..692e260`，3 commit = T2/T4/T3；T1-A 与 T5 为测量项无代码）① 🟢 **§4.5.12 ASBE backport 阶段 A 完成**（逐条 ORM 遍历非 grep）：🔴 **两个吸收口不是一个** —— `5\(53)`（营业成本，吃 5xxx 除 53xx）+ **裸前缀 `-5`**（往年未分配 `balance_domain`，**吃全部 5xxx**）；**病根 = 存货行 `140+142+145+147+1411` 不含任何 5xxx**。**A2 已实查坐实**：R36-T4 那 −6000 权益正来自裸 `-5`（`CN_OE`/`CN_PC` 只含 4xxx 故均 0）；🔴 **不可照 ASSBE 的 `-4` 形状推**（ASBE 损益段是 6xxx、5xxx 是成本类）。⇒ **必须三处成对摘**（存货 + 营业成本 + 权益），逐组推演验证：只加存货破、加存货+摘成本但留 `-5` 仍破 6000、三处齐摘才平。**归属拆分**：官方原生 **3** + 我方源头 XML **2**（`prev_year` 的 `bal_begin` 是 `aggregation` 自动跟随、无需单独改，与 V6b 同构）；**形状与 R36-T1 完全一致，可复用**。**偏离数 5 + 3 = 8，未超封顶 9**；note 侧「约 4 处回到 9」的旧估计**已作废**。② 🔴 **`5401`/`5402`/`5403` 决议：本轮不做，登记已知偏离** —— 受害科目实为**六个**（`\(53)` 只排除 `5301`），而 **v20 官方只补三个 ⇒ 照抄上游不等于修完**；不做的三条理由：完工百分比法下 `5401`/`5402` 是**净额列报**、简单当存货加总可能是错的会计 / 保持「把上游修法提前」的叙事不发明口径 / **`nonneg` 断言会在建筑业客户身上报红字兜住**。解冻 = 建筑业客户 + 二姐列报判断。`5301` 被排除是对的（有专用行 `cn_rde`、type=费用）。③ 🔴 **阶段 B 验收口径订正**：正确终态是**存货由 −6000 回到 `0`**（`1403 ⊂ 140`，属**存货内部结转净额为零**），施工单原写「+6000」把改善量当成了绝对值 —— 落 `background v15 §8 惯例 19`（**判据必须写绝对终态、不写改善量**）。④ 🟢 **T2 `nonneg` 断言交付**（新增 `op='nonneg'`，与 `gte` 正交仅消息不同；ASBE BS + ASSBE BS 各加「行9 ≥ 0 存货不得为负」；**改我方 form 不碰官方 ⇒ 不占偏离额度**；已标注自推·无原文）：实测**仅 `nonneg` 破口、无任何平衡类破口**，**坐实 B-69「精确抵消、躲过所有平衡断言」**。⑤ 🟢 **T3 自检交付（形态 b+a）**：`_check_overrides_effective` 只读任意库可调 + server action；`_apply_all` 末尾自查非空则 ERROR + 登记漂移台账；补齐 `test_e7_baseline_drift` 缺的反向半。**诚实边界已写进代码与测试**：本轮无存量库 ⇒ fresh 只能验**不误报 + 逻辑正确**，真正抓「`-u` 无 migration 漏放」要等第一个客户装过、发过第二版。⑥ 🟢 **T4 缺陷 #1+#3 交付**：`_publish` 逐公司以**目标公司 `chart_template`** 为判据、非 `cn` 一律拒绝且不建科目不写台账 + 可读原因（不写「见日志」）；认领分支写台账（认领前 name/type 快照 + 同名标，幂等不翻转）；`_scan_foreign_publications` 只读扫描**只报不修**。🔴 **诚实盲区**：旧版认领不写台账（正是 #3 本身）⇒ **历史误认领无痕扫不到**；非 `cn` 公司编码与官方 ASBE 大量重合，纯码命中会全误报官方 ⇒ 不产噪声启发式清单。⑦ 🔴 **`background v15 §7 B-68` 整条改写**（详见该条）+ 新增**惯例 19/20**。⑧ 🟢 **副产品 `verified`**：`4001`/`5001` 跨准则语义实测（ASBE `4001`=实收资本 / `5001`=生产成本；ASSBE `4001`=生产成本 / `5001`=主营业务收入），**来自 Odoo 自身数据**。配套 `status s-11` / `background v15` |
| **v27** | 2026-08-13 | **R36 缺陷 #2 修复交付 + ASBE 侧存货口径 backport 拍板，note 侧回写。**① 🟢 **缺陷 #2 已修复**：`report_columns_assbe.xml` **即源头、无独立生成器**（`noupdate="0"`，改产物即改源头），我方 3 条源头写对；`REPORT_OVERRIDES` `labels` 由 `None` 收窄为 `('balance',)` ⇒ **台账 8→5**、只覆盖官方原生 `balance`；基线常量 `n_formula` 7→4；`test_report_override_roundtrip` 收窄到 `label=='balance'`；`uninstall._restore_all` 只写回官方 5 条。**验收五项全过**，`suite_cn_coa` 13 绿、全量 **32 tests 0 failed**（含 V6/V6b/dir_split）。② 🔴 **施工单漏的一条债：migration 是必需品**（note 侧责任）—— 原推理只走到「我方 3 条脱离 B-67」，**没再往下推一步**：官方 5 条不重放 + 我方 3 条重放 ⇒ 升级后**同表两列来自两个年代**，实测 `test_dir_split_double_column` **差 500 破 `30=53`**。⇒ `suite_cn_coa` **19.0.2.2.0 带 `post-migration.py`**（清 2.1.0 误登记的陈旧台账防卸载反噬 + 显式重放官方 5 条）。🔴 引入「**每个触及官方 5 条的版本各带一次 migration**」这一**靠记性守不住的模式**，须一条**能在任意既有库上跑的自检**（单测在 fresh install 上恒绿、抓不到升级路径漏放）。教训 = **部分重放比完全不重放更坏** → `background v14 §8 惯例 18`。③ 🟢 **B-67 升 `verified`** 并补出两条加载路径的分野（XML `data`+`noupdate="0"` 随 `-u` 重载 / `post_init_hook` 不随 `-u` 重跑）。④ 🟢 **T2 中文大写金额已交付**：override `_convert_to_amount_in_word`，**币种守卫仅 CNY 走中文、其他落回 `super()`**；**是方法 override 非改官方数据，不受偏离封顶约束、不登记为偏离**（已在代码注释）；单测 10 用例全过（亿分级 / 连续零只写一个 / 元位 0 补零 / 无整数部分 / 三位小数 HALF-UP 到分且避开 `.xx5` 浮点边界 / `0.1+0.2` 不误判 / 币种守卫），voucher **16 tests 0 failed**；🔴 **负数按绝对值转换、形态待二姐**（中国凭证实务用红字非负号），已在代码与测试标注。⑤ 🟢 **变体可达性完整规则**（R34-T3 + R36-T3 合并）：**root 通用版恒可达 / 兄弟 variant 受 `availability_condition` 相互隔离** ⇒ 这次守卫**有效**，ASBE 库下拉里 28/25 均不在列；残余风险（程序化强制渲染可绕过、行11 会读实收资本）**量化存档、按令只报不修** → `background v14 §7 B-70`。⑥ 🔴 **新增 ASBE 侧存货口径观测并拍板 backport**：`CN_INV` 不含 `5001` + `CN_OC` 的 `5\(53)` 吃入 ⇒ **存货变负数（−6000）+ 成本高估**，且资产与权益精确抵消 ⇒ **`30=53` 不破、守卫抓不到**；**与 B-60 的「漏计」是两种机制、措辞不可混**，另立 `background v14 §7 **B-69**`，**B-60 不经此升 `verified`**（前置「第 2 项破」为假）。Safi 拍板：**照 v20 官方修法（`5001+5101+5201`）backport 到 19.0**。🔴 **实施前必须先逐条 ORM 遍历测量**（不得 grep，B-65 教训）；**必须成对摘且可能是三处非两处**（只加存货不摘损益/权益 ⇒ 反而破 `30=53`，参照 E-7 的 6000 破口）；note 侧此前「约 4 处、加上正好回到 9」**是未测量估计、不得引用**；**若新总数超封顶 9 须先回来讨论，不得先实施后报数**。⑦ 🔴 **`#4/#7` 前置仍 `unknown`**（目标语言未激活时 `with_context(lang=…)` 落目标键还是回落 `en_US`），本轮精力用于 T1 的 500 破口定位 + migration，未测。⑧ 指标 1 与「装 2.0.0 → `-u`」全新实验**仍受限**（开发无法触发 rebuild、非人工操作者，**当场无人工计时即不补估**，推演不落 `verified`）；结构性步骤数 `observed` **7→6**。配套 `status s-10` / `background v14` |
| **v26** | 2026-08-13 | **R35 staging 首次真装走查 + 二姐 `1406` 语义证言，note 侧回写。**（库 `electrostate-platform-2-stating1-36349193`，实施侧 Safi 单人，二姐未参与）① 🔴 **§4.5.12-E7 新增「时序漏覆盖」**：我方 3 条 `bal_begin`/`ytd` 在 coa 覆盖后 **1.148 秒**才创建（毫秒时间戳 `verified`），`_apply_all` 遍历时尚不存在 ⇒ **每次装机必中且静默**，期末列读 `4001`、年初列仍读 `1406`；按 §4.5 **依赖铁律**（子模块不依赖兄弟、伞 `data` 必空）**结构上不可解**，「伞加 hook」「statement depends coa」两方案排除；唯一可行 = **源头 XML 生成时即正确**、`REPORT_OVERRIDES` 缩回官方 5 条，附带 **XML data 随 `-u` 重载 ⇒ B-67 对这 3 条不再适用**、处理面从 8 缩到 5。**8 条台账表据此待拆两张，连带须同步改 T1 基线常量 / `test_e7_baseline_drift` 断言 / `uninstall_hook` 写回范围，四处漏一处即红。** ② 🟢 **逐码 diff 升 `verified`**（独立复算法复现小企业库碰撞 = 1 条 `1406`）。③ 🔴 **新增跨准则碰撞面 14 条**（`cn_large_bis`）：ASSBE `3xxx`↔ASBE `4xxx`、ASSBE `5xxx`↔ASBE `6xxx` 整体平移，**碰撞是必然非偶发**；最刺眼者 **`4001` ASSBE=生产成本 / ASBE=实收资本** ⇒ **认领键 `(company, code)` 跨准则失效、14/100 认领错且全静默**，处置 = **关入口禁止跨准则发行**；连带**待查**：report 28 在 `cn_large_bis` 上是否可渲染（可则 E-7 会把实收资本读成在产品，R34-T3 已教过一次「以为看不到、其实切得到」）。④ 🟢 **`1406` 语义实质结案 = 发出商品**（`observed`，三源独立一致：金蝶星云 263 行表 / 2025 精解 / **二姐口述**）⇒ **不再阻塞 B-60**，§8 补查该项由 `unknown` 降为补齐项（附录原件仍缺故不升 `verified`）；据此确认 **E-7 是双重正确非「副作用可接受」**（行11 改读 `4001` = 在产品真实载体、`1406` 挪进行12 = 发出商品归位），原「明细行语义微偏」措辞上调为**归位**。⑤ 🔴 **风险方向反转**：二姐「`1406` 一般都不用」⇒ 官方报表行11 **几乎必然取不到**在产品、工业 WIP 一律走 `4001` ⇒ **E-7 覆盖是工业客户必需品非可选优化**，B-67 与时序漏覆盖严重度同步上升；反之 T4 场景 A 触发概率近零，其已知偏离更稳维持不修。⑥ 🆕 **二姐商业/工业分野入档**（工业：`1403` 原材料/辅助材料/周转材料 → **`4001` 下设直接材料/直接人工/间接费用** → 完工入 `1405`）⇒ 两条待办：我方发行件是否预置这些明细；**间接费用直挂 `4001` 则 B-60-lite 的 `4101` 留字适用面须重估**。⑦ §8 索引登记 R35 回报与核对表两份。回写点见 `background v13` **B-68**（翻译型字段 create 时冻结、后装语言不回溯，**官方 chart 同样中招**）+ **惯例 17 补句**（平铺后须追问「异质列是否意味着机制该分叉」）；`status s-09` 收全 7 条缺陷与修复排序 |
| **v25** | 2026-08-11 | **R34 脱钩前置探查 + 原值哨兵轮，note 侧回写。**（开发侧推送 `71d8308` 仅 T1 含代码；`suite_cn_voucher` → 19.0.1.1.0；造数全 rollback 已执行）① 🔴 **§4.5.12-E7 乘法式表述作废，改 8 条台账平表**（权威源 = 代码常量 `REPORT_OVERRIDES.expected`）：台账实为 **8 条**非 7 条，漏的正是权益侧 `#7 cns_prev_year_earnings`（`balance_domain` **单表达式**）—— **同一条、两种机制、两次漏**（R33-A 实现时 grep 漏 → V6 抓；文档计数时乘法式漏 → R34-T1 抓）；`#7` 年初列走**官方原生 `aggregation`** 按列 `date_scope` 各自重算，故单条覆盖两列（V6b 坐实），**不是漏了一条、是机制上只有一条**；xmlid 归属 = 我方 3（全为年初/累计列）+ 官方 5 ⇒ **脱钩时前者重建、后者只需重覆盖**。② 🟢 **基线哨兵**（覆盖前逐条比对官方当前值与基线常量，漂移则跳过 + ERROR + 登记 `suite.cn.coa.baseline.drift`，不硬改不中止）；**§8 补查项 V-1 自动化**、手工 grep 降为可选。③ 🔴 **新增 `post_init` 不随 `-u` 重放**（`observed`）⇒ 本节全部覆盖只在**首次安装**施加，旧版已装+仅升级的客户库拿不到覆盖且无提示 → `background v12 B-67`。④ §4.5.12-E7 补**脱钩工作量度量**（204 line + 204 主列 + **191 我方 xmlid，全是年初/累计列 ⇒ 191 是资产度量非工作量度量**）+ **五 form 挂点**（4 张官方 variant + CF 独立）+ 🔴 **官方按型通用版 id4 仍可达**（variant 下拉可切）⇒ 「客户默认看不到按型版」成立、「客户看不到」**不成立**，**一个库两张表两个答案今天就存在**。⑤ §4.5.12-E7 补 **T4 两行为矩阵**（`verified`）：客户记 `1406` → 我方合计对/内含项行错位（**推演升 `verified`**）、记 `4001` → 官方通用未分配 −6000 我方正确；场景 A 明细行错位**登记为已知偏离不修**；🔴 原样报「官方通用 BS 存货行 = `None`」是**行名 key 不匹配非取不到值**。⑥ **§9.3 漂移第七次**，且**性质与前六次不同**：不是两侧账对不上，是**账本身从一开始就数错**（乘法式表述制造漏项）→ 处置 `background v12 §8 惯例 17`。⑦ §8 索引订正 s-06→s-08、draft-36→draft-40。**编码风格定案连号**（R34-T2：CSV 含点 0 行、一级 68 全 4 位 / 二级 22 全 7 位 / 三级 10 全 10 位、`code = parent + 3 位`）+ `suite_cn_voucher` 三档规则已交付（`test_account_path_serial_tiers` 12 绿）。配套 `status s-08` / `background v12` / `design draft-40` |
| **v24** | 2026-08-11 | **R33-A 自发 ASSBE 科目表交付 + 电子税务局外部验证轮，note 侧回写。**① 🟢 新增 **§4.5.21 电子税务局外部验证**十条：`.xlsx` 被接受 + 我方导出件**逐格落位全中**（两条 `verified`，**§9 指标2 的外部锚首次到手**）、跨期/跨表校验、软提示不阻拦但异常进「我的提醒」与信用失分同池、**准则性必然差异**（固定资产处置在增值税按销售货物、在 ASSBE 入行22 营业外收入 ⇒ 账对也对不上，处置=上传说明，**反证我方落点正确**）、季报属期**季初~季末**、元数据字面格式（`observed`，夹具仍赢）；②  🟢 新增 **§4.5.12-E7 编码体系判定**：逐码 diff 取数层碰撞面**仅 1 码**（`1406`）⇒ **二选一**（取数口径以我方 101 为准，**≠ 清场**）+ 对官方 ASSBE 报表 **5 处 formula 外科覆盖**（3 行×2 表达式 + 1 条 `hide_if_zero`，台账 7 条原值，先存原值/`uninstall_hook` 写回）；🔴 **`4001` 须从【损益+权益】两处成对摘**，只摘一边 `资产≠负债权益`（V6 实测破 6000）；B-60 **ASSBE 侧修好、ASBE 侧仍在**；B-60-lite 留字（`4101`/`4401` 不动）；③ 🔴 **§6.7 第二十二条：作废 `R30-P3 ②`「存货 4001 报表正确排除」—— 本项目第一次推翻自己的 `verified`**，三条理由（不完整变换 / 与自引原文矛盾 / 期末结转错读），归属 **note 侧**，补自查「同一结论里出现『原文说 X』与『所以不 X』必须停下来解释」；④ **§6.7 第二十三条**：纸质印刷账本惯例（`本年累计｜本月`）≠ 官方模板（`本期｜本年累计`），**R27-T1 列序不回滚**，权威序写死；⑤ **§5.5**：勾稽守卫价值首次在真实缺陷上兑现（非静默、delta=WIP），**须与 R32-T1-F4 盲区并说**；⑥ **§9.3 三条漂移**（日期读法理由错 / wizard 与存量公司措辞失准 / R30-P3 ② 作废）；⑦ §8 索引换发行输入件、删已验收任务书行（§0.1：回报验收后删除）。**note 侧本轮三处自我订正、一次 `verified` 作废。** 配套 `status s-07` / `background v11` / `design draft-39` |
| **v23** | 2026-08-10 | **R33-T0 取数机制实测 + 自发科目表方向定案，note 侧回写。**① 🔴 **§4.5.6 新增 -bis 取数机制归属**：我方 BS/PL 四张纯 `account_codes` 按编码、官方通用报表按 `account_type`、冲突各不相让（`verified` live，完整回流 `background v10 §7 B-64`）；三含义=编码号段是取数键必须对 / `account_type` 只货币资金类必钉 `asset_cash`（为 CF） / CN 公司默认打开的 BS 就是我方编码变体非按型通用版；② 🟢 **自发科目表定案**（`status s-06 §8 第9项`）：做，形态=预置骨架+客户可改（顺官方 `cn` 轻量 B）、编码稳、名称明细活；数据源=星辰 ASSBE 266 行原件已筛为 `l10n_cn_assbe_chart_skeleton.csv`（68 一级 + 59 候选明细待二姐勾选、剔 19 演示脏数据）；③ B-60 的「类型建错=静默错报」恐慌经 B-64 **收窄到只剩现金类**（我方报表按码不看型）；④ `background` 升 v10（B-64）。**下一步 = R33-A 自发 ASSBE 科目表任务书（新窗口出，因本窗口上下文已长）。** ⚠️ **note 侧一处自我订正**：上一窗口一度判「客户会打开按型官方报表故 account_type 必须全对」——R33-T0 证实 CN 公司默认打开的就是我方编码变体，该顾虑重估、类型除现金外次要。详见 R33-T0 回报（design draft-38 §17-R33-T0）与 `status s-06` |
| **v22** | 2026-08-10 | **R31 v20 源码定位（只读）+ R32 dev 库实测，note 侧回写。**① 🔴 **B-54 前提订正回流**：`code` 非公司无关单值（`code_store` company_dependent、按 root 存），引擎带跨公司 code 兜底 → R32 坐实为**静默错报**（`background v9 §7 B-63`，`verified`），入 `status s-05 §6` 首行；销号 `status §8 第4项子问题③`；② §4.5.12 九补 v20 旁证（官方 ASBE 存货口径加 `5001+5101+5201`）+ 一条**推演待验**（官方修法可能自破勾稽，验法见 §8 补查 V-2）；③ §6.7 新增**第二十一条**（`1406` 在 v20 删除 ⇒ 处置选项 ③ 作废；判据「我依赖的东西下个大版本还在吗」，同源 `sum_if_pos` 也被删）；④ §8 索引订正 s-02→s-05 + 登记 R31/R32 两份回报 + 新增 VPS 侧补查 V-1/V-2/V-3；⑤ §6.6 表格残缺六行补齐（原件位置 / 原件自查两列）；⑥ `background` 升 v9（B-54 订正、B-55/58/59 加 v20 锚、新增 **B-63**、§8 新增惯例 15 工作树完整性 / 惯例 16 官方先例效力边界、惯例 12 补渲染语言假 0 变体）；⑦ 🟢 **R32-T2 结案**：executed 版 ASBE form 照财会〔2019〕6号附件2 正确建（BS 72 行 / 票据分列 / 使用权资产·租赁负债齐全），解除 `status §5 第一梯队 #1` 前置；⑧ 🟢 **R32-T3 done**：dev 库 vat 填占位值。⚠️ **note 侧自我订正一处**：上一窗口把「官方先例」教训落点定为 §6.7，实为 `background §8`（惯例 16）——B-58 在 19.0 仍成立，无物可废。详见 R31/R32 回报与 `status s-05` |
| **v21** | 2026-08-10 | **编码体系专题收口**（R30 尾）。① 🔴 **§4.5.7 整节扩写**：四套编码并存（ASBE / ASSBE / 会计制度 / Odoo 两模板）+ **2017 后新增科目财政部未重发完整科目表、各家自行分配、无权威码**，附 ASBE BS/PL 十四行逐行对照（**碰撞 4 / 错位 9 / 一致 3**）；处置分两类（老码前缀匹配够用；十四行 = **实施期适配、非模块缺陷**），**设置项不现在做**（零客户时形态未知）→ status §4 冻结；现在只做「取到 0 时出口径提示」一条；② §4.5.12 四 **`1481` 订正**（T2-b「直接路径已正确」限定为默认 chart 下）；③ §6.6 登记 ASBE 科目表 263 行 + 2025 工具书（第三方）+ **整理件已证伪三处**；④ §6.7 第十九条**加第三款**（判缺口前先过 §6.6/§8 索引）、注明**第一款对 note 侧同样适用**、附方法论「同类冲突第三次 = 停下来换问题」。⚠️ **本轮 note 侧再证伪两条**：据单文件断言「Odoo 用 ASSBE 编码体系」（实际 ASBE 有独立模板且对齐）、把 `1704`/`2241` 两行误归为「错位」（实为碰撞） |
| **v20** | 2026-08-10 | **R30 全轮收口（P1 / P2 / T2-a / P3 / T2-b 五件）+ 财政部原文逐条核完**。① §4.5.12 增补**六**（ASSBE 三表勾稽原文 9/9/5 条，纠「3.4 九条」的引法）、**七**（行19 累计折旧符号定案：原文填正数，我方原渲染错，已改码；连带确立「每格等于官方屏幕报表」是内部锚**不是权威**，财政部胜）、**八**（ASBE 两附件**无勾稽条款**，显式记「无」+ **永久自推**处置）、**九**（存货十科目，`4001` 生产成本漏计=科目类型层缺陷）；②【订正】§4.5.12 **四表**「单一来源」措辞（= 每表达式单一来源、非每行），并按 R30-T2a/T2b 交付态重写；③ 新增 **§4.5.20 财会〔2026〕11号**（CAS 30 修订，利润表五类别，非上市 2030 施行，**本轮不据此改任何公式**）；④ §6.7 新增**第十九条**（覆盖面没回原文反查 / 读原文没读完）、**第二十条**（文件名≠内容），第十七条补两条指针；⑤ §6.10 新增 **A 的连带发现**（`4001`/`1406` 类型缺陷）→ `background` **B-60**；⑥ §6.6 加**「原件自查」列**，登记四份原件 + 标出财会〔2006〕18号附录**缺原件**；⑦ 🔴 **分级违规订正**：§4.5.12 标题原写「`verified` 原文级」，改为原文=`observed`。⚠️ **本轮三条 note 侧自我证伪**：PL 勾稽「七条」实为九条、§4.5.12 四表措辞歧义致发令错、误以为 `1406` 非在产品。详见 design §17-R30 与 status s-02 |
| **v19** | 2026-08-10 | **R30-P1 + 财政部原文到位 + 两条自我证伪 + 结构改版**。① 🔴 §4.5.12 整节重写：ASBE 应收侧**无交叉**、两版口径**逐字一致**、ASBE 明细级 vs ASSBE 科目级**互斥**；② §6.7 新增第十七 / 十八条（**作废「对称推演」这个方法**）+ 第十六条补第三条边界；③ R30-P1 判定**原生可行**（`domain` + `sum_if_pos` / `-sum_if_neg`，四夹具全对，越南 TT200 先例）→ `background.md` B-58 / B-59；④ **结构改版**：当前状态迁出至 `l10n_cn_status.md`，新增 §9 工作方式约定、§5.4 历轮沉淀抢救、§10 本节；⑤ 材料使用三条铁律成文（版本陷阱、二次整理件、原文缺条）。详见 design §17-R30 与 status s-01。⚠️ 改版过程中曾据一份**过期的 background（v2）**误判「B-47~B-52 回流查无实据」，核对 v5 后证实回流早已完成 —— 教训已并入 §9.8 第 1 条 |
| **v18** | 2026-08-09 | 2026-08-09。🔴 **定位轮 + R28 回写，本档改动最大的一版**：① 🔴 **§6.8 定案 = (b) 法定账主体**，(c) 凭证桥降级为 (a) 之下的实施选项、不再是路线 → **§0 一句话定位整段重写**（原「内账地基层 / DATEV 模式」作废）；② 🔴 **§2 与 design §11.0 的红线「呈现层做、流程层不做」改写** —— (b) 下凭证签章、结账、结转都是流程层，改为「做，但用可预览 / 可红冲 / 可幂等兜住」；③ **R28 只读探查完工** → 新增 design §17（原 §6.2.5，v19 删除）、§5.3；**D 定 = 选①（不建结账记录表）**、**A 的 go/no-go = GREEN**；④ 🔴 **§6.7 第十六条**：「`account_codes` 表达不了按借贷方向取余额」**作废** —— 引擎有 `D`/`C` 方向后缀，但逐 `account.account` 判方向、非伙伴粒度；⑤ **§6.10 收敛**：A/B/C/D 四条各自落到一个选项，**D 正式纳入**，并把错位的 §6.10/§6.9 顺序调正；⑥ status §4 冻结清单按 (b) 逐条解冻；⑦ §4.5.17 按 R28 订正两处 dev 库事实 |

> v17 及更早的沿革未逐版留存，详见 design §7 验证记录与各轮任务书。
