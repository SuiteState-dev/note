# 中式账簿与凭证版式矩阵 — 补充件

来源：金蝶 AI 星辰旗舰版第二批导出件，2026-08-07 取得
构成：账簿/凭证打印模板 XML 37 份（新增 22 份）、业务凭证模板 JSON 41 份、报表取数公式截图 2 份、报表导出件 2 份、凭证列表导出件 1 份、科目关联设置导出件 1 份
用途：主文件 `l10n_cn_ledger_form_matrix.md` 的补充与修订
证据等级：**verified（解析自模板定义文件与产品导出件本身）**，标注 `observed` 者除外

---

## 0. 材料口径警告

本批全部材料来自同一账套。报表导出件落款为 **会小企01表 / 会小企02表**，科目表编码段亦确证为 **ASSBE（小企业会计准则）**（见 §8.2）。

影响范围：

- 该批材料**不可用作 ASBE 科目表或 ASBE 报表行的对照源**
- 产品侧无 ASBE 选项。金蝶 AI 星辰旗舰版的科目引入引出模板仅提供小企业会计准则 2013 版一套，`observed`
- 准则由建账时选定的科目表决定，一账套一准则。产品按账套计价（含 3 账套，每增 1 账套 ¥1000/年，`observed` 自购买页），准则切换等同新建账套

对 Odoo 侧的结构含义：Odoo 的 chart template 同为建库时装载一次、一个 company 一套。`l10n_cn`（ASSBE）与拟建的 `l10n_cn_asbe` 作为两个独立 template 并存，无需准则切换逻辑，与金蝶的账套边界同构。

**未决**：小微 SaaS 产品线全线不覆盖 ASBE，可解读为空白存在，亦可解读为该客户规模带无此需求。本材料不足以判定，须由目标客户规模带另行确定。

## 1. 业务凭证模板：单据到凭证的映射规则语言

材料：41 份 `.template`（JSON），覆盖 23 类源单，共 215 条分录定义。

此项**不属于版式材料**，是金蝶「智能核算中心 → 业务凭证模板」的规则定义导出件，记录业务单据生成会计凭证的完整规则。

### 1.1 模板级结构

| 字段 | 含义 |
|---|---|
| `sourceBillId` | 源单实体标识（如 `pur_invoice`、`sal_invoice`、`inv_out_bill_third`） |
| `condition` | 单据级触发条件（如「发票类型 等于 专票」），配套 `conditionExp` 为结构化过滤器 JSON |
| `mergeType` | 分录合并规则，取值为逗号分隔的编码串 |
| `entrySortType` / `voucherSumType` | 分录排序与汇总方式 |
| `entries[]` | 分录定义数组 |

`mergeType` 观测值：`0,2,4`（33 份）、`,1,3,4,`（11 份）、`1,3,4`、`,2,4,`、空（5 份）。**编码含义未知**，模板文件不含枚举定义，须从产品界面另查。标记 `unknown`。

### 1.2 分录级结构

| 字段 | 含义 | 示例 |
|---|---|---|
| `accountExp` | 科目解析表达式 | `bd_material.defaultaccount` |
| `dc` | 方向，`1` = 借，`-1` = 贷 | — |
| `amount` / `amountFor` | 金额表达式（原币 / 本位币） | `{商品分录.实际不含税金额}` |
| `qty` / `price` / `unit` | 数量 / 单价 / 计量单位表达式 | `{商品分录.基本数量}` |
| `assitItem` | 核算项目映射，JSON 对象 | `{"商品":"单据.商品分录.商品编码"}` |
| `explanation` | 摘要模板，含占位符 | `采购发票入库{单据编号}` |
| `entryCondition` | 分录级触发条件 | `商品分录.源单类型.名称 包括 ('采购入库单',…)` |

金额表达式支持算术运算：`{商品分录.价税合计}-{商品分录.本次收款金额}`、`0-{商品分录.出库单价税合计}`、`{勾稽商品信息.勾稽金额本位币}/{勾稽商品信息.勾稽基本数量}`。

每条模板同时提供中文表达式与内部字段表达式两套（`amount` / `amountExp`、`condition` / `conditionExp`），互为翻译。

### 1.3 科目解析模型

分录不直接写死科目，而是通过表达式从基础资料取默认科目。第一批 41 份模板观测到 11 种解析源，215 条分录分布如下（全量 106 份的 38 种解析源见 §9.2）：

| 表达式 | 条数 | 含义 |
|---|---|---|
| `model.defaultaccount` | 70 | 模板固定科目（在模板上直接指定） |
| `bd_material.incomeaccount` | 33 | 商品档案上的收入科目 |
| `bd_bank.defaultaccount` | 32 | 银行账户档案上的默认科目 |
| `bd_material.defaultaccount` | 26 | 商品档案上的默认（存货）科目 |
| `bd_customer.defaultaccount` | 21 | 客户档案上的默认科目 |
| `bd_customer.incomeaccount` | 12 | 客户档案上的预收科目 |
| `bd_material.costaccount` | 11 | 商品档案上的成本科目 |
| `bd_supplier.defaultaccount` | 4 | 供应商档案上的默认科目 |
| `bd_supplier.incomeaccount` | 3 | 供应商档案上的预付科目 |
| `bd_auxinfo.defaultaccount` | 2 | 辅助资料（出入库类型）上的科目 |
| `bd_paccttype.defaultaccount` | 1 | 收付款类型上的科目 |

即：主数据档案挂科目 + 模板挂科目，两条路径并存。同一档案可挂多个用途科目（`defaultaccount` / `incomeaccount` / `costaccount`）。

配套材料 `-科目关联设置 # bd_payouttype` 导出件的表头为「编码 / 名称 / 对应科目编码 / 对应科目名称 / 科目2编码 / 科目2名称 / 科目3编码 / 科目3名称」——**一条基础资料最多可关联 3 个科目**。该导出件数据行为空，仅表头有效。

### 1.4 核算项目映射

`assitItem` 实际使用的维度仅四项：客户（26 次）、部门（21 次）、商品（20 次）、供应商（7 次）。

一条分录可同时映射多个维度，例：`{"部门":"单据.部门","客户":"单据.客户"}`（LSD007 零售结存单）。此项与 PDF 材料的观察一致——同一科目在不同凭证上可挂不同数量的核算项目。

### 1.5 数量 / 单价 / 单位的实际填充率

| 字段 | 填充条数 / 215 |
|---|---|
| `qty` | 25 |
| `price` | 23 |
| `unit` | 17 |

集中出现在存货类分录（拆卸单、盘盈盘亏、其他出入库、采购单票勾稽、销售发票成本结转行）。收入、税额、往来款分录均为空。

**主文件 §6.3 修订**：原表述「在金蝶为凭证分录的一等字段，任何分录均可填」不成立。数量 / 计量单位 / 单价在分录上确为独立字段（与 `amount` 平级，非发票语义派生），但填充由模板逐条配置，且实际仅用于存货类分录。

修订后表述建议：`quantity` / `measureunit` / `price` 在金蝶为凭证分录的独立字段，与金额字段平级，不承载发票语义；是否填充由业务凭证模板逐分录配置。Odoo `account.move.line` 无对应的语义中立字段，`quantity` / `price_unit` 已被发票语义占用。缺口性质与原判定一致，成因描述须更正。

科目属性是否另有「数量核算」开关，模板材料不含该信息，仍待查（主文件 §7 待办保留）。

### 1.6 源单覆盖

| 域 | 源单 |
|---|---|
| 采购管理 | 采购发票、采购单票勾稽、暂估调整单、受托入库/结算/退货单、联营结算单 |
| 销售管理 | 销售发票、委托代销单/退货单、委托代销结算单、委托结算退货单 |
| 库存管理 | 产品入库/退库单、其他入库/出库单、生产领料/退料单、盘盈/盘亏单、拆卸单、成本调整单 |
| 门店管理 | 零售结存单 |

同一源单按业务条件分出多张模板，例：销售发票分「不结转成本」与「同时结转成本」两版；委托结算按「一般准则适用 / 仅确认收入」交叉分出四版。**准则差异在凭证模板层区分**，非在科目表层。

### 1.7 与 Odoo 的对位

| 金蝶概念 | Odoo 19 Enterprise 对应 | 判定 |
|---|---|---|
| 业务凭证模板（源单 → 凭证规则） | 无统一规则层。各业务模块以 Python 硬编码生成 `account.move` | **无** |
| 档案默认科目（商品/客户/供应商/银行账户挂科目） | `product.category` 的 `property_*_account_id`、`res.partner` 的 `property_account_receivable_id` 等 | **近似**（Odoo 用 property 字段，粒度与可配数量不同；金蝶为一档案最多 3 科目且用途命名自由） |
| 分录级 `entryCondition` | 无 | **无** |
| 摘要模板占位符 | 无（`ref` / `name` 由代码拼装） | **无** |

该项不在主文件 §5 差集表覆盖范围内（§5 限于账簿与凭证版式），建议另立差集表或并入 §11.7 待评估。

---

## 2. 报表取数公式模型

材料：`资产负债表公式.png`、`利润表公式.png`（产品界面截图），证据等级 `observed`。

### 2.1 结构

报表项目（一行）对应一张取数规则表，每条规则为三元组：

| 列 | 取值 |
|---|---|
| 科目 | 科目名称（支持末级明细，如「税金及附加_教育费附加」） |
| 运算符号 | 加 / 减 |
| 取数规则 | 余额 / 损益发生额（其余枚举值未观测） |

结果列随报表类型变化：资产负债表输出「期末数 / 年初数」，利润表输出「本期金额 / 本年累计金额」。表底有合计行。

### 2.2 两项观察

1. 取数规则表是**科目清单式**，非公式字符串。不存在 `ACCT()` 类函数文本，也不存在跨行引用。
2. 「货币资金」项目的规则表中列有「短期借款」科目（运算符号为加）。该配置与准则不符，判定为**该演示账套的配置错误**，非产品行为。样本 1 例，`observed`。

### 2.3 与 Odoo 的对位

Odoo 19 `account.report.line` + `account.report.expression` 支持 `account_codes` 引擎与 `balance` / `sum` 等 subformula，可表达同构的「科目清单 + 加减 + 取数口径」。

判定：**有**。此项不构成产品缺口，构成的是**配置界面缺口**——Odoo 侧报表行编辑需在开发者模式下操作 `account.report.expression`，无面向会计的规则表界面。该差异属实施体验，非功能。

---

## 3. 手工凭证观察（原「期末智能结转的产出」，已作废重写）

材料：`引出列表_录凭证_20260807034535.xlsx`（凭证列表导出件）与 记-86 的打印件 PDF。

**前一版本的误判**：曾将 记-86 判为智能结转产出，并据此得出「结转产出单张凭证 / 日期取期末日 / 停在保存态」三条结论。打印件显示 记-86 摘要为「支付房租」「员工还款」，科目为 `1001001 库存现金_杨` / `1002001 银行存款_建行`，属手工录入凭证。导出件来源菜单为「录凭证」，非结转。**上述三条结论全部作废，不得引用。**

智能结转的产出形态目前**无任何材料**，主文件 M3 后一步的规格来源仍为空缺。

### 3.1 手工凭证的三项观察

1. **外币折算正确**。欧元汇率 7.958，原币 -12,580,806.05，本位币 -100,118,054.51，折算无误。
2. **大写金额可处理负号**，输出「负壹亿零壹拾壹万捌仟零伍拾肆点伍壹」。仍为「点」而非元角分。累计样本 3 例（2,980.00 / 1.00 / -100,118,054.51），全部同一缺陷形态，`observed`。
3. **计量单位、单价、数量为三个独立字段**。该凭证首行 `单位 = EA`，而单价与数量均为空。与 §1.5 的模板级证据一致：三者由模板逐项配置，不构成一组。

### 3.2 演示账套数据状态

该账套演示数据本身异常：库存现金期末为负、资产总计与负债所有者权益总计相差 1,739,118.00 不平。**任何取自该账套的金额均不可用作行为判据**，仅字段结构与版式可用。

## 4. 新增账簿模板

### 4.1 日记账（`ca_journal_rpt`）

三份模板：纵向 A4（210×297，边距 10/10/19/19）、横向 A4（297×210，同边距）、以及一份 `_copy` 变体，其模板名为 **现金支票付款凭证**（纸张 210×297，边距 1/1/1/1，绑定 `number` / `explanation` / `creditfor`）。

数据列绑定：

`date`（日期）/ `explanation`（摘要）/ `debitfor`（收入金额）/ `creditfor`（支出金额）/ `endbalancefor`（余额）

表头：`account`（账户）、`currency`（币别）、`periodrange`（期间/日期）。页脚含 `getUserName()` 打印人。

**结构性观察**：仅五栏，**无对方科目栏**，无日结 / 月结行绑定。此形态为出纳侧的账户流水，非《会计基础工作规范》所指的三栏式现金日记账（后者应含对方科目）。差集判定须按此实际形态书写，不可按传统形态推定。

现金日记账与银行存款日记账是否共用该模板（由 `account` 表头参数区分），材料未直接给出，从 `formId` 单一推定为共用，标记 `observed`。

### 4.2 数量金额总账（`gl_rpt_quantitysum`）

两份模板，297×210 横向，边距 10/10/15/15。绑定 14 字段：

`acctnumber` / `acctname` / `period` / `unitname` / `beginqty` / `beginprice` / `beginamountfor` / `debitqty` / `debitamountfor` / `creditqty` / `creditamountfor` / `endqty` / `endprice` / `endamountfor`

与数量金额明细账（KZ-J105）比较：明细账为「借/贷/余各三栏（数量、单价、金额）」共九栏；总账的借方与贷方**无单价栏**，仅期初与期末有单价。

模板 2 较模板 1 少 `period` 列，表头多 `period` 参数——即按期间过滤与按期间分行两种形态。

### 4.3 核算项目余额表（`gl_rpt_assistbalance`）

三份模板，均 297×210 横向，边距 10/10/0/0（套打），`splicePrint = true`。命名即为规格：

- `prtpl01` — 4 个动态列 1 页（1 个 DataGrid）
- `prtpl02` — 6 个动态列 2 页（2 个 DataGrid）
- `prtpl03` — 8 个动态列 2 页（2 个 DataGrid）

固定绑定：`period` / `begindebit` / `begincredit` / `debit` / `credit` / `enddebit` / `endcredit`，`prtpl02` 与 `prtpl03` 另加 `ytddebit` / `ytdcredit`（本年累计借 / 贷）。

即：较科目余额表（KZ-J104，六栏）**多本年累计借贷两栏**，共八栏。核算项目本身的列为动态生成，无绑定。

### 4.4 银行余额调节表（`ca_bank_adjust_rpt`）

297×210 横向，边距 5/10/10/0，拼版。绑定 12 字段，构成左右对称的两组：

| 企业侧 | 银行侧 |
|---|---|
| `comexplanation` | `bankexplanation` |
| `comdate` | `bankdate` |
| `comamount` | `bankamount` |
| `comsettletype.name` | `banksettletype.name` |
| `comsettleno` | `banksettleno` |
| `comremark` | `bankremark` |

表头：`balancetext`、`enddate`、`filter.paramaccount.name`、`filter.paramcurrency.name`。

Odoo 19 的银行对账（`account.bank.statement` + reconciliation widget）为交互式核销界面，无「企业账 / 银行账双栏对照 + 未达账项四类」的静态调节表形态。判定 **无**。

### 4.5 费用明细表（`gl_feedetailrpt`）

297×210 横向，边距 0/0/0/0，拼版，2 个 DataGrid，15 个 DataColumn。仅 3 项绑定：`number`、`name`、`ytdamount`。其余 12 列动态生成（推定为期间列）。

### 4.6 调汇历史信息表（`gl_adjustrateinforpt`）

297×210 横向，边距 10/10/15/15。绑定 12 字段：

`period` / `number` / `name` / `currency.name` / `dc` / `begamountfor` / `begamount` / `exchangerate` / `endamount` / `adjustamount` / `voucherno` / `date`

记录每次期末调汇的科目、原币余额、本位币余额、所用汇率、调整额、生成的凭证号。

Odoo 19 的 `Unrealized Currency Gains/Losses` 报表提供科目、外币余额、当前汇率、调整额与调整分录，形态接近。判定 **近似**（缺 `period` 维度的历史留痕视图，Odoo 为当期快照）。

### 4.7 固定资产（19 份模板）

| 模板组 | 份数 | 分档维度 |
|---|---|---|
| 资产卡片 | 2 | 主模板 + 一份副本 |
| 资产清单 | 2 | A4 横向（17 字段）/ A4 纵向（12 字段） |
| 折旧明细表 | 3 | 不小计 / 按使用部门小计 / 按资产类别小计 |
| 折旧汇总表 | 2 | 按使用部门 / 按资产类别 |
| 折旧分配表 | 8 | 四个汇总维度（变动方式 / 使用部门 / 资产类别 / 使用状态）× 显示科目与否 |
| 资产清理 | 1 | — |
| 折旧调整 | 1 | — |

折旧明细表字段：`assetnumber` / `assetname` / `assetmodel` / `txtdept` / `originalvalue` / `accumdepr_begin` / `period_depr` / `accumdepr_diff` / `accumdepr_year` / `accumdepr_end` / `netvalue`。

**折旧分配表的「显示科目版」较基础版多出 `acctnumber` / `acctname` / `detailitem` 三项**——即折旧费用的科目去向与核算项目。该形态是「折旧计提凭证的分配依据表」，中国实务中用于核对折旧分录借方去向。

资产卡片模板含两个 DataGrid：设备信息（`devicenumber` / `devicename` / `devicemodel` / `deviceunit.name` / `deviceqty` / `deviceamount` / `devicedate` / `devicelocation.name`）与变动记录（`date` / `mode` / `content` / `operator`）。Odoo `account.asset` 无卡片打印形态。

固定资产各表与 Odoo 的逐项差集尚未判定，本补充件仅记录金蝶侧形态。

---

## 5. 结构性发现（补充主文件 §6）

### 5.1 动态列分档是产品级设计模式

| 模板 | 并存 DataGrid 数 | 分档依据 |
|---|---|---|
| 多栏明细账 KZ-J107 | 5 | 分析栏数量 |
| 核算项目余额表 | 1 / 2 / 2（三份模板） | 动态列数（4 / 6 / 8） |
| 核算项目组合表 | 2 | 未标注 |
| 费用明细表 | 2 | 未标注 |

产品不做真正的自适应布局，而是**预置若干档位模板，按动态列数选档**。该做法对 Odoo 侧的含义：若实现多栏账或核算项目余额表，QWeb 模板同样需要按列数分档，或改用可横向滚动的非打印形态。

### 5.2 绑定分层

模板中的字段绑定挂在 `DataCell` 上（分「标题行 / 数据行 / 合计行」三种 `DataRow`），不挂在 `DataColumn` 上。`DataColumn` 仅承载 `columnWidth` 与 `index`。

该结构导致「有 DataColumn 无绑定」即为运行时生成列。据此可判定：数量金额总账、日记账、核算项目余额表的固定栏有绑定；费用明细表、核算项目组合表、多栏账的分析栏无绑定。

---

## 6. 主文件修订清单

| 位置 | 修订 |
|---|---|
| §0 使用须知 | 增加材料口径声明：本批材料来自 ASSBE 账套 |
| §1 材料清单「未取得」 | 移除：数量金额总账、核算项目余额表、费用明细表、调汇历史信息表。保留：核算项目多栏账、核算项目明细表、核算项目综合表 |
| §5 差集表 | 新增行：日记账（判定见 §4.1）、银行余额调节表（**无**）、数量金额总账（**无**）、核算项目余额表（**无**）、费用明细表（待判）、调汇历史信息表（**近似**） |
| §6.3 | 按本件 §1.5 与 §8.3 改写：数量核算为科目级开关，计量单位在科目上指定。缺口判定不变，载体设计结论更新 |
| §4.1 | 六栏为打印模板栏数；报表侧为八栏（含本年累计借贷）。见本件 §10 |
| §7 待办 | 勾除四张已取得模板与「科目数量核算开关」（已由 §8.3 结案）；新增「结转凭证分录级材料」「mergeType 编码含义」「分录能否覆盖科目现金流量项目」三项 |

---

## 7. 待办（本件产生）

- [ ] 取智能结转凭证的**分录级**材料（打印件 PDF 或本年利润科目明细账），用于定义 M3 后一步的结转规则。此项材料目前完全空缺
- [ ] 查 `mergeType` 编码枚举（产品界面「业务凭证模板 → 合并规则」）
- [ ] 取核算项目多栏账 / 明细表 / 综合表三张模板，补全六张形态
- [ ] 固定资产七组模板与 Odoo `account.asset` 系列报表的逐项差集
- [ ] 验证分录级能否覆盖科目带出的现金流量项目；一笔分录能否拆分多个流量项目
- [ ] ASBE 账套另行取材。金蝶星辰产品线不提供 ASBE，须换 K/3 Cloud 或其他产品取材

---

## 8. 科目主数据模型

材料：`引出数据_科目引入引出模板`（产品「引入引出模板」功能的出厂预置模板 + 实际数据导出），266 个科目、81 个一级科目。证据等级 **verified**。

### 8.1 科目属性字段全集（20 项）

| 字段 | 键 | 观测分布（266 科目） |
|---|---|---|
| 科目编码 | `number` | 必填 |
| 科目名称 | `name` | 必填 |
| 助记码 | `helpcode` | 拼音首字母 |
| 科目类别 | `accounttype.name` | 13 类枚举，见 §8.2 |
| 余额方向 | `dc` | 借 / 贷，必填 |
| 现金科目 | `iscash` | 是 0 / 否 266 |
| 银行科目 | `isbank` | 是 31 / 否 235 |
| 现金等价物 | `iscashequivalent` | 是 4 / 否 262 |
| **数量核算** | `isqty` | **是 9 / 否 257** |
| 计量单位 | `measureunitid.name` | 随数量核算填写 |
| 外币核算 | `iscurrency` | 是 8 / 否 258 |
| 币别 | `currencyhelp` | 单一币别或「所有币别」 |
| 期末调汇 | `ischangecurrency` | 是 3 / 否 263 |
| **辅助核算** | `isassist` | **是 31 / 否 235** |
| 辅助核算项目 | `checkitemhelp` | 斜杠分隔多值 |
| 是否必录 | `checkitemisrequire` | 与上一字段一一对应 |
| **借方现金流量项目** | `debitcashflow.name` | **231 个科目已配置** |
| **贷方现金流量项目** | `creditcashflow.name` | 同上 |
| 启用状态 | `enable` | 启用 / 禁用 |
| 不允许手工录入凭证 | `isinput` | 是 2 / 否 264 |

### 8.2 科目类别与编码段（ASSBE 确证）

| 类别 | 一级科目数 | 编码段 |
|---|---|---|
| 流动资产 | 31 | 1001 ~ 1901（含一个自建 8888） |
| 非流动资产 | 14 | 1501 ~ 1901 |
| 流动负债 | 10 | 2001 ~ 2401 |
| 非流动负债 | 2 | 2501 ~ 2701 |
| **所有者权益** | 5 | **3001 ~ 3104** |
| **成本** | 5 | **4001 ~ 4403** |
| **营业收入** | 1 | **5001** |
| 营业成本及税金 | 3 | 5401 ~ 5802 |
| 期间费用 | 3 | 5601 ~ 5603 |
| 其他收益 | 3 | 5051 ~ 5301 |
| 其他损失 | 2 | 5402 ~ 5711 |
| 所得税 | 1 | 5801 |
| 以前年度损益调整 | 1 | 6000 |

关键科目：`3001 实收资本`、`3103 本年利润`、`3104 利润分配`、`4001 生产成本`、`4101 制造费用`、`5001 主营业务收入`。

三项指纹（权益 3xxx、成本 4xxx、收入 5xxx）与 ASSBE 一致，与 ASBE（权益 4xxx、成本 5xxx、收入 6xxx）不符。**该账套执行小企业会计准则**，与 Odoo 官方 `l10n_cn` 科目表同构。

### 8.3 数量核算（结 §6.3 悬案）

9 个科目开启数量核算：`1001001 杨`（EA）、`1122005 明细应收`（吨）、`1404 材料成本差异`（g）、`1405 库存商品`、`1902 原材料1`、`1905 测试`（个）、`1907 测试核算`、`2026 测试科目`、`2202006001 测试333`（个）。部分未指定计量单位。

**结论**：数量核算为**科目级开关**，计量单位在科目上指定；凭证分录是否填数量由「科目开关」与「业务凭证模板配置」共同决定，非分录自由字段。

对 Odoo 的含义：载体设计应为 `account.account` 上的布尔开关 + 计量单位字段，配合 `account.move.line` 上的独立数量字段（不得复用 `quantity` / `price_unit`，P-01 约束不变）。

`1001001 杨` 同时开启数量核算（EA）与外币核算（欧元），解释了 记-86 打印件上「单位 EA 有值、单价与数量为空」的现象——三个字段各自独立。

### 8.4 辅助核算

31 个科目开启。一个科目可挂多个维度，每个维度独立标注必录 / 非必录。

维度全集（15 项，括号内为使用该维度的科目数）：客户(16)、供应商(12)、商品(5)、部门(5)、账户(3)、职员(3)、项目号(2)、生产项目(2)、收入类别(2)、支出类别(2)、仓库(1)、品牌(1)、检验方式(1)、有票无票(1)、签约主体(1)。

极端样例：`1501001001` 挂 12 个维度全部非必录；`2202004 暂估应付` 挂 6 个维度，其中「供应商」与「支出类别」为必录。

与 Odoo 对位：Odoo `account.analytic.plan` 支持多维度，`account.account` 无「本科目必须填某维度」的强制约束（`analytic_distribution` 的必填由 `account.analytic.applicability` 按科目前缀规则实现，粒度不同）。判定 **近似**。

### 8.5 现金流量项目

231 个科目配置了现金流量项目，**借方与贷方各一个**，可不同。

样例：

| 科目 | 借方流量项目 | 贷方流量项目 |
|---|---|---|
| 1122 应收账款 | 销售产成品、商品、提供劳务收到的现金 | （同左） |
| 1123 预付账款 | 购买原材料、商品、接受劳务支付的现金 | （同左） |
| 1124 预付账款 | 支付其他与经营活动有关的现金 | 收到其他与经营活动有关的现金 |
| 1101 短期投资 | 短期投资、长期债券投资和长期股权投资支付的现金 | 收回短期投资、长期债券投资和长期股权投资收到的现金 |

**结论**：现金流量项目的**默认值来源为科目 + 方向**，非分录自由指定。

对 `suite_cn_cashflow` 的含义：取数模型应为「科目 × 方向 → 流量项目」的映射表，分录上保留可覆盖字段。分录级能否覆盖科目默认值、一笔分录能否拆分至多个流量项目，本材料未回答，仍待验。

---

## 9. 业务凭证模板全量（第二批）

材料：106 份 `.template`，覆盖 12 个业务域、61 类源单。第一批 41 份为其子集。

### 9.1 域分布

| 域 | 模板数 | 域 | 模板数 |
|---|---|---|---|
| 库存管理 | 30 | 发票管理 | 7 |
| 应收应付 | 18 | 费用报销 | 7 |
| 销售管理 | 14 | 外部单据 | 6 |
| 采购管理 | 8 | 生产管理 | 5 |
| 资产管理 | 4 | 出纳管理 | 3 |
| 工资管理 | 2 | 税务管理 | 2 |

### 9.2 科目解析源扩展至 38 种

第一批的 11 种基础资料默认科目之外，新增：

| 类别 | 表达式 |
|---|---|
| 人员与组织 | `bd_employee.defaultaccount`、`bd_department.defaultaccount` |
| 出纳 | `bd_account.acct`（账户档案对方科目）、`bank_transfer_bill.entryentity.inaccount` / `outaccount` |
| 收支类别 | `bd_payouttype.defaultaccount` |
| 往来备用 | `bd_customer.costaccount`、`bd_supplier.costaccount` |
| 固定资产（18 种） | `fa_card.assetacctid` / `depracctid` / `decpreacctid` / `taxacctid` / `alteracctid` / `expenselist_acctid`、`fa_alter.*`、`fa_clean.cleantypeid.*` |

固定资产的科目全部挂在**资产卡片与变动方式**上，而非商品或往来档案。资产变动方式（`mode.oppositeacctid`）与清理类型（`cleantypeid.clearcostacctid` / `scrapvalueacctid`）各自带对方科目。

### 9.3 新增域的规则形态

**出纳管理**：日记账（`ca_journal`）本身是源单，经 RJZ001 收款 / RJZ002 付款 / RJZ003 通用三张模板回流总账。借贷两侧分别为 `bd_bank.defaultaccount`（账户科目）与 `bd_account.acct`（账户档案上配置的对方科目）。触发条件为「收入金额 不等于 0」/「支出金额 不等于 0」。

**应收应付**：核销单（`arap_verifybill`）按核销类型分五张模板——预付冲应付、预收冲应收、应收冲应付、应收转应收、应付转应付。每张均生成两条分录。

对 Odoo 的含义：Odoo 的核销（`account.partial.reconcile`）**不生成会计分录**，仅建立 move line 之间的勾稽关系；预收预付的科目重分类需另做手工分录或依赖 `account_reports` 的期末重分类功能。此为明确差异，判定 **无**。

**工资管理**：计提工资（借 `bd_department.defaultaccount` / 贷 `bd_employee.defaultaccount`，各挂部门与职员核算项目）；发放工资（应发工资借方，社保个人 / 公积金 / 代扣个税 / 实发合计四条贷方）。

**税务管理**：税金缴纳单（`fp_tax_payment_receipt`）按税（费）种的辅助资料编码分流至增值税与企业所得税两张模板，科目取 `bd_auxinfo.defaultaccount`（辅助资料上的科目）。

**发票管理**：进项发票（`fp_invoice_1`）四张模板，按「专票 / 普票」× 「是否冲暂估」交叉。冲暂估版本较基础版多两条红蓝对冲分录。

**外部单据**：六类虚拟单据（`iac_virtualbill_1..6`，对应销售 / 采购 / 出库 / 入库 / 收款 / 付款），为外部系统数据接入后的凭证生成规则。该形态是金蝶的第三方集成方案。

### 9.4 凭证核算项目的实际使用

全量 106 份模板中，`assitItem` 实际使用 7 个维度：客户(38)、商品(26)、供应商(22)、部门(22)、职员(4)、支出类别(1)、项目号(1)。

科目主数据可配置 15 个维度（§8.4），凭证模板实际驱动 7 个。差额部分依赖手工录入或分录默认带出。

---

## 10. 科目余额表的栏数矛盾

主文件 §4.1 记 KZ-J104 打印模板为六栏（期初借贷 / 本期借贷 / 期末借贷）。

本批取得的**报表导出件**为**八栏**：期初余额（借 / 贷）、本期发生（借 / 贷）、**本年累计（借 / 贷）**、期末余额（借 / 贷）。

即：报表界面输出八栏，KZ-J104 套打模板只印其中六栏。核算项目余额表模板的绑定字段含 `ytddebit` / `ytdcredit`（§4.3），与报表侧的八栏一致。

**主文件 §4.1 与 §5 修订**：六栏为**打印模板的栏数**，非报表的栏数。与 Odoo Trial Balance 作差集时，对照基准应为八栏报表——Odoo 的 Trial Balance 为期初单列 + 本期借贷 + 期末单列，缺本年累计与期初期末的借贷拆分，差距大于原判定。判定仍为「近似」，但差距描述须更正。

