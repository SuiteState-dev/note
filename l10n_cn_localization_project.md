# Odoo 19 中国本地化项目 — 上下文与材料档

版本：v1（2026-08-07 建）
用途：跨窗口上下文恢复。读此一份即可接手 `suite_cn_localization` 线的讨论，不必重发文件包。
证据分级：`verified` = 本轮解析自导出件/源码本身；`observed` = 界面观察或单点样本；`unknown` = 未查。

**权威边界**：本文件是**上下文快照与材料索引**，不是设计权威。设计权威 = 代码仓库内的 `l10n_cn_design.md`；实测行为权威 = `odoo19_*` 笔记家族（B-xx 编号）。三者冲突时以后两者为准。

---

## 0. 一句话定位

给 Odoo 19 Enterprise 做中国本地化的**内账地基层**：做「账」不做「证」。目标客群 = **法定账在外部持有**（代账公司报账，或财务主账在其他软件）、Odoo 承担企业内部经营账的公司。同构先例 = 德国 DATEV 模式。

不上架、不外售，但按标品工程标准做（幂等可重跑 / 版本化 migration / Python 记录写 `ir.model.data` / 扩展同模块不开分支）。

**产品目标**不是「让企业选 Odoo 做账」，是「让已经想用 Odoo 的企业，不被财务部门一票否决」。判据 = 中国会计打开任一报表时**认得出这是一套中国账**。

---

## 1. 模块结构（R20 定，5 个模块）

容器目录 = `suite_cn_localization`（非模块，无 manifest）。

| 模块 | 内容 | depends | 层 | 状态 |
|---|---|---|---|---|
| `suite_cn` | 聚合伞，纯 depends，永不放代码 | 三个子模块 | — | 19.0.1.0.0 |
| `suite_cn_coa` | 科目分级树（`account.group`）+ 原生 TB/GL 中文化说明 | account, account_reports | 呈现层 L0 | 19.0.1.0.0 |
| `suite_cn_statement` | BS/PL 年初/累计列 + 中式报送版式 XLSX 渲染器 | account, account_reports, l10n_cn_reports | 呈现层 L0 | 19.0.1.1.0 |
| `suite_cn_cashflow` | 现金流量项目维度 + ASSBE 直接法 22 行报表 | account, account_reports | 呈现层 L1 | 19.0.1.4.0 |
| `suite_cn_cashflow_statement` | 桥：现金流量 × 版式行次映射，`auto_install` | 上二者 | 呈现层 L0 | 19.0.1.1.0 |

**依赖铁律**：除桥外，任何子模块不依赖兄弟、全部直挂官方。伞只做 depends，HARD BOUNDARY 写死在 manifest 注释里。此举避免重蹈 `l10n_cn_sme`（共享代码底座、一改全连锁）的覆辙。

**已废弃**：`l10n_cn_sme`（名 SME 实为 ASBE，与官方 `cn_large_bis` 撞车）、`suite_cn_reports`（与官方 BS/PL 重叠）。代码在 git 历史 commit `f090bf1`。

**命名预留**：`suite_cn_voucher`（凭证字号，冻结）、`suite_cn_closing`（期末结转，冻结）、`suite_cn_gbexport`（国标导出，归档）。

---

## 2. 设计原则

| 编号 | 原则 | 要点 |
|---|---|---|
| **P-01** | 可卸载性 | 卸载后 `account.move.line` 的 debit/credit/account_id 无变化，试算平衡表与安装前逐账户一致 |
| **P-02** | 顺框架 | 不重新引入 Odoo 已删除对象，不覆写核心计算（余额/对账/报表取数骨架）。可接受：新增字段、新增模型、在既有 hook 插分支且留 fallback |
| **P-03** | Enterprise 前提 | 允许依赖 `account_reports`，不为社区版降级适配 |
| **P-04** | fallback 优先 | 未配置本地化数据的公司，行为与未装模块一致 |
| **P-05** | **可移交性**（R22 拟增） | 每个新增维度须：① 以标准 Odoo 对象承载 ② 具备不依赖本模块的导出通道 ③ 文档列明卸载会丢什么 |

**呈现层做、流程层不做**（§11.0 决策规则）：呈现层只读既有数据、出错当场可见且有 BS 勾稽做外部锚点；流程层写入账簿，出错会在客户账簿留下需人工冲销的错误分录。

---

## 3. 侵入等级与卸载残留现状

| 层 | 本包实例 | 卸载后 |
|---|---|---|
| L0 纯呈现 | 科目分级、报表列、版式渲染 | 无痕 |
| L1 加维度 | `account.move.line.cn_cash_flow_item_id` | **维度数据必然丢失**（原生无此概念），会计数据不变 |
| L2 动核心 | 无 | — |

**已知残留两处**：

1. `cn_cash_flow_item_id` 卸载删列，分类数据不可逆丢失。已写入 README/manifest（E-6）。`uninstall_hook` 先 SQL 置空引用，保留 `ondelete='restrict'` 的运行期保护。
2. **`suite_cn_coa` 的孤儿认领**（R22-T2 待修）：客户手工建的 `account.group` 若前缀撞上目标前缀，会被认领、**改名**（原名无备份）、并在卸载时删除。另 `_adapt_parent_account_group` 会重写该公司全部 group 的父链，含外来组，卸载后不自动还原。

**贴着核心的两处 override**（均未违反 P-02，但需知情）：

- `account.move.line.write()`（cashflow）——aml 上最热的方法，`if 'account_id' in vals` 守卫使无关写入成本可忽略；风险在与其他模块 write override 的叠加顺序
- `account.report._init_options_buttons()`（statement）——全局 override，对任何报表每次 `get_options` 跑一次 search（R22-T3 拟加前置短路）

---

## 4. 金蝶材料库（一手材料，本项目的需求规格来源）

### 4.0 使用铁律

> 金蝶材料是**需求规格来源，不是架构蓝图**。它记录「中国会计期待看到什么」，不是「Odoo 应该怎么实现」。凡材料中的形态源自金蝶自身架构（业务单据与会计凭证分属两个世界、故需规则引擎搭桥），Odoo 无对应结构，**不得据此推断缺口**。

配套解析文件：`l10n_cn_ledger_form_matrix.md`（版式主件）+ `l10n_cn_ledger_form_matrix_supplement.md`（补充件，含科目主数据模型 / 凭证模板全量 / 栏数矛盾）+ `l10n_cn_asbe_chart_reference.csv`（ASBE 科目表 260 行）。

### 4.1 两条产品线，两种架构

| | 金蝶 AI 星辰旗舰版 | 金蝶星云（大型企业） |
|---|---|---|
| 目标 | 小微企业 | 集团 / 大型企业 |
| 准则 | **仅 ASSBE 小企业会计准则**，无 ASBE 选项 | 多科目表并存 |
| 科目表数 | 一账套一套 | **4 套**：新准则(`0001`,系统预置) / IFRS-COA / US-COA / 政府会计(`ZFKJ-KMB`) |
| 计价 | 按账套（含 3 账套，每增 1 账套 ¥1000/年） | 未查 |
| 准则切换 | 建账时选定不可切 | 换科目表 / 换账簿 |
| 科目属性数 | 20 | 59 |
| 现金流量项目载体 | **挂科目**（借贷各一个字段） | **不在科目上**，走独立模块（T型账户指定 / 附表项目指定） |
| 核算维度载体 | 科目上斜杠分隔字符串 | **子表**（`checkitementry`），每维度可配默认值 |
| 科目余额表栏数 | 八栏 | **十栏**（多「年初余额」借贷一组） |

**推论**：「3 账套起订」不宜读作「中国企业普遍做两套账」。大型版的答案是**多科目表 + 多账簿体系**，不是多买账套。需求实质是「同一批业务、多套呈现口径」。

### 4.2 账簿体系（parallel ledger）— `observed`，战略含义最大

星云的账簿命名分两类：`XX公司法人体系主账簿` / `XX公司利润体系主账簿`。同一法人可同时拥有两者。

实测对照（金蝶电器公司，2023年7期，同一套 ASBE 科目表）：

| | 法人体系 | 利润体系 |
|---|---|---|
| 出现科目数 | 43 | 20 |
| 1002 银行存款 年初 | 732,226.00 | 732,226.00（**相同**） |
| 1002 期初 | 102,000.00 | 110,000.00（**不同**） |
| 1002 本年累计 借/贷 | 12,000 / 642,226 | 0 / 622,226 |
| 内部往来科目（1221.99 其他 / 2241.02 供应商往来 / 2241.04 统支款） | 有 | **无** |

即：**同一科目表、同一期初，不同的凭证集合**。内部往来（统收统支）类分录只进法人体系，不进利润体系。差额 20,000 与 12,000 与 T型账户指定界面上的两笔一致。

**含义两条**：

1. 这是 parallel ledger 机制，与 SAP 的 ledger group 同构。Odoo v19 原生无对应层（`availability_condition` 仅 always/country/coa、一公司一 chart、无 `account_consolidation`）。**本项目第一版声明不支持集团 IFRS + 中国 ASBE 平行账，是正确判断而非能力不足。**
2. 本项目的「内账地基」定位正好落在**利润体系**这一格——在金蝶的产品语言里是一个有名字的标准形态，不是自造的边角场景。

**待坐实**：「体系」是否为账簿上的独立字段；同一张凭证如何决定进哪个体系。取法 = 看 `账簿列表`。

### 4.3 ASBE 科目表（`verified`，最高价值的一件）

来源：星云 `新准则会计科目表`（编码 `0001`，**系统预置=是**），260 个科目 / 118 个一级 / 三层。253 个为厂商出厂件，7 个为该演示账套自建。

内容为现行版本：含合同资产/合同负债/合同履约成本（新收入准则）、使用权资产/租赁负债/未确认融资费用（新租赁准则）、信用减值损失（新金融工具准则）。

**编码指纹（ASBE vs ASSBE 判别的权威对照）**：

| 类别 | ASBE（星云） | ASSBE（星辰 / Odoo 官方 `cn`） |
|---|---|---|
| 共同类 | **3101 衍生工具 / 3201 套期工具 / 3202 被套期项目** | 无此类 |
| 所有者权益 | 4001 实收资本 / 4103 本年利润 / 4104 利润分配 | 3001 / 3103 / 3104 |
| 成本 | 5001 生产成本 / 5101 制造费用 / 5301 研发支出 | 4001 / 4101 / 4301 |
| 损益 | 6001 主营业务收入 / 6401 主营业务成本 / 6601 销售费用 | 5001 / 5401 / 5601 |

即「共同类占住 3xxx → 权益推到 4xxx」，与设计文档 §10 E-9 凭财政部原文得出的结论一致，现有厂商预置表实证。

**会计要素 14 类**（ASSBE 13 类，差「共同」）：流动资产 / 非流动资产 / 流动负债 / 非流动负债 / 共同 / 所有者权益 / 成本 / 营业收入 / 其他收益 / 营业成本及税金 / 期间费用 / 其他损失 / 所得税 / 以前年度损益调整。

**预置核算维度绑定 95 个科目**（免费的最佳实践清单）：

| 维度 | 科目数 | 典型绑定 |
|---|---|---|
| 部门 | 64 | 6601/6602 全部费用明细（必录）、5101 制造费用（选填）、6001/6401（选填） |
| 客户 | 8 | 1122.01/.02、2203、2204、1462 |
| 供应商 | 6 | 1123、2202.01/.02、2241.02 |
| 项目 | 8 | 5201 劳务成本、5301 研发支出全线 |
| 组织机构 | 4 | 1221.04 统收款、2241.04 统支款、1511 长期股权投资 |
| 资产类别 | 2 | 1601 固定资产、1602 累计折旧（均必录） |
| 现金账户/银行/银行账户 | 各 1 | 1001 / 1002（均必录） |

**规律**：往来类与费用明细类**必录**，收入成本类**选填**。此规律可直接指导 `account.analytic.applicability` 的配置，不必猜。

明细见 `l10n_cn_asbe_chart_reference.csv`（15 列：编码 / 名称 / English / 会计要素 / 余额方向 / 上级 / 层级 / 现金·银行·现金等价物标志 / 期末调汇 / 外币核算类型 / 数量核算 / 系统预置 / 预置核算维度）。

### 4.4 科目属性模型

**星辰 20 属性**（ASSBE 账套，266 科目）：

`number` / `name` / `helpcode`(助记码) / `accounttype.name`(13类) / `dc`(余额方向) / `iscash` / `isbank` / `iscashequivalent` / **`isqty`(数量核算, 仅9科目开)** + `measureunitid.name`(计量单位) / `iscurrency` + `currencyhelp` + `ischangecurrency` / **`isassist`(辅助核算, 31科目开)** + `checkitemhelp`(斜杠分隔多值) + `checkitemisrequire`(逐维度必录标志) / **`debitcashflow.name` + `creditcashflow.name`(231科目已配)** / `enable` / `isinput`(不允许手工录入凭证)

**星云 59 列**，较星辰新增：`startdate`/`enddate`（**科目版本化日期**）、`accrualdirection`（发生额方向）、`control`（**受控系统**：无/应收/应付/资产管理）、`bw`（表外科目）、`isjournal`（登日记账）、`acnotice`（往来通知）、`isbudget`（预算科目）、`orgcontrollevel`、`createorg`、`accounttable`、`acctcurrency` 三态 + 币种子表、核算维度子表（含每维度**默认值**）。**无现金流量项目字段。**

**三条已结的悬案**：

- **数量核算 = 科目级开关 + 科目上指定计量单位**，非分录自由字段。→ Odoo 侧载体应为 `account.account` 布尔 + uom，配合 aml 独立数量字段。**不得复用 `quantity`/`price_unit`**（承载发票语义，P-01 约束）。
- **辅助核算 = 科目级、可挂多维度、每维度独立必录标志**。维度全集 15 项（客户/供应商/商品/部门/账户/职员/项目号/生产项目/收入类别/支出类别/仓库/品牌/检验方式/有票无票/签约主体）。→ 印证 Odoo 侧选 analytic（R19-T1 已实测锁定）+ `account.analytic.applicability` 的 `account_prefix` × `mandatory` 是同构表达。
- **现金流量项目挂科目且借贷各一个**（星辰）。双向科目两侧不同：`1124 预付账款` 借方=支付其他与经营活动有关、贷方=收到其他与经营活动有关；`1101 短期投资` 借方=投资支付、贷方=收回投资收到。→ Odoo 侧现行 `cn_default_cash_flow_item_id` 单字段设计不足（R22-T1）。

### 4.5 报表取数公式模型（`observed`）

结构 = 报表项目挂一张规则表，每条为三元组 `(科目, 运算符号[加/减], 取数规则[余额/损益发生额])`。结果列随报表类型变：BS 出「期末数/年初数」，PL 出「本期金额/本年累计金额」。

**非公式串、无跨行引用、无 ACCT() 类函数文本。**

→ 对 Odoo 是好消息：`account.report.line` + `account.report.expression` 的 `account_codes` 引擎可表达同构结构。**判定「有」，不构成产品缺口**；缺口在**配置界面**——Odoo 侧需开发者模式操作 expression，无面向会计的规则表 UI。

### 4.6 业务凭证模板 = 单据到凭证的规则引擎（`verified`，106 份）

覆盖 12 业务域、61 类源单、215+ 条分录定义。分录级字段：`accountExp`（科目解析，38 种表达式）/ `dc` / `amount` 算术表达式 / `qty`·`price`·`unit` / `assitItem`（核算项目映射）/ `explanation`（摘要模板）/ `entryCondition`（分录级触发条件）/ `mergeType`（合并规则，编码含义 `unknown`）。

科目解析来源：档案默认科目（`bd_material`/`bd_customer`/`bd_supplier`/`bd_bank`/`bd_employee`/`bd_department`/`bd_payouttype`/`bd_auxinfo`/`bd_account` 等，各档案可挂 default/income/cost 多个用途科目）+ 模板固定科目 + 固定资产卡片/变动方式/清理类型上的科目（18 种）。

**结论：不照搬。** 该层之所以存在，是因为金蝶的业务单据与会计凭证是两个世界；Odoo 的 sale/purchase/stock 各自直接生成 `account.move`，没有这道沟。搬过来 = 第二条记账路径 = 两个真相来源。**此判断记入设计文档 §11.3 已否决表。**

材料的正确用法 = **差异对照表**：逐条看金蝶生成了什么分录、Odoo 对应场景生成了什么、差在哪。差集才是产品输入。

已知明确差异一例：**核销单**（预付冲应付/预收冲应收/应收冲应付/应收转应收/应付转应付，5 类）在金蝶各生成两条分录；Odoo 的 `account.partial.reconcile` **不生成分录**，仅建勾稽关系。判定「无」。

### 4.7 账簿与凭证版式（详见版式矩阵两份文件）

已取得模板约 52 份。要点：

- **两种打印模式并存**：边距 0 = 套打（印在预印刷账页上）；边距 10/19 = 白纸。不可合并为同一纸张配置。
- **明细账是装订册**：封面 / 目录（含每科目起始页码）/ 正文三个独立模板。Odoo 无「按科目分页、编页码、生成目录、装订成册」概念。
- **方向栏（借/贷/平）出现在四张账表上**：三栏式明细账、总分类账、多栏明细账、核算项目明细账。Odoo 以带符号数值表达同一信息。计算列，不改取数路径。
- **动态列分档是产品级设计模式**：多栏账 5 个并存网格、核算项目余额表 3 档（4列1页/6列2页/8列2页）。产品不做自适应布局，按列数选档。Odoo 侧若实现，QWeb 同样需分档或改用可横向滚动的非打印形态。
- **日记账仅五栏**（日期/摘要/收入金额/支出金额/余额），**无对方科目栏**。→ 原生 GL 按现金科目过滤后形态接近，**判定「已覆盖」，不是缺口**。
- **大写金额缺陷**：输出为「贰仟玖佰捌拾点零零」「负壹亿零壹拾壹万捌仟零伍拾肆点伍壹」，未按元角分转换，能处理负号。样本 3 例，`observed`。

---

## 5. 官方 Odoo 存量（历轮实测，`verified`）

| 项 | 官方状态 |
|---|---|
| 科目表 | `cn`(71, ASSBE 3xxx) / `cn_common`(88, 父模板不可单独装) / `cn_large_bis`(202, ASBE 4xxx)。三套 **`account.group` 均为 0** |
| BS/PL | `l10n_cn_reports` 四张：assbe_bs 62行 / assbe_pl 38行 / asbe_bs 75行 / asbe_pl 29行 = 204 行。**均单列** |
| 现金流量表 | **无**（全 l10n_cn* 搜 cash_flow = 空） |
| account.move.line 扩展 | 官方**未加任何字段** |
| 试算平衡表 | `account_reports.trial_balance_report`，custom handler，渲染 期初(单列)｜本期借｜本期贷｜期末(单列)，**期初/期末不拆借贷**，列由 Python 动态生成 → 严格多栏式**不能加列**，须新建报表 |
| 总账 GL | 列 = Date/Partner/Currency/Debit/Credit/Balance。**running balance 有**、**每科目期初余额行有** → 三栏式明细账原生具备，中文化零代码。行由 custom handler 生成 → 加独立列撞墙 |
| zh_CN 译文 | 官方已发（试算表/期初余额/期末余额/借方/贷方/report.name），装语言包即中文 |
| 官方对华投入 | 近 24 月 8 + 14 次提交，全为 CoA/BS/PL/翻译。全仓 `24589`/`金税`/`fapiao` = **0 命中** |

---

## 6. 待办与未决

### 6.1 R22 任务书（已下发开发分支）

| 编号 | 内容 | 性质 |
|---|---|---|
| T1 | 现金流量项目借贷方向拆分（+migration，单独一轮） | 唯一设计改动 |
| T2 | `suite_cn_coa` 孤儿认领残留（原名/父链备份还原） | 卸载洁净性 |
| T3 | `_init_options_buttons` 前置短路 | 性能 |
| T4 | 文档规格更正四条（栏数 / 数量核算载体 / §14 加中国比较系 / §7.2 更新） | 文档 |
| T5 | 新增 P-05 可移交性 + 维度导出通道盘点 | 原则 |

**T1 的架构分叉已定**：自动值 vs 手工值的判定采**等值启发式**（沿用现行「值 == 方向对应侧默认 → 视为自动」），不引入 `is_manual` 布尔——后者对存量行只能一刀切，两种切法都会静默改变历史分类。将来若需精化，升级路径是**记录自动值来源**（存 `(account_id, 方向)`），对存量 null 时自然回退到启发式，不需为历史数据编造答案。此项记入 §7.2 备选，不在本轮做。

**T4-1 修正**：栏数规格按产品线分层——期初/本期/本年累计/期末**四组八栏是共性**（星辰），年初一组是大型版加的（星云十栏）。首版做八栏，十栏作设置项。

### 6.2 §11.7 冻结/待评估清单（知识不冻，开工冻）

严格八栏科目余额表（M1 增强，设置项）/ 数量金额式明细账（撞 GL handler 墙）/ 明细账加独立列（凭证号·摘要·方向，同墙）/ 路线甲（现金集合按 journal 圈定）/ M3 凭证字号（流程层）/ M4 期末结转（流程层，报表默认全选未解）/ 发票金税全电 / 国标导出 GB-T 24589（归档，待三项外部输入 + 买单信号）。

### 6.3 材料缺口

| 缺 | 取法 | 优先级 |
|---|---|---|
| 星云 T型账户指定 / 附表项目指定 / 附表项目调整 | 财务会计→总账→现金流量 三个菜单 | 高（T1 载体对照 + §11.5 路线甲 + ASBE 间接法补充资料） |
| 智能结转凭证的**分录级**材料 | 查凭证打印 PDF，或本年利润科目明细账 | 中（M4 解冻时需要） |
| 账簿「体系」字段的机制 | 账簿列表 | 中（坐实 §4.2） |
| 金蝶 `mergeType` 编码枚举 | 业务凭证模板→合并规则界面 | 低（仅流程层需要） |
| 分录能否覆盖科目带出的现金流量项目 | 录一笔凭证试 | 低 |

**已知空缺**：ASBE 现金流量表的**间接法补充资料**（附表），`suite_cn_cashflow` 现为 ASSBE 22 行直接法单套，完全未覆盖。ASBE 线解冻时是明确缺口。

### 6.4 已作废、勿引用

- 曾把凭证「记-86」判为智能结转产出，并得出「结转产出单张凭证 / 日期取期末日 / 停在保存态」三条——**实为手工凭证**（摘要：支付房租、员工还款），导出件来自「录凭证」菜单。三条全部作废。智能结转的产出形态目前**无任何材料**。
- 曾表述「金蝶星辰是 SaaS，客户连库都摸不到」——在**数据导出**上说过头了。实测科目主数据、余额表、报表、凭证列表、打印模板、业务凭证模板均可导出，科目模板还带回填导入设计。真正的锁定在**配置资产**（231 科目的现金流量映射、31 科目的维度组合、106 份凭证模板、报表取数公式），导出成 xlsx 后无任何其他系统能吃，换系统须重做。

---

## 7. 校验计划（§12.3，未跑）

不上架 → 无下载/转化数据。改以真实客户库上的实施侧指标：

1. **装机人工步骤数** —— 目标 = 装 `suite_cn` + 至多一次「生成中国科目分级」向导。逐客户记录，不收敛即定位失败信号。
2. **中国会计首次可识别度** —— 未培训的会计打开 TB/GL/BS/PL，能否认得出是科目余额表/明细账/资产负债表/利润表。金蝶账簿版式矩阵即这张判据的检查清单。
3. **每客户重复劳动是否归零** —— 新客户上线时本地化相关的手工重配是否为零。

任一不达标 → 回 §11.7 解冻相应项。

**当前状态**：设计有一手材料支撑、代码实测过、卸载验过、官方升级存活验过。**缺的是这三个数**，只能在真实客户库上跑，调研不能替代。

---

## 8. 相关文件索引

| 文件 | 位置 | 内容 |
|---|---|---|
| `l10n_cn_design.md` | 代码仓库 `suite_cn_localization/` | **设计权威**。draft-22，含 §7 验证记录（R1–R21 逐轮）、§10 E 系列决策、§11 模块蓝图、§14 他国差集表 |
| `l10n_cn_ledger_form_matrix.md` | note 仓库 | 中式账簿与凭证版式矩阵（金蝶打印模板解析，主件） |
| `l10n_cn_ledger_form_matrix_supplement.md` | note 仓库 | 补充件：科目主数据模型 / 业务凭证模板全量 / 报表取数公式 / 栏数矛盾 |
| `l10n_cn_asbe_chart_reference.csv` | note 仓库 | ASBE 科目表 260 行 × 15 列（本文件 §4.3 的明细） |
| `l10n_cn_gb24589_data_matrix.md` | 代码仓库 | 国标导出可得性矩阵（**已归档**，待客群信号解冻） |
| `odoo19_index.md` 及家族 | note 仓库 | Odoo 19 实测行为库（B-xx），**技术权威** |
| `R22_task_brief.md` | 一次性交付 | 本轮开发分支任务书 |
