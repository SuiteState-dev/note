# Odoo 19 中国本地化项目 — 上下文与材料档

版本：v4（2026-08-07。对齐 `l10n_cn_design.md` draft-24 / R23 完工；金蝶材料已抽出为独立档）
用途：跨窗口上下文恢复。读此一份即可接手 `suite_cn_localization` 线的讨论，不必重发文件包。
证据分级：`verified` = 本轮解析自导出件/源码本身；`observed` = 界面观察或单点样本；`unknown` = 未查。

**权威边界**：本文件是**上下文快照与材料索引**，不是设计权威。设计权威 = 开发分支仓库内的 `l10n_cn_design.md`（改动频繁，不入 note 仓库，需要时由 Safi 直接上传）；实测行为权威 = `odoo19_*` 笔记家族。三者冲突时以后两者为准。

---

## 0. 一句话定位

给 Odoo 19 Enterprise 做中国本地化的**内账地基层**：做「账」不做「证」。目标客群 = **法定账在外部持有**（代账公司报账，或财务主账在其他软件）、Odoo 承担企业内部经营账的公司。同构先例 = 德国 DATEV 模式。

不上架、不外售，但按标品工程标准做（幂等可重跑 / 版本化 migration / Python 记录写 `ir.model.data` / 扩展同模块不开分支）。

**产品目标**不是「让企业选 Odoo 做账」，是「让已经想用 Odoo 的企业，不被财务部门一票否决」。判据 = 中国会计打开任一报表时**认得出这是一套中国账**。

---

## 1. 模块结构（R20 定，5 个模块）

容器目录 = `suite_cn_localization`（非模块，无 manifest）。

| 模块 | 内容 | depends | 层 | 版本（R23 后） |
|---|---|---|---|---|
| `suite_cn` | 聚合伞，纯 depends，永不放代码 | 三个子模块 | — | 19.0.1.0.0 |
| `suite_cn_coa` | 科目分级树（`account.group`）+ 原生 TB/GL 中文化说明 | account, account_reports | 呈现层 L0 | 19.0.1.1.0 |
| `suite_cn_statement` | BS/PL 年初/累计列 + 中式报送版式 XLSX 渲染器 | account, account_reports, l10n_cn_reports | 呈现层 L0 | **19.0.1.4.0** |
| `suite_cn_cashflow` | 现金流量项目维度（**借/贷双默认**）+ ASSBE 直接法 22 行报表 | account, account_reports | 呈现层 L1 | **19.0.1.7.0** |
| `suite_cn_cashflow_statement` | 桥：现金流量 × 版式行次映射，`auto_install` | 上二者 | 呈现层 L0 | 19.0.1.1.0 |

**依赖铁律**：除桥外，任何子模块不依赖兄弟、全部直挂官方。伞只做 depends，HARD BOUNDARY 写死在 manifest 注释里。此举避免重蹈 `l10n_cn_sme`（共享代码底座、一改全连锁）的覆辙。

**已废弃**：`l10n_cn_sme`（名 SME 实为 ASBE，与官方 `cn_large_bis` 撞车）、`suite_cn_reports`（与官方 BS/PL 重叠）。代码在 git 历史 commit `f090bf1`。

**命名预留**：`suite_cn_voucher`（凭证字号，冻结）、`suite_cn_closing`（期末结转，冻结）、`suite_cn_gbexport`（国标导出，归档）。

---

## 2. 设计原则

| 编号 | 原则 | 要点 |
|---|---|---|
| **P-01** | 可卸载性 | 卸载后 `account.move.line` 的 debit/credit/account_id 无变化，试算平衡表与安装前逐账户一致 |
| **P-02** | 顺框架 | 不重新引入 Odoo 已删除对象，不覆写核心计算（余额/对账/报表取数骨架）。可接受：新增字段、新增模型、在既有 hook 插分支且留 fallback |
| **P-03** | Enterprise 前提 | 允许依赖 `account_reports`，不为社区版降级适配 |
| **P-04** | fallback 优先 | 未配置本地化数据的公司，行为与未装模块一致 |
| **P-05** | **可移交性**（R22 入 §2） | 每个新增维度须：① 以标准 Odoo 对象承载 ② 具备不依赖本模块的导出通道 ③ 文档列明卸载会丢什么 |

**呈现层做、流程层不做**（§11.0 决策规则）：呈现层只读既有数据、出错当场可见且有 BS 勾稽做外部锚点；流程层写入账簿，出错会在客户账簿留下需人工冲销的错误分录。

---

## 3. 侵入等级与卸载残留现状（R23 后）

| 层 | 本包实例 | 卸载后 |
|---|---|---|
| L0 纯呈现 | 科目分级、报表列、版式渲染 | 无痕 |
| L1 加维度 | `account.move.line.cn_cash_flow_item_id` | **维度数据必然丢失**（原生无此概念），会计数据不变 |
| L2 动核心 | 无 | — |

**残留现状**：

1. `cn_cash_flow_item_id` 卸载删列，分类数据不可逆丢失。已写入 README/manifest（E-6）。`uninstall_hook` 先 SQL 置空引用，保留 `ondelete='restrict'` 的运行期保护。**P-05 盘点认定这是唯一无导出通道的维度**，导出功能仍未实现——**这是 P-05 立项当轮盘出、至今未闭合的唯一一处原则违反**。
2. ~~`suite_cn_coa` 孤儿认领~~ **已修（R22-T2）**。修复过程发现一条更深的问题：`account.group.parent_id` 是 **`ondelete='cascade'`**——删自建组会级联删掉挂其下的用户认领组，比「认领组被改名后删除」更隐蔽。处置 = 新增 `suite.cn.coa.adopted.group` 台账（记原名全语言 jsonb + 原父）；`uninstall_hook` 先释放认领组（整列 jsonb 精确重置、仅释放不删）→ 删自建组前把幸存组从待删父摘开 → 末尾 `_adapt_parent_account_group` 重建前缀父链。V1–V5 全过。

**贴着核心的两处 override**（均未违反 P-02，但需知情）：

- `account.move.line.write()`（cashflow）——aml 上最热的方法，`if 'account_id' in vals` 守卫使无关写入成本可忽略；风险在与其他模块 write override 的叠加顺序。**R23-T5-1 追加红冲守卫**：reassign 过滤加 `not l.move_id.reversed_entry_id`。
- `account.report._init_options_buttons()`（statement）——全局 override。**R23-T4-2 起短路依赖 `self.country_id`（company-independent），不再依赖 `env.company`**（R22-T3 的旧写法在「默认公司非 CN + 选择器勾 CN」组合下会误伤隐藏按钮）。

---

## 4. 金蝶材料（结论摘要）

> **完整材料档 = `l10n_cn_kingdee_material.md`**（版式 / 科目主数据 / 凭证规则引擎 / 报表取数公式 / 差集总表 / 已作废条目）。本节只留**驱动设计决策的结论**，细节一律去材料档，不在两处维护。

**使用铁律**：金蝶材料是**需求规格来源，不是架构蓝图**。两种相反的误用都要防——**用窄**会把金蝶架构产物当成 Odoo 缺口（业务凭证模板规则引擎即此类，已否决）；**用宽**会把中国法定形态误判掉（银行余额调节表即此类，R23-T5-2 已更正为真缺口）。区分判据：问该形态**在中国是否法定 / 通用**，而不是问它在金蝶里长什么样。

### 4.1 驱动了设计决策的六条

| # | 结论 | 影响 |
|---|---|---|
| 1 | **ASBE vs ASSBE 编码指纹**：ASBE 共同类占 3xxx → 权益 4001 / 成本 5001 / 损益 6001；ASSBE 权益 3001 / 成本 4001 / 损益 5001。官方 `cn` = ASSBE、`cn_large_bis` = ASBE | 准则归属的判据；`l10n_cn_asbe` 线的科目表对照源 = `l10n_cn_asbe_chart_reference.csv`（260 科目，厂商预置） |
| 2 | **现金流量项目挂科目且借贷各一个**，双向科目两侧不同（1124 预付账款、1101 短期投资） | 驱动 R22-T1 方向拆分（已完工） |
| 3 | **数量核算 = 科目级开关 + 科目上指定计量单位**，非分录自由字段 | 数量金额式明细账解冻时的载体结论；不得复用 `quantity`/`price_unit` |
| 4 | **辅助核算 = 科目级、多维度、每维度独立必录标志**；ASBE 表预置 95 科目绑定，规律为「往来与费用明细必录、收入成本选填」 | 印证 analytic 载体选择（R19-T1 已锁定）；可直接指导 `account.analytic.applicability` 配置 |
| 5 | **报表取数公式 = (科目, 加/减, 取数规则) 三元组清单**，非公式串 | 判定「有」——`account.report.expression` 可表达。缺口在**配置界面**不在功能 |
| 6 | **科目余额表报表输出八栏**（期初/本期/本年累计/期末各拆借贷；星云十栏另加年初），六栏只是打印模板栏数 | §11.7 解冻时按八栏立规格 |

### 4.2 账簿体系 = 平行账簿（`observed`，战略含义最大）

星云同一法人可同时有「法人体系主账簿」与「利润体系主账簿」。实测（金蝶电器公司 2023年7期）：**同一科目表、同一期初、不同的凭证集合**——内部往来（统收统支）类分录只进法人体系，不进利润体系。

两条含义：

1. 这是 parallel ledger 机制，与 SAP ledger group 同构。Odoo v19 无对应层 → **「第一版不支持集团 IFRS + 中国 ASBE 平行账」是正确判断，非能力不足**（R17-E1）。
2. **本项目的「内账地基」定位正好落在利润体系这一格**——在金蝶的产品语言里是有名字的标准形态，不是自造的边角场景。

### 4.3 两条产品线的关键差异

| | 星辰（小微） | 星云（大型） |
|---|---|---|
| 准则 | **仅 ASSBE**，无 ASBE 选项 | 4 套科目表并存（新准则/IFRS/US/政府会计） |
| 现金流量项目载体 | 挂科目（借贷各一字段） | 不在科目上，走独立模块 |
| 科目属性数 | 20 | 59 |
| 科目余额表 | 八栏 | 十栏 |

**推论**：「3 账套起订」不宜读作「中国企业普遍做两套账」。大型版的答案是多科目表 + 多账簿体系。需求实质是「同一批业务、多套呈现口径」。

---

## 5. 官方 Odoo 存量（历轮实测，`verified`）

| 项 | 官方状态 |
|---|---|
| 科目表 | `cn`(71, ASSBE 3xxx) / `cn_common`(88, 父模板不可单独装) / `cn_large_bis`(202, ASBE 4xxx)。三套 **`account.group` 均为 0** |
| BS/PL | `l10n_cn_reports` 四张：assbe_bs 62行 / assbe_pl 38行 / asbe_bs 75行 / asbe_pl 29行 = 204 行。**均单列** |
| 现金流量表 | **无**（全 l10n_cn* 搜 cash_flow = 空）。官方通用 Cash Flow（id5）是 `country=None` + `account.cash.flow.report.handler` |
| account.move.line 扩展 | 官方**未加任何字段** |
| 试算平衡表 | `account_reports.trial_balance_report`，custom handler，渲染 期初(单列)｜本期借｜本期贷｜期末(单列)，**期初/期末不拆借贷**，列由 Python 动态生成 → 严格多栏式**不能加列**，须新建报表 |
| 总账 GL | 列 = Date/Partner/Currency/Debit/Credit/Balance。**running balance 有**、**每科目期初余额行有** → 三栏式明细账原生具备，中文化零代码。行由 custom handler 生成 → 加独立列撞墙 |
| zh_CN 译文 | 官方已发（试算表/期初余额/期末余额/借方/贷方/report.name），装语言包即中文 |
| 官方对华投入 | 近 24 月 8 + 14 次提交，全为 CoA/BS/PL/翻译。全仓 `24589`/`金税`/`fapiao` = **0 命中** |

---

## 6. 待办与未决

### 6.1 R22 已完工（draft-23，全实测）

| 编号 | 结果 |
|---|---|
| T1 | 现金流量项目借贷拆分。`account.account` 加 `cn_default_cash_flow_item_debit_id`/`_credit_id`；方向 = **`balance` 符号**（`balance<0`→贷方侧，零额取借方侧）；compute/write 重排为「**仅当值为空或等于旧方向自动值才重带，否则保留手工**」。旧单字段 deprecated + post-migration 等值拷入。V1–V9 全过；500 行 create 1.38s / 整批翻转 1.73s |
| T2 | 孤儿认领卸载还原（见 §3 残留现状） |
| T3 | `_init_options_buttons` 前置短路（**实现已由 R23-T4-2 更正，见下**） |
| T4 | 文档更正四条全部落地（八栏 / 数量核算载体 / §14.1 金蝶比较系 / §7.2 加两项待决） |
| T5 | P-05 入 §2 + 盘点表（`cn_cash_flow_item_id` 无导出通道 = 唯一缺口） |
| — | §11.3 增「业务凭证模板规则引擎」否决条目 |

**T1 的架构分叉决策（保留备查）**：自动 vs 手工采**等值启发式**，不引入 `is_manual` 布尔——后者对存量行只能一刀切，两种切法都会静默改变历史分类。将来若需精化，升级路径是**记录自动值来源**（存 `(account_id, 方向)`），对存量 null 时自然回退到启发式，不为历史数据编造答案。

### 6.2 R23 已完工（draft-24，全实测，2026-08-07）

R22 交付后首次做**浏览器界面端到端走查**（历轮验收以 `_get_lines()` 程序化跑数为主）。BS/PL 的中式版式 XLSX 逐行核对无误；问题全不在取数与渲染，而在**可达性 / 生成器守卫 / 验收覆盖面**。

| 编号 | 结果 |
|---|---|
| **T1** | 中式现金流量表可达性 = **挂官方变体（甲）**。`cn_cash_flow_s` 原 `root_report_id/section_report_ids/variant_report_ids` 三者全空 → 菜单无入口。修法 = 设 `root_report_id=account_reports.cash_flow_report`，照抄官方 CN BS/PL 变体形态。**甲的唯一风险（官方 custom handler 污染取数）实测证伪**：我方 `custom_handler_model_name=False`，变体渲染直接走自身 `report_id/line_ids`（domain 引擎）。两 CN 公司打开官方 Cash Flow 菜单自动落我方报表、22 行两列、数值 identical、**≤3 击可达**（无开发者模式、无手敲 URL）。cashflow 1.5.0→1.6.0 |
| **T2** | 年初/累计列生成器守卫 = **单趟单层漏洞，真 bug 全新库复现**。`CN_TCI.ytd`（综合收益总额）引用不存在的 `CN_NEPOIAT.ytd` → 打开 PL 即 `Could not expand term`。根因链：`CN_NEPOIAT` 的子行是 **`external` 引擎**（手工录入、无可导 ytd）→ 官方基表不发该 label → 生成器正确跳过子行、**却漏跳父行**（单趟扫描不把「中间层被跳过」向上传播）。修法 = **不动点判据**（`S={能直接产出目标 label 的行}`，迭代并入「引用全 ∈ S」的 agg 行至稳定，仅为 S 中的行生成）+ 删孤儿 `exp_127_ytd` + **装机后自检 `_log_dangling_expressions`**（`post_init_hook` + `post-migration 19.0.1.3.0`）+ 2 单元测试。实测 192 表达式仅 1 孤儿、四报表两 CN 公司渲染无错。statement 1.2.0→1.4.0 |
| **T3** | 中式 XLSX 按钮折进齿轮 = **缺 `always_show`，真因非 sequence**。`buttons_bar.js` 的 `barButtons` 只收 `always_show:true`，其余一律进 `cog_menu`。修法 = 加 `always_show:True` + `sequence:5`（排在官方 PDF/XLSX 之前） |
| **T4-1** | 行次**有序**（完整性≠有序）。R21 只验了「无缺无重」；本轮官方导出即 33 行一个不少、顺序全错。断言 = `row_no` 逐个转 int、单调递增无缺号，**两栏对开表左右各自单调**。实测 BS 左 1..30 / 右 31..53、PL 1..32、CF 1..22。固化为数据无关单元测试 |
| **T4-2** | 多公司短路误伤 = **别依赖 `env.company`**。R22-T3 的短路在单公司切换下通过，但「默认公司非 CN（Chicago, chart=False）+ 选择器勾 CN 公司」组合下 `env.company` 取默认公司 → 误伤隐藏按钮。修法 = 改依赖报表自身 `self.country_id`（company-independent） |
| **T4-3** | 界面可达性纳入 §7.0 验收惯例（铁律，见下） |
| **T5-1** | 红冲/反过账方向 = 行次层面双边虚增，**本轮已修**。见下方专条 |
| **T5-2** | §14.1 银行余额调节表**改判真缺口**（法定形态非金蝶产物），并在 §14.1 引言加区分判据 |
| **T5-3** | deprecated `cn_default_cash_flow_item_id` **删除窗口 = 19.0.1.7.0，本轮同步删除**（model + 约束 + 列；migration 走 raw SQL，因 `odoo.upgrade.util` 未装）。cashflow 1.6.0→1.7.0 |

**T5-1 红冲守卫（本轮唯一代码型设计改动）**

- **机制（源码 `account_move.py:5455` 实读）**：`_reverse_moves` 先 `move.copy(default_values)`（`reversed_entry_id` 在 default 里；`cn_cash_flow_item_id` 因 **`copy=True` 带原值**，此刻 balance 未翻、compute 保留原项目）→ 再 `write({'line_ids':[Command.update(..., {'balance': -balance})]})` 翻符号，**正是这个 write 触发我方 override 把原值当自动值重算成对侧项目**。
- **落点 = 便宜那条**：`copy=True` 已带原值，守卫加在 `write()`（reassign 过滤加 `not l.move_id.reversed_entry_id`）+ `_compute`（`reversed_entry_id` 为真即 `continue`）即可，**无需行级匹配**。Odoo 无 `reversed_line_id`、行序非承诺 API，那条脆且贵一个量级——本轮实测 `copy=True` 把它排除掉了。
- **实测**：修前 行2(S2)=+1000 未冲回 / 行6(S6)=−1000 幻影 / 净额 0；修后 **行2=0 / 行6=0 / 净额 0**。+2 单元测试（红冲保留 / 普通方向编辑仍重算）。
- **声明边界（不得被「已修红冲」掩盖）**：① 守卫**只认走 `_reverse_moves` 的红冲**（有 `reversed_entry_id`）；手工直接录负数红字无此字段、守卫抓不到，且与「真的手工改方向」不可区分。② Odoo `debit`/`credit` **非负**，「借方红字」被自动存成「贷方蓝字」→ **中式红冲第二常见形态在 Odoo 里无法原样表达**，属框架层非本模块可修。

**§7.0 新增三条验收惯例（跨轮铁律，R23-T4 产出）**

1. **界面可达性**：凡交付物带界面入口者（报表/菜单/按钮/向导），验收必须包含一次**不用开发者模式、不手敲 URL** 的走查，记录「登录到看见」的点击路径与步数。程序化跑数通过 ≠ 用户找得到。
2. **多公司组合**：凡逻辑读 `env.company` 或依公司属性门控者，须实测「**默认公司非 CN + 选择器勾 CN 公司**」组合，不能只测单公司切换。优先改依赖对象自身属性。
3. **有序性独立于完整性**：导出件除「无缺无重」外须单独断言**有序**。

**贯穿性教训（本轮最大产出）**：程序化验收与界面使用从未对齐——**数值正确 ≠ 用户找得到；行次完整 ≠ 行次有序；单公司通过 ≠ 多公司通过**。本轮多数问题在历轮验收里都「通过」过。

### 6.3 R23 收尾未清（下轮先清，成本都很低）

| 项 | 内容 |
|---|---|
| **设计文档 §7.1 陈旧** | 第 515 行的 T5-1 条目仍写「本轮只出事实 / 特判待产品判断」，与已改为「已实施」的 §7.2、§9 直接打架。§7 自己规定「检索先看 7.1/7.2」，7.1 是第一入口 |
| **设计文档 §7.2 重复段** | T5-1 行内「落点=便宜那条」整段出现两次，前一段是草稿残留（无具体落点），删前留后 |
| **T5-3「留一轮」压缩成同轮需记依据** | 1.5.0 标 deprecated、1.7.0 删除，中间不到一天。技术上安全（`-u` 会按版本序先跑 1.5.0 post-migration 拷值、再跑 1.7.0 pre-migration 删列；且 R20-P0 已确认无外部库安装），但**理由未写进文档**，日后会读成违规。应补一句「为什么这次可以压缩 / 什么情况下不可以」 |
| **CF 中式版式 XLSX 端到端导出未验** | 原记于 R23 下发稿「报表本身够不着，T1 修完须补」。T1 已修完、报表已可达，但 draft-24 未见对应记录。T4-1 那条 CF 1..22 有序性明写是「`row_no` 来自静态配置的**数据无关单元测试**」，不等于真点一次按钮导出一张 CF |
| **红冲守卫的第三条边界待补测** | 守卫是「`reversed_entry_id` 为真 → compute 直接 `continue`」，副作用是**红冲分录上该字段从此完全手动**。`_reverse_moves(cancel=False)` 产出草稿态，若用户在草稿态改某行 `account_id`，不会带出新科目默认值、会静默留着从原分录复制来的分类。跑一笔即知；若确认如此，作为边界③写死 |

### 6.4 下一轮开工候选（排序建议，非决定）

1. **路线甲 / Outstanding 清算户口径**（设计 §7.2 A2-口径 + §11.5 方案书，R13-B1 出稿后一直「待实施」）。这是 CF 模块唯一还在**数值覆盖面**上的缺口——客户走 register payment 时款项走 Outstanding（`asset_current`），对账前不进表，P9 只是止血。重要度高于行次排版与按钮位置一个量级，却被排在它们后面拖了十轮。
2. **`cn_cash_flow_item_id` 导出通道**（P-05 唯一未闭合的原则违反）。成本约等于一个 xlsx 导出 + 反向导入，换原则闭合。
3. **银行余额调节表探查**（R23-T5-2 刚改判真缺口）。未解的是「Odoo 对账模型能否取出未达账项四类」——**只读探查非开工**，便宜，且结论决定它能不能做。
4. **A2-方案：CF 必填约束对技术行过度触发**（税/汇兑/rounding，倾向甲 = 约束排除这些 display_type）。小，但直接影响开箱体验。

### 6.5 §11.7 冻结/待评估清单（知识不冻，开工冻）

严格八栏科目余额表（M1 增强，设置项）/ 数量金额式明细账（撞 GL handler 墙）/ 明细账加独立列（凭证号·摘要·方向，同墙）/ 路线甲（现金集合按 journal 圈定）/ **银行余额调节表（R23-T5-2 新入，真缺口）** / M3 凭证字号（流程层）/ M4 期末结转（流程层，报表默认全选未解）/ 发票金税全电 / 国标导出 GB-T 24589（归档，待三项外部输入 + 买单信号）。

### 6.6 材料缺口

| 缺 | 取法 | 优先级 |
|---|---|---|
| 星云 T型账户指定 / 附表项目指定 / 附表项目调整 | 财务会计→总账→现金流量 三个菜单 | **高**（同时服务三个下游：流量项目载体对照 + §11.5 路线甲 + ASBE 间接法补充资料；取法只是去界面点几下，已挂两轮） |
| 智能结转凭证的**分录级**材料 | 查凭证打印 PDF，或本年利润科目明细账 | 中（M4 解冻时需要） |
| 账簿「体系」字段的机制 | 账簿列表 | 中（坐实 §4.2） |
| 金蝶 `mergeType` 编码枚举 | 业务凭证模板→合并规则界面 | 低（仅流程层需要） |
| 分录能否覆盖科目带出的现金流量项目 / 一笔分录能否拆多个流量项目 | 录一笔凭证试；中国实务口径须另确认 | 低（已入设计 §7.2） |

**已知空缺**：ASBE 现金流量表的**间接法补充资料**（附表），`suite_cn_cashflow` 现为 ASSBE 22 行直接法单套，完全未覆盖。ASBE 线解冻时是明确缺口。

### 6.7 已作废、勿引用

- 曾把凭证「记-86」判为智能结转产出，并得出「结转产出单张凭证 / 日期取期末日 / 停在保存态」三条——**实为手工凭证**（摘要：支付房租、员工还款），导出件来自「录凭证」菜单。三条全部作废。智能结转的产出形态目前**无任何材料**。
- 曾表述「金蝶星辰是 SaaS，客户连库都摸不到」——在**数据导出**上说过头了。实测科目主数据、余额表、报表、凭证列表、打印模板、业务凭证模板均可导出，科目模板还带回填导入设计。真正的锁定在**配置资产**（231 科目的现金流量映射、31 科目的维度组合、106 份凭证模板、报表取数公式），导出成 xlsx 后无任何其他系统能吃，换系统须重做。

---

## 7. 校验计划（设计 §12.3，**至今未跑**）

不上架 → 无下载/转化数据。改以真实客户库上的实施侧指标：

1. **装机人工步骤数** —— 目标 = 装 `suite_cn` + 至多一次「生成中国科目分级」向导。逐客户记录，不收敛即定位失败信号。
2. **中国会计首次可识别度** —— 未培训的会计打开 TB/GL/BS/PL，能否认得出是科目余额表/明细账/资产负债表/利润表。金蝶账簿版式矩阵即这张判据的检查清单。
3. **每客户重复劳动是否归零** —— 新客户上线时本地化相关的手工重配是否为零。

任一不达标 → 回 §11.7 解冻相应项。

**当前状态与真正的瓶颈**：设计已跑到 draft-24 / R23，五个模块代码全实测、卸载验过、官方升级存活验过。但这三个数**一个都没跑**，因为没有真实客户库。功能面继续加厚（八栏余额表、数量金额式明细账、银行调节表……）边际收益在下降，而「中国会计打开报表认不认得出」这个唯一的外部锚一次都没测过。

**下一个真正的瓶颈不在待办清单里任何一条，是找到第一个愿意让本模块在他库上装一次的中国客户。**

---

## 8. 相关文件索引

| 文件 | 位置 | 内容 |
|---|---|---|
| `l10n_cn_design.md` | **开发分支仓库** `suite_cn_localization/`（改动频繁，不入 note，需要时直接上传） | **设计权威**。draft-24，含 §7 验证记录（R1–R23 逐轮）、§10 E 系列决策、§11 模块蓝图、§14 差集表（含 §14.1 金蝶比较系）、§15 报送版式逐行对照 |
| `l10n_cn_kingdee_material.md` | note 仓库 | **金蝶材料档**（本文件 §4 的展开）：版式 / 科目主数据 / 凭证规则引擎 / 报表取数公式 / 差集总表 / 已作废条目 |
| `l10n_cn_asbe_chart_reference.csv` | note 仓库 | ASBE 科目表 260 行 × 15 列 |
| `l10n_cn_gb24589_data_matrix.md` | 开发分支仓库 | 国标导出可得性矩阵（**已归档**，待客群信号解冻） |
| `odoo19_accounting_mechanics.md` | note 仓库 | Odoo 19 实测行为（估值架构、成本法、Landed Cost、期末关账），**技术权威**。B-xx 家族其余文件未推仓库 |
| `background.md` | note 仓库 | 跨项目背景快照。本线产出的**通用**踩坑（Odoo.sh rebuild、migration util、报表按钮/短路、`_reverse_moves` 语义）已归入其 §7/§8，不在本文件重复 |

**已作废**：`l10n_cn_ledger_form_matrix.md` 与 `l10n_cn_ledger_form_matrix_supplement.md` 已合并入 `l10n_cn_kingdee_material.md`，两份旧文件已从仓库删除（raw 返回 404，与 `index.md` 一致）。
