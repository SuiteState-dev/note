# l10n_cn 项目状态档

**版本**：s-07（2026-08-11 整体重写，吸收 R33-A 自发科目表交付轮 + 电子税务局外部验证轮）
**配套**：`项目档 v24` / `design draft-39` / `background v11`
**仓库**：note
**职责**：**当前状态**。已交付 / 冻结中 / 开工候选 / 收尾未清 / 材料缺口 / 开放项 / v20 迁移面。

> **本档规则（务必遵守）**
> 1. **每轮整体重写，不留历史，不加删除线。** 历史由项目档 changelog 与 design §17 逐轮记录承担。
> 2. **本档不放规则、不放口径、不放材料内容。** 那些在项目档。本档只放「现在处在什么位置」。
> 3. **本档不放已作废结论。** 作废清单永远在 `项目档 §6.7`。
> 4. 引用其他文件一律带版本号：`项目档 v22 §x`、`design draft-36 §y`、`background v9 §z`。
> 5. 本档每轮回写时更新，**与项目档解耦** —— 改本档不需要动项目档。

---

## 1. 现在的位置

**R30 全轮收口 → R31 v20 源码定位（只读）→ R32 dev 库实测 → R33-T0 取数机制实测，四轮连续收口。**

🟢 **R33-A 已交付闭合，自发科目表这条线走完。** 同轮拿到项目至今最强的一批外部真值（电子税务局实测）。

- **R31（只读）**：读 v20 两仓源码，产出 v20 破坏面清单（十条，见 §10），全部 `observed`。副产品是发现一条 **19.0 的现存疑点** —— `account.account.code` 按公司多值（`code_store`）、引擎带跨公司兜底。
- **R32（实测）**：把 R31 那条疑点在 dev 库跑实，**坐实为一个 19.0 的静默错报**（§6 首行，`verified`）；同轮解决 T2（executed 版 ASBE form 建法）与 T3（`vat` 填写）两件收尾。

🔴 **R32 的核心结论（三句，一起说才不误导）**：
1. **跨公司 code 兜底在 19.0 存在，且会把余额吃到错行**（F3，`verified`）。
2. **吃错后 `_CN_CROSSFOOT` 抓不到，静默错报，与 `4001` 同级**（F4，`verified`）。
3. **但这个危险状态普通 ORM 造不出来** —— `create` 无条件要求每家公司都有 code，只有导入 / 建表模板 / load 这类 `defer_account_code_checks` 路径能产生（T1-0-d，`verified`）。
⇒ **触发面窄（不是日常操作会踩的雷），但一旦触发就是静默错报。** 排期时两句都要在。

🔴 **R33-T0 的核心结论（取数机制,直接定自发科目表的做法）**：我方 **BS/PL 四张 form 纯按编码前缀取数、不看 `account_type`**（`background v10 B-64`，`verified` live）；官方通用报表按类型；两条路径冲突时各不相让。**且 CN 公司默认打开的「资产负债表」就是我方的编码变体、不是按型的通用版。** 唯一例外是 CF（按 `asset_cash` 判现金）。⇒ 自发科目表时 **编码号段必须对，`account_type` 只有货币资金类必须钉死**，其余次要。这把 B-60 的类型恐慌收窄到只剩现金类。

🟢 **R33-A（交付）**：`suite_cn_coa` 2.0.0 由「分级树生成器」升级为「**科目表发行方**」。发行国标小企业（财会〔2011〕17号）**68 一级 + 32 明细**；`post_init` 发通用 88 条、wizard 按纳税人身份（一般/小规模）补税费档；认领优先、幂等、纯 ORM 不写 `ir.model.data`、另设发行台账。tests：`suite_cn_coa` 9 绿（含 V6/V6b）+ `suite_cn_statement` 21 绿。

🔴 **R33-A 的核心结论（三句）**：
1. **E-7 编码体系判定 = 二选一**，取数口径以我方 101 条为准（`项目档 v24 §4.5.12 E-7`）。逐码 diff 实测：取数层碰撞面**只有 1 个码**（`1406`）。**二选一 ≠ 清场**，不删客户已有的官方科目。
2. **连带对官方 ASSBE 报表 5 处 formula 外科覆盖**，先存原值、`uninstall_hook` 写回。🔴 `4001` 必须从**【损益 + 权益】两处成对摘**，只摘一边 `资产 ≠ 负债+权益`。
3. 🔴 **作废了一条 `verified`**：`R30-P3 ②`「存货 4001 报表正确排除」判错（`项目档 v24 §6.9`）。**note 侧的错**——引了原文却没让原文管住结论。

🟢 **外部验证（电子税务局，和界鸿源，深圳，ASSBE）**：`.xlsx` 被接受、我方导出件**逐格落位全中**（`verified`）。**§9 指标2 的外部锚首次到手。** 十条结论见 `项目档 v24 §4.5.18`。
🔴 **和界鸿源账号上禁止造数** —— 实测确认数据异常会进「我的提醒」，与**信用失分**同池。前期的编造数据测试方法有误，已停。

### 🔴 下一个窗口的待办

| # | 待办 | 谁做 | 大小 | 依据 |
|---|---|---|---|---|
| **1** | **ASBE 未执行版 form** —— 前置已解除（R32-T2） | 开发 | 中 | §5 第一梯队 #1 |
| **2** | **十四行取到 0 时出口径提示** —— 可并行 | 开发 | 小 | §5 |
| **3** | 🆕 **`suite_cn_coa` 升级路径策略** —— 发行不写 `ir.model.data` ⇒ 无 `noupdate` 语义，骨架改版时靠自建台账判定。机制已在，策略未定 | **Safi 拍**，不急 | 小 | §8 第 11 项 |
| **4** | 🆕 **中式内部账表版式** —— 新交付物候选，等客户信号 | 待定 | 中 | §5 候选但不排期 |

🟢 **上一窗口四项已销两项**：R33-A 已交付；F3 静默错报处置（选①「每公司必有 code」校验）已随 R33-A §4.7 落地、V5 多公司测试覆盖。

**不做**：任何 v20 适配。v20 未发版、`20.0` 分支尚未切出，落公式违 `项目档 §9.2 第2条`。§10 只做清单，不做工程。

---

## 2. 已交付

| 项 | 模块 | 状态 |
|---|---|---|
| 🆕 **自发 ASSBE 科目表（发行方）** | `suite_cn_coa` 2.0.0 | R33-A。68 一级 + 32 明细；`post_init` 发通用 88 + wizard 按纳税人身份补税费档；认领优先/幂等/纯 ORM 不写 `ir.model.data`/发行台账；建表校验「每公司必有 code」；成对科目删除告警（走 `dc_notice`）。**不碰分级树。** 9 tests 绿（含 V6/V6b） |
| 🆕 **E-7 报表国标口径覆盖** | `suite_cn_coa` 2.0.0 | 对官方 ASSBE 报表 5 处 formula 外科覆盖（3 行 × 2 表达式 + 1 条 `hide_if_zero`），先存原值、`uninstall_hook` 写回。口径详见 `项目档 v24 §4.5.12 E-7` |
| 五张报送 form（ASBE BS/PL、ASSBE BS/PL、现金流量表），192 表达式 | `suite_cn_statement` / `suite_cn_cashflow` | ✅ 中式版式导出可用 |
| 双列取数（期末/年初、本年累计/本月），`date_scope` 静态双列 | 同上 | ✅ |
| 上期列走原生 comparison，filter = `same_last_year` | 同上 | ✅ 税局「上期」= 上年同期 |
| 报送 form 三维（`period_scope` × `standard_version`） | 同上 | ✅ R26 建好；R30 第二次兑现 |
| `_CN_CROSSFOOT` 表内勾稽守卫 + 双通道曝光（WARNING + 导出件红字） | 同上 | ✅ R24 起；R30 换原文断言 |
| 不等式断言类型 `op='gte'` + 第三类破口「内含项超出」 | 同上 | ✅ R30-P2 |
| ASSBE 往来四行方向分流（B-58 装配式） | 同上 | ✅ R30-T2a　🔴 **机制在 v20 已被官方删除，见 §10 M-2** |
| ASBE 应付/预付 明细级 D/C 方向分流 | 同上 | ✅ R30-T2b　🟢 D/C 在 v20 保留 |
| 行19 累计折旧符号修正（`_CN_NEGATE`） | 同上 | ✅ R30-P3 |
| M3a 中式记账凭证呈现层 | `suite_cn_voucher` | ✅ R29-T1，commit `3e740ef`　🟢 宿主 `account_voucher_cn` 在 v20 仍在 |

**勾稽守卫当前条数**：ASBE BS 9 / PL 6（**自推，永无原文可换**，见 `项目档 v22 §4.5.12 八`）；ASSBE BS 11 / PL 9（原文）；CF 5。

🔴 **全部已交付物均带一条新确认的风险**：多公司共享账户 + 本公司 code 缺失时的前缀吃入（§6 首行，R32 已坐实为静默错报）。它不区分模块、不区分 form。

---

## 3. 待下发 / 进行中

| # | 项 | 状态 |
|---|---|---|
| 1 | **design draft-36 四处落档缺陷** | 🔄 开发分支执行中（状态未回报） |
| 2 | **design §17.4 B-59 撞号订正** | 🔄 T2-b 不占新 B 号，跨项目部分并入 B-55 第四条锚；公式 home 在 `项目档 §4.5.12` |
| 3 | **design §17.4 B-60 按收窄版改写** | 📄 缺陷 = WIP 建模错配，不是编码体系错配 |
| 4 | **十四行取到 0 时出口径提示** | 📄 **可立即下发** |
| 5 | **R31 + R32 两轮 note 回写** | 🔄 本轮（background v9 / status s-05 / 项目档 v22）执行中 |

---

## 4. 冻结中

| 项 | 冻结原因 | 解冻条件 |
|---|---|---|
| **M3b 凭证签章流程层** | 需真实实施场景才能定工作流形态 | 等客户信号。法源：《会计基础工作规范》§53 四角色 |
| **合同结算叠加**（ASBE 合同资产/负债第二路径） | 附件2 第22条是可选自设科目，财政部未给编码 | 由客户配置项给编码（默认空）。直接路径 `1481`/`2204` 现已正确 |
| 凭证规则引擎（design §11.3） | 第二条记账路径，已否决 | 不解冻 |
| 跨 journal 重排 | 超出 M3a 边界 | 不解冻 |
| 分数编号法 | 首版声明不支持（§51(二) 法规允许，**但文档不写"法规不允许"**） | 客户信号 |
| 「按同一合同净额」列示（ASBE 已执行版 第22条） | Odoo 无合同维度对应 `account.account`，且落点需按流动性二次分流 | 🔴 **范围风险**，仍不碰 |
| 发票 / 金税 / 全电 | 🔴 **不在范围内**（不是"冻结"） | 见 `项目档 v22 §6.7 第九条` |
| 国标导出 GB/T 24589 | 归档 | 待客群信号 |
| 十四行新增科目的可配置化（设置项） | 零客户，形态未知 | 第一个客户，且其科目表非我方生成 |
| **CAS 30（财会〔2026〕11号）适配** | 表样格式通知未发布 | 财政部发布新表样。目标客群施行日 2030-01-01（可能因集团口径提前）。🆕 R31 在 v20 官方 CoA 增删中**未发现**任何可归因于 CAS 30 的变动 |
| 🆕 **全部 v20 适配工程** | v20 未发版、`20.0` 分支尚未切出、靶子在动 | **20.0 正式发布**（约 2026-09 上旬）。清单见 §10，**现在不得据此改任何代码** |

---

## 5. 开工候选

**排序原则**：① 能碰到**外部真值**的优先（外部真值 = 电子税务局，由和界鸿源账号取得；dev 库只是内部真值）；② **修正已交付物的正确性，优先于新增交付物。**

### 第一梯队

| # | 项 | 说明 |
|---|---|---|
| 1 | **ASBE 未执行版 form** | 🟢 **前置已解除**（R32-T2：executed 版照财会〔2019〕6号附件2 正确建、BS 72 行/票据分列/使用权资产·租赁负债齐全，可安全照抄结构）。材料够开工。<br>🔴 **两条硬约束仍在**：行次号无权威源，**必须留空并显式标待验，不得推演后落 `verified`**；须同轮补自推 `_CN_CROSSFOOT`（**永无原文可换**，标「无原文背书」，见 `项目档 v22 §4.5.12 八`）。 |
| 2 | **结账向导**（D 选①：写 lock date + 原生留痕） | (b) 下结账是法定动作（《会计基础工作规范》§63）。流程层，须过三护栏。小 |
| 3 | **凭证号断号检查**（按 公司 × 期间 × 凭证字 列断号区间） | R29-T1 的配套可发现性工具，**告警非拦截**。一条 SQL |

### 第二梯队（呈现层，材料齐）

| # | 项 | 备注 |
|---|---|---|
| 4 | **ASBE 现金流量表正表** | 35 数据行 + 3 节标题 |
| 5 | **现金流量表间接法补充资料** | 25 数据行 + 3 节标题，两列无行次。按科目取数、不依赖流量项目维度 |
| 6 | **银行余额调节表** | 材料闭合（官方版式 + 法定归档件 + 12号 §23 + §62(三)） |
| 7 | **路线甲 / Outstanding 清算户口径** | CF 唯一的数值覆盖面缺口 |
| 8 | **A2-方案：CF 必填约束对技术行过度触发** | 小，影响开箱体验 |

### 第三梯队（(b) 才需要，工作量最大，全是新建报表、零 L2 覆写）

| # | 项 | 备注 |
|---|---|---|
| 9 | **中式明细账（多栏 / 数量金额式）** | 解法是**新建报表**，不是覆写 |
| 10 | **中式总分类账（月汇总形态）** | 法源 §63(二) + §60(七)。**Odoo 无对应形态，真缺口** |
| 11 | **严格六 / 八栏科目余额表** | 官方 TB 期初期末不拆借贷、列动态生成 → 须新建报表 |
| 12 | **M4 期末结转** | 流程层，须过三护栏。机制已验；残留 = 报表日记账筛选默认全选 |
| 13 | **电子凭证会计数据标准适配** | 财政部有免费基础工具包。过渡期终点 2027-12-31 |

### 候选但不排期

会企04表所有者权益变动表 / 企业所得税季度预缴（A200000）/ 现金流「未分类项目」兜底清单界面 / 跨境数据境内备份方案（**是服务不是模块，可以卖**）/ 平行账边界的重新表态。

> ⚠️ **ASBE 段的处置**：行集合层面追加安全（有权威源），**但取值与版式的外部验证仍无载体**（ASSBE 是唯一载体）。第 1、4 项交付时须标明「未经税局验证」。

---

## 6. 收尾未清

| 项 | 内容 |
|---|---|
| 🔴 **多公司共享账户 + 本公司 code 缺失的前缀吃入 = 静默错报** | 🆕 **R32 坐实（`verified`）。** 详见 `background v9 §7 B-63`。触发面：**仅经导入/建表模板/load（`defer_account_code_checks`）路径可造**，普通建账户造不出（T1-0-d）；一旦触发，该账户发生额被跨公司兜底吃到另一公司的前缀行，`_CN_CROSSFOOT` **静默**（资产=负债权益仍平）。**处置待 Safi 拍**（§8 第 10 项）。**它不是 v20 问题，是 19.0 今天就带着的。** |
| 🆕 **生产库 `base_vat` / USCI 校验位** | R32-T3 附带发现：dev 库未装 `base_vat`，占位 vat 未经校验位校验。**生产库上电子税务局时，税局导入器认不认校验位错的统一社会信用代码，`unknown`。** 装不装 `base_vat`、真实 USCI 是否合规，接客户前须确认。不阻塞当前 |
| ✅ **executed 版 ASBE form 按哪一版附件2 建的** | 🟢 **R32-T2 结案**：照财会〔2019〕6号附件2（已执行版）**正确建**，非已废止的 15号、非未执行版。三判据全绿。**解除 §5 第一梯队 #1 的前置阻塞。** |
| ✅ **`res.company.vat` 未填** | 🟢 **R32-T3 done**（dev 库 company 5 已填占位值并跨事务持久化确认）。真实值待电子税务局侧实测 |
| **T3-5 税局上传外部验证** | design §12.3 唯一外部锚。被拒则带回税局报错原文，**不推演**。含待验：行19 改填正数后是否被导入器接受 |
| **备案报告「核算软件」栏** | **比版式更前置** |
| **元数据四格的「值」格式未知** | 唯一取法 = 手工填官方模板导入成功后原样记录 |
| **`.xls` vs `.xlsx` 接受度未验** | 不提前改栈，随手工导入一并试 |
| **季报「上期金额」口径升 `verified`** | 机制侧 `same_last_year` 已 `verified`；准则依据与税局列头文字仍 `observed` |
| **T2-C 三处「部分满足」** | 核算方法变更留痕 / 结账前完整性预检 / 审计日志报表。**是实施项不是模块项** |
| 🆕 **ASSBE 报表在 v20 的公式改动未核** | R31 只列了 ASBE 两份文件的公式变化；ASSBE 两份各 diff 187/109 行**改了什么未答**。直接关系 `项目档 v22 §4.5.12 九`。一次 grep 可答（已列入 `项目档 §8` 补查清单 V-1） |

**观察，不开工**：官方 `cn_nep` = 利润总额 − 所得税 − 以前年度损益调整。ASBE 下前期差错更正是追溯重述，不进当期利润表 —— 是 `l10n_cn_reports` 上游的候选缺陷，不在本线范围。

---

## 7. 材料缺口

🟢 **法规材料侧基本清零**（索引见 `项目档 v22 §6.6`）。剩余为操作项：

| 缺 | 取法 | 优先级 |
|---|---|---|
| 四个元数据格的「值」字面格式 | 🟢 **R33-A 外部验证已取（`observed`）**：识别号 18 位无分隔符、所属期起/止 `YYYY-MM-DD`。`项目档 v24 §4.5.18` 第10条。🔴 网页渲染非 `.xls` 单元格，与夹具冲突时**夹具赢** | 降为低 |
| `res.company.vat` 的实际内容 | 🟢 **R32-T3 已填占位；真实值待电子税务局侧** | 降为低 |
| ASSBE 三处符号分流的实务口径 | 问二姐 | 中，不阻塞 |
| 凭证字实务上用几个 | 问二姐 | 低，不阻塞（§50 明文允许通用「记」字） |
| 星云（ASBE）是否有凭证引入入口 | —— | 低 |
| 《电子凭证会计数据标准》标准文本 + 基础工具包 | 财政部会计司门户，免费 | 低 |

---

## 8. 开放项 / 待拍

| # | 项 | 谁定 |
|---|---|---|
| 1 | 🟢 **父科目记账守卫 —— 已定 ③ 只告警，R33-A 实现**。19.0 `account.account` 扁平（无 `parent_id`/`is_group`）⇒ 实为 **no-op**，只加断言锚不加代码。🔴 **v20 挂 `parent_id` 后须重估**（M-1） | **已定 ③**（note 侧原即倾向 ③）。🆕 **v20 加强了这个倾向**：官方 commit 明说父科目要能当"中间科目"直接入账、纯分组节点靠 `active=False` 表达 ⇒ 硬约束是跟框架方向对着干 |
| 2 | **推荐编码风格**：点分 vs 连号 | Safi |
| 3 | 结账前完整性预检的判据集合（§63(一) + 12号 §24） | 待 D 落地时定 |
| 4 | **account_codes 在 v20 的语义** | 🟢 **R31 大部收口**：前缀匹配语义与正则**逐字未变**；D/C 后缀**保留**、语义等价；`date_scope` 枚举**逐字未变**；`code_path` **零消费方**（"改按 code_path 匹配更稳"这条想法**当前无源码依据**）。<br>🔴 **子问题③「code 是否公司无关的单值」销号** —— R31/R32 两侧确认：**从来就不是**（`code_store` company_dependent，按 root 公司存），19.0 已如此。转为 §6 首行的 19.0 实测项，**已由 R32 坐实**。<br>🟢 **R33-T0 补**：19.0 侧确认我方 BS/PL 纯 `account_codes`、CF 例外走 `asset_cash`（`background v10 B-64`）。<br>🔴 **剩余**：`sum_if_pos` 删除的迁移（§10 M-2） |
| 5 | **Q-34 字体回退**：阿语 RTL + CJK（paper-muncher） | 待 20.0 发版 |
| 6 | 🟢 **`4001 生产成本` 类型缺陷 —— 销号（R33-A）**。开发 grep 实证：ASSBE BS/PL **零引用** `4001`/`5001` ⇒ `account_type` 对我方报表**零影响**，维持 `expense_direct_cost` **不改**。🔴 **原提法本身错了**——问题不在 type，在报表取数；已由 E-7 的 5 处 formula 外科覆盖修掉（`项目档 v24 §4.5.12 E-7`）。选项 ①②③④ 全废 | **已销** |
| 7 | 🔴 客户是否需要提前执行 CAS 30 | **Safi + 二姐 + 第一个客户**。这不是排期问题，是实务问题 |
| 8 | **`l10n_cn` 官方报表脱钩到什么程度** | **Safi 拍**，note 侧建议分层：① 科目表层倾向脱钩；② 官方报表行层暂不脱钩但建**已知偏离清单**（超过 8 条或任一偏离涉及合计行 = 重估时点）；③ 凭证报表层不动。🔴 **R33-A 后偏离数跳到 9 处**：行19 符号、行14/24 结构破口、v20 存货/成本口径公式改动、**+ E-7 的 5 处 formula 外科覆盖**（行11/行12/PL行2/往年未分配利润/行26）。**已触及「超过 8 条」这条重估线，且其中两处涉及合计口径（存货、权益）** ⇒ 本项从「不急」升为**须在下一轮定** |
| 9 | 🟢 **自己发一套科目表 —— 已交付（R33-A）** | `suite_cn_coa` 2.0.0 发行方形态落地。数据源 = `l10n_cn_assbe_chart_R33A.csv`（101 行：一级 68 / 二级 23 / 三级 10；100 保留 + 1 不预置）。已闭合，不再是开放项 |
| 10 | 🟢 **F3 静默错报的处置 —— 已定 ①，R33-A 实现**。建表路径加「每公司必有 code」校验，V5 多公司测试覆盖。剩余的 defer/import 路径兜底属实施期告知 | **已定** |
| 11 | 🆕 **`suite_cn_coa` 的升级路径策略** —— 发行不写 `ir.model.data`（为避 R22-T2 那种级联删）⇒ 无 `noupdate` 官方升级语义。骨架改版（财会〔2026〕11号 落地、CAS 30 新增项目）时「哪些是我方发的、能不能改」全靠自建台账。**机制已在，策略未定。** 与 §10 M-1「我们的树是自己建的，迁移得自己写」同族 | **Safi 拍**，不急 |

---

## 9. 校验计划三指标（design §12.3）—— **指标 2 首次拿到外部锚**

1. 装机人工步骤数　2. 中国会计首次可识别度　3. 每客户重复劳动是否归零

🔴 **真正的瓶颈不在 §5 任何一条：是找到第一个愿意让本模块在他库上装一次的中国客户。**
在 (b) 定位下这是**唯一前置**，(b) 是三条路里最重的一条，却建立在最少的外部验证上。

🟢 **指标 2 已拿到外部锚（2026-08-11，`verified`）**：我方导出的 `.xlsx` 在电子税务局被接受、**逐格落位全中**（指纹数据行1–8 全部落对行，两列未串）。十条结论见 `项目档 v24 §4.5.18`。

**二姐给出的可操作判据**（指标 2 至此有了定义）：**报表项目名称对得上 + 与系统备案的财务会计制度一致 + 数据不能瞎弄**。前两条已过；第三条约束的是**测试方法**不是产品。

🔴 **和界鸿源账号上禁止造数** —— 数据异常会进「我的提醒」，与**信用失分**同池。后续外部验证一律用真实数据，或网页试算后点「重置」。**按 `项目档 §4.5.2`：走到数据落位为止，不点「提交申报」。**

🟢 **产品叙事的四条实证**（措辞要说准）：**生产成本户类型错致在产品成本静默进损益**、**累计折旧符号与编制说明相反**、**2017 后新增科目各家一套、碰撞会静默取错数**。前两条是 Odoo 的问题，第三条是行业现状 —— 混着说会被懂行的人当场拆掉。
🆕 **第一条在 v20 得到侧面印证**：官方自己在 ASBE 报表侧加了 `5001+5101+5201`。
🆕 **第四条（R33-A 实测，会计一眼能懂的那种）**：**科目表缺明细会造成填列缺陷** —— 某小规模客户未设「预付账款」明细，借方净额无处可去，以 **-37,948** 负数挂在 BS 行33 应付账款并计入行41 合计。二姐认「分流填列更专业」。⇒ 我方预置成对科目 + 删除告警，负数不会挂错行。**这条是行业现状不是 Odoo 的问题，别混着说。**

---

## 10. 🆕 v20 迁移面（**清单，不排期，不开工**）

> **锚**：`odoo/odoo@a0b6c0a376cc` / `odoo/enterprise@d2368c8b6fe`（2026-08-10）。**全部 `observed`。**
> **口径**：20.0 未发版、`20.0` 分支尚未切出、master 距上次调研已漂移 797 / 493 个提交。**每条都要在 20.0 发布后重核，现在不得据此改任何代码。**

| # | 打到什么 | 严重度 | 有无迁移路径 |
|---|---|---|---|
| **M-1** | **`suite_cn_coa` 主交付物整体失效** —— `account.group` 模型已删除、53 个 `account.group-*.csv` 全删、无兼容层 | **致命** | 改挂 `account.account.parent_id`；纯分组节点用 `active=False`。🔴 **官方脚本 `COA_TO_MIGRATE` 不含 `cn`**（因 l10n_cn 从无 group CSV）⇒ **我们的树是自己建的，迁移得自己写**。🟢 **R33-A 已把冲击面减半**：模块定位改为「科目表发行方」、本轮**不碰分级树**，M-1 打到的只剩历史 group 部分；但**发行侧新增一笔债** = §8 第 11 项（无 xmlid ⇒ 无 `noupdate` 升级语义） |
| **M-2** | **ASSBE 往来四行的 8 个表达式全部报错**（`sum_if_pos` / `-sum_if_neg` 已删），且 `0` 值语义反转 | **高** | 官方范式 = 拆两个表达式 + `aggregation` + `if_above(CNY(0))`。**0 值边界逐条人工判定，不可机械替换** |
| **M-3** | **中式竖式 XLSX 取数逻辑失效** —— line/column 改 dataclass、`column_group_key`(str) → `column_group_index`(int) | **高** | 无官方脚本，人工改写全部 dict 式访问 |
| **M-4** | 凡 override 过 `_compute_formula_batch_with_engine_*` 的代码失效（改名 + 掉 3 参） | **高** | 机械改名 + 签名裁剪；用到 `offset`/`limit`/`next_groupby` 的需重设计 |
| **M-5** | **全部 `suite_*` 模块的 `security/ir.model.access.csv` 失效**（`ir.model.access` / `ir.rule` 双双删除，无兼容层） | **高** | 有：`odoo/upgrade_code/19.4-00-ir-access.py`。🔴 **与 l10n_cn 线无关、影响 12 个已发布模块 + 5 个内部模块，应单独立项** |
| **M-6** | `suite_cn_coa` 差异表要重算（官方 CN CoA 层级化 + 增删科目） | 中高 | 无脚本，人工对齐。🆕 **R33-A 后基线已有**：`l10n_cn_assbe_chart_R33A.csv` 101 行 + 19.0 逐码 diff（同码同义 29 / 同码异义 1 / 官方独有 5 / 我方独有 38），v20 重算时按同一四分类跑一遍即可 |
| **M-7** | ASBE/ASSBE 差异表要重算（存货/成本口径公式改动 + `groupby` 上提 + `foldable` 替换） | 中高 | `foldable` 有脚本；公式口径无 |
| **M-8** | 导出向导二进制赋值 `base64.b64encode` → `BinaryBytes` | 中 | 有脚本 |
| **M-9** | `account.report.expression` 字段变化（`formula`/`subformula` Char→Text、engine 新增 `text`/`reference`） | 低 | 向后兼容 |
| **M-10** | 官方 XLSX 按钮 `always_show` 默认翻转 | 低 | 我方显式带 `always_show: True` 即可 |

**本轮已排除、明确不构成风险的**：D/C 方向后缀、前缀匹配语义与正则、`date_scope` 枚举、条件类 subformula 白名单、comparison 多 column_group 机制、`_init_options_buttons` / `dispatch_report_action` / `export_file` / `export_to_xlsx` / `root_report_id` / custom_handler 回落、`account_voucher_cn`、B-57 的 code 邻近继承。

**阻塞**：DB 级迁移脚本在闭源 `odoo/upgrade#9357`，`unknown`。

**重启条件**：20.0 正式发布 / master 版本号跳到 20.1 / 2026-09-24~26 Odoo Experience 之后 / 业务侧出现具体触发（某 `suite_*` 要提前适配、或客户提出升级时间表）。

---

> **附录 A（旧节号 → 新落点对照）已按本档自身规则删除。**
