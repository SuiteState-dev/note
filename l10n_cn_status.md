# l10n_cn 项目状态档

**版本**：s-28（2026-08-18 **口述佐证轮**。🟢 **最后一格关闭** —— 二姐（独立执业会计师）答「**没有借贷混合科目，属于贷方科目余额就是默认贷方**」⇒ **父行方向 = 父科目属性，不因子目有借有贷而变**；**R48 判据 4 销号**、§3 第 8 项销号。🔴 **属性制第一次有非金蝶来源** ⇒ `项目档 §4.5.22 三` 的「单一厂商孤证」警示放宽为「两源一致」（仍不升 `verified`）。⚠️ 她答的是**方向**，「父行是净额」仍是**我方推论**（惯例 33）。**R50 工单据此改版**（撤诚实边界第 3 条与留 Safi 第 2 项），尚未下发。前记 s-27（2026-08-18 **材料轮第二批**。🔴 **会小企01表官方行次与列序入档**（`项目档 v41 §4.5.12 十`，唯一归宿）：**期末余额在前 / 年初余额在后**、货币资金 = **行次 1**、负债侧 **短期借款 = 31**、资产总计 = 30、总计 = 53。⇒ **新增开发侧核对项**：我方 ASSBE BS form 的**列序与行次**须逐条对官方。🟢 **R45「资产总计」悬案结案**（准则原文即此词）。🔴 **金蝶演示账套不得作锚**（表不平 869,459.00、公式被改、缺短期借款行、行次 +1、列序反）。🟢 星辰取数元模型入档（三元组 + 规则枚举、无前缀匹配 ⇒ 我方前缀取数是结构性优势）。🔴 **`2221` 父行那一格仍未答**（本轮是科目表截图，无金额）。前记 s-26（2026-08-18 🔴 **R49 交付件转「须返工」** —— 拿到金蝶**总账导出件** + **8 份打印模板原件**后，R49 的 Q1 方向栏裁决**证伪**：正确规则是**双栏表与单栏表共用一条**（零余额→「平」；非零→方向取科目属性，**符号留在余额里**），不是「方向=余额实际符号」。`项目档 §4.5.22` 整节重写、v39 那节进 §6.7 作废清单。**错因是 note 侧从 §60(七) 推出的那句「因此」被当成了法源本身** ⇒ 立**惯例 33**。🟢 同批闭合：**明细账版式锚两种到手**（三栏式模板+导出件双证、KZ-J105 逐字表头）、**多栏式 KZ-J107 借贷双模板坐实**、**§58 装订签章逐字原文到手**、**不必再向二姐要总账样本**。金蝶材料档升 **v3**（§3.6b 八份模板逐字段解析）。前记 s-25（2026-08-17 **R49 中式总分类账全绿结单**（`suite_cn_statement` **1.23.0**，63 tests 0 failed）—— **中式账簿三件已交付两件，只剩明细账**。🔴 本轮两条比交付本身更重要：① **「总分类账无实务版式锚」是错的** —— 金蝶材料档 §3.6 早有 `KZ-J101` 逐字段绑定、§7.1 早有「方向栏是计算列、恒有借/贷/平」，**材料在库里、两侧都没查**，⇒ 立**惯例 32**（说「没有」与说「有」同为断言）、新增 `项目档 §4.5.22`；② **判据 4「两张自建报表互证」措辞下调为「同源一致性检验」**（两表共用取数、构造必等 ⇒ 惯例 14 第二实例）。**Q1 方向栏 note 侧初判错、开发侧裁对**（单栏表用实际符号、双栏表用属性）。前记 s-24（2026-08-17 §3 第 2 项补**中式账簿两件的开工次序与因由**（总分类账先、明细账后），R49 已下发。前记 s-23（2026-08-17 **R48 三件全绿结单**：余额方向落栏 + 毛额不取绝对值 + 中式版式页脚撤下改独立说明件（`suite_cn_statement` **1.22.0**，49 tests 0 failed）；🔴 **首份余额表实务版式锚到位**（二姐客户真实账套）；三库已升齐。前记 s-22（2026-08-16 **R47 两件全绿结单**：严格六 / 八栏科目余额表落地（`suite_cn_statement` **1.21.0**，37 tests 0 failed）+ 期初口径判定；🔴 工单自带门槛被触发并正确停下 ⇒ 裁决「禁的是官方机件、不是 handler 这个手段」，立**惯例 31**。前记 s-21（2026-08-16 **§10 v20 迁移面按新锚点重写**：VPS 侧 Q-43~Q-50 定向查探回报（`A-88`~`A-96`）到位 —— 地基三条确认未塌（`account_codes` 引擎语义全保留、`l10n_cn_asbe_bs` xmlid 原地未动、`account.report.line` 无新增必填），伤面集中到**科目挂载层**；新增 M-12 ~ M-16，收窄 M-3。前记 s-20（2026-08-16 **R46 空绿排查结单**：全量 77 用例逐条打桩、空绿 **4 条**、修后复验 **0**、全量重跑 0 failed；两条历史结论降级入 `项目档 §6.7`；**§3 整节重写**（旧节累积已交付未销号项）；T3-5 订正为**已做且导入通过**。前记 s-19（2026-08-15 **R45 三件全绿结单**：连号检测判据收窄（`coa` **3.0.1**）、十三项披露删结果预测（`statement` **1.20.0**）、行名核修 + 两处新分歧由财政部原文裁定；`4401` `unknown` 销号；🔴 顺带挖出 `TestCoaPublish` **8 条空绿** ⇒ 立惯例 30。前记 s-18（2026-08-15 **R44 结单**：T2 双载体全绿结案（`suite_cn_statement` 1.19.0）、T1 交接待跑（卡平台侧 build）、T3 撤下。前记 s-17（2026-08-14 R44-T2 交付、T1 卡等平台侧 build。前记 s-16（2026-08-14 R44-T2 口径订正：前缀直取 8 条、口径声明≠告警、载体改报表尾部披露行。前记 s-15（2026-08-14 增量更新：三份新准则修订通知入库，ASBE 未执行版 form **判定不做**，`5401`/`5402` 的 `unknown` 销号。前记 s-14（2026-08-14 整体更新，吸收 **R43 骨架修正轮全绿交付**：编码统一点分 3.0.0、缺陷 #7 消除、三件口径落地、凭证呈现骨架、6 条地方税种默认归档）
**配套**：`项目档 v42` / `design draft-48`（**待随 R50 回写**）/ `background v28` / `l10n_cn_kingdee_material.md v4`
**仓库**：note
**职责**：**当前状态**。已交付 / 冻结中 / 开工候选 / 收尾未清 / 材料缺口 / 开放项 / v20 迁移面。

> **本档规则（务必遵守）**
> 1. **每轮整体重写，不留历史，不加删除线。** 历史由项目档 changelog 与 design §17 逐轮记录承担。
> 2. **本档不放规则、不放口径、不放材料内容。** 那些在项目档。本档只放「现在处在什么位置」。
> 3. **本档不放已作废结论。** 作废清单永远在 `项目档 §6.7`。
> 4. 引用其他文件一律带版本号：`项目档 v28 §x`、`design draft-40 §y`、`background v15 §z`。
> 5. 本档每轮回写时更新，**与项目档解耦** —— 改本档不需要动项目档。

---

## 1. 现在的位置

**R34 脱钩前置探查 → R35 首次真装走查 → R36 缺陷 #2 → R37 backport 测量 + 三件修复 → R38 阶段 B → R39 design 清债 → R41 存量库活体实测 → R42 反序复现 + 两件小修 → R43 骨架修正（编码统一点分 3.0.0），十轮连续收口。**

🟢 **R41 是分水岭**：Safi 能在 staging 上按需重建任意版本的库，**「等第一个客户」这条前提被打破**。四件长期挂账一次结清。

### 🟢 R43 交付 —— 骨架修正轮，全绿（commit `6ed1bd2..b6e57ef`，14 文件；`suite_cn_coa` → **3.0.0**、`suite_cn_voucher` → **1.3.0**；**45 tests / 0 failed**）

🟢 **T0 结案 —— 门控 `verified` 无恙，且成因二次订正**
活体四公司 dry_run（`cn_large_bis` REJECTED / `cn` 通过 / 无 chart REJECTED / `generic_coa` REJECTED）+ wizard `action_run` + 三入口代码走查，全部汇流到 `_publish` 逐公司 `chart_template` 复核，**无法绕过**。
🔴 **原疑证的真因不是开发侧推断的「R35 pre-gate 残留」，而是「staging 推了代码但没执行 upgrade apps ⇒ 跑的仍是旧代码、门控未加载」**（Safi 重跑给出）。⇒ 落 `background v18 §7 B-76` + **惯例 26**（取证须带 `latest_version`）。**惯例 23 第二次兑现**：因推断成因规规矩矩标了 `observed`，一次重跑即翻掉、成本为零。

🟢 **T1 编码统一点分（缺陷 #7 消除，`verified` live 全 rollback）**

| 判据（绝对终态） | 小规模档 | 一般纳税人档 |
|---|---|---|
| `2221` **新建** | **6** | **6** |
| `2221` **认领** | **15** | **25** |
| 整库终态 | 159 → **166** | 159 → **166** |
| 连号残留 / 撞号(正常路径) | 0 / 0 | 0 / 0 |

- 映射硬编码逐条对照官方点分，**判据只用 `code`、不写同义**；6 条我方独有走 **`2221.51`–`.56`**，号段写死 design + 常量
- **撞号哨兵两半都跑**（合成撞号能触发 = 构成证据的那一半，惯例 14）
- 🔴 **T1-d 无自动迁移**：只 `_detect_legacy_lianhao` 告警、**不删不改**（守 manifest「发行的科目留库」承诺）；版本跳 **3.0** 让断裂显式
- **措辞边界已入档**：点分 = 统一到 **Odoo 形态**，**不是国标**

🟢 **T2 三件口径**：`1012` **选 A**（认领仅告警，文案含「改了会变已出报表数字」+ 原始英文类型码；新建才写 `asset_cash`；理由「`observed` 不配自动改客户数据」入 design）／缺陷 **#5 销号**（零代码，注释「待二姐」→「已定 `observed`」）／缺陷 **#8 修**（`VOUCHER_PATH_SEPARATORS` 枚举剥除、无分隔符不剥，三夹具：剥/不剥/防误剥）

🟢 **T3 凭证呈现骨架**：「记-N」上 form + list、可排序/过滤；**批量打印按凭证号正序重排 `docs`**（乱序输入断言渲染 `记-1..5` 递增）；`binding_view_types` → `list,form`；单张打印回归
🟢 **T4 6 条地方税种 `active=False`**，台账照记；🔴 **补齐「归档=隐形」的可发现性**：`_publish` 报告加 `archived` 清单、wizard 回执明写六条编码 + 默认已归档 + 取消归档路径，`test_r43_archived_disclosed_in_report_and_receipt` 守住

**诚实边界（本轮保留）**：T4 启用路径按代码/惯例给出（会计 → 配置 → 科目表 → 过滤器「已归档」→ 操作/取消归档，约 3–4 步），**UI 未逐步点验**，`observed`。

### 🟢 R42 交付（design `draft-41`；`suite_cn_coa` 2.4.0→2.4.1）

**T0 同轮回写 design** → `draft-41`：§9 补 R42/R41 两行 + 就地订正 R35 行成因（带「R41 订正」标记，非静默改）；§17.5 更新 B-67 / T1-b / B-68 三段 + 新增 R35 成因订正块与元教训。

🟢 **T1 场景 5 反序复现（`verified` live，`develop-6` 全新库、`zh_CN` 未激活起点、全 rollback）** —— 挂了四轮的惯例 20 待验项销号：
- **官方 chart 回填、不永久英文**（激活前单键 `en_US` 英文 → 激活后 `zh_CN` 补中文，键分布 500/499）⇒ B-68 官方侧升 `verified`
- **运行时非模板账户不回填**（id46）= 我方账户机制的活体缩影
- 🔴 **我方账户「激活前存在」态不可达** —— R38-T3 门控禁止激活前发行，**设计使然非漏做**；改用可达对照组（激活后双键中文）。免责向结论按惯例 21 留 `observed`、**不进对客户表述**
- **顺带**：R38-T3 门控由打桩 `observed` 升 `verified`，且 build 期 `post_init` 即被挡下（台账 0 行）= 门控在真实安装路径首次生效实证

🟢 **T2 两件小修**：`report_override.py` 转义序列（改 3 行全在 docstring，python 编译器扫全仓修后 0 命中、公式常量字节零改）；wizard 回执去「见日志」改界面直说。回归 `suite_cn_coa` **19 tests 0 failed**。

🔴 **T0 立一条元教训 → 本轮编为惯例 23**：事后推断的成因须显式标 `observed`，`verified` 只能来自按原路径重跑。

### 🔴 本轮（二姐两批反馈 + 两库全量科目表实测）—— 三条口径落地、一条编码决策翻转

**材料**：二姐测试清单增删判断（行为）+ 三条口径追问（口述）/ 小企业库 181 条 + 大企业库 333 条全量导出 / 金蝶星云·星辰·精斗云界面。**全部单人样本，`observed`；导出件形态事实 `verified(artifact)`。**

| # | 结果 |
|---|---|
| **缺陷 #6（`1012`）** | 🟢 **结案**：编现金流量表时**算现金及现金等价物、受限保证金不剔除** ⇒ 整户改 `asset_cash`，**不做明细级区分**。我方 wizard 告警「应为 `asset_cash`」**内容正确** |
| **缺陷 #5（负数大写）** | 🟢 **定案销号**：「红字是手工登记账的标识」「现在没有手工帐啦」「没红字」⇒ **取绝对值、方向由借贷栏承载、不做红字**。与 R36 已交付实现一致，**零代码改动** |
| **建造合同列报** | 🟢 **拿到方向**：老制度工程施工/工程结算入存货；新准则改用合同履约成本/合同结算，借方合同资产、贷方合同负债。⇒ `5401`–`5403` 严重度降级（见 §8 第 15 项） |
| **凭证是否需另存** | 🟢 **不需要**（「凭证不都是按月份打印或者单独打印哪一张吗」）⇒ 我方「不造第二条记账路径」架构获背书。**同时给出功能规格：按月份批量打印 + 单张打印** |
| **编码格式** | 🔴 **§8 第 2 项翻转：连号 → 统一点分**（详见 §8 第 2 项） |
| **归档动机** | 🟢 **已答**：归档的是她认为**用不到**的（非「重复碍眼」）⇒ 归档 wizard 判据方向可定，排期仍待客群信号 |

🔴 **两条新缺陷**：
- 🟢 **#7 已于 R43 消除**（统一点分 ⇒ 认领生效）；🟢 **#8 已于 R43 修复**（`VOUCHER_PATH_SEPARATORS` 枚举剥除，三夹具）。以下为立项时的原始记载，留作根因索引：
- **#7 发行同义重复** —— 我方与官方在 `2221` 下同义重复 **15 条（小规模纳税人档）/ 25 条（一般纳税人档）**，两档「我方独有」均 **6 条**。根因 `background v17 §7 B-73`（认领按 `code` 匹配、明细层形态不一致故结构性失效）。**由统一点分一并解决。**
  🔴 **驱动变量是「纳税人档」，不是「chart / 准则」**（R43-T1-b 逐条映射核清）。note 侧原按库分行写成「ASSBE 15 / ASBE 25」**是错的** —— 样本里两个变量恰好共变（小企业库用小规模档、大企业库用一般纳税人档），因果被挂到 chart 上。归族惯例 12。
- **#8 凭证 path 前缀重复** —— 官方明细名自带父名前缀（「应交税费 - 应交增值税（销项税额）」），我方 `_l10n_cn_account_path` 档 1 取一级再拼明细名 ⇒ 凭证上呈现「应交税费 / 应交税费 - …」。**仅官方点分科目触发**（我方是短名），故 dev 库一直测不出。

🟢 **原「我方科目出现在 `cn_large_bis` 上」的阻断项已结（R43-T0，2026-08-14）**：真因是 **staging 推了新代码但未执行 upgrade apps ⇒ 运行的仍是旧版本、门控未加载**（`background v17 §7 B-76`）。升级后重跑，`cn_large_bis` 两档纳税人身份**均 REJECTED**、290 → 290 一条不加（`verified`，活体 dry_run）。**门控本身无恙。** ⚠️ 开发侧当时的推断成因「R35 pre-gate 历史污染残留」**事后证明是错的**，因按惯例 23 标了 `observed`，一次重跑即翻掉、成本为零 —— **惯例 23 第二次兑现。** ⇒ 那份 333 条导出件是**别的环境的历史件**，T1 不碰它，只在干净 `cn` 库（公司 7，基线 159）上做。

### 🟢 R38 交付（`coa` 2.4.0 / `statement` 1.18.0）

**ASBE 存货口径 backport 阶段 B**：三处成对摘全部落地（存货加 + 营业成本摘 + 权益 `balance_domain` 摘），官方原生 3 条进 `REPORT_OVERRIDES`、我方 2 条走源头 XML。**偏离 5 → 8，未超封顶 9。** 四处同步齐改 + 新增 `19.0.2.4.0/post-migration.py`。验收按绝对终态：存货 `0` / 营业成本 `0` / `30=53` 差 `0` / `nonneg` 无破口 / 对照组 `Dr1403/Cr2202` 存货 `+6000`。

**#4/#7 前置校验**：`zh_CN` 未激活则拒绝发行 + 可读原因；激活态以显式 `lang='zh_CN'` 建记录。

### 🟢 R39 交付：design 五轮债清完 → `draft-40`

补齐 R34–R38 五轮 changelog + 新增 **§17.5 施工轮耐久结论与待验**。**「同轮回写 design」自 R39 起为固定交付项。**

🔴 **note 侧自我订正**：`draft-40` 这个版本号是 v25（R34）时 **note 侧自己填进 `项目档 §8` 索引的**，当时开发侧从未写过——**把「应该有」写成了「已经有」**，此后四轮引用一路指向虚构版本。**比不带版本号更坏**，因为它看起来是对得上的。

🔴 **`draft-36 四处落档缺陷` 已销号**（挂六轮）：四项内容在任何档与历轮对话中**从未枚举**；开发侧两次回报「全文搜不到此列表」是正确回答。⇒ 立 **惯例 22**。

### 🔴 R41 存量库活体轮 —— 四件挂账一次清完（`verified`）

全新 staging 库、装 **2.1.0** 造现场 → 推代码 `-u` 升 **2.4.0**。

| 结清项 | 原状态 | 现在 |
|---|---|---|
| **B-67 受控实测** | 挂 R34 → R39 **六轮** | 🟢 `verified`：八条公式**同步到位、无半应用**；漂移台账 0、覆盖台账 8 |
| **R38-T1-b 真实升级路径** | `unknown` | 🟢 `verified`（**2.1.0 直跳 2.4.0，跨三个 migration**） |
| **R37-T3「要等第一个客户装过、发过第二版」** | 诚实边界 | 🟢 **解除** |
| **R35 污染样本** | 旧库不可达 | 🟢 **重建坐实**：43 新建 + 56 认领、台账**只记 43 条新建** |

🟢 **附带确认**：台账表 `suite_cn_coa_published_account` **无 `action` 列** ⇒ 「2.1.0 认领不留痕、#3 修复前历史安装存在永久盲区」从推断变**结构性事实**，可结案。

### 🔴 R41 推翻两条已入档结论

| # | 原记 | 实测 |
|---|---|---|
| **1** | R35 那 43 条是 **`post_init` 误发到 `cn_large_bis`** | ❌ **错**。`post_init` 走 `_publish_all_companies`，源码 `coa_publisher.py:37-38` 只搜 `chart_template='cn'`，**天然不跨公司**。43 条来自 **wizard 手动填入非 `cn` 公司** |
| **2** | B-68 = 「我方发行账户不落 `zh_CN`」 | ❌ **太笼统**。**`post_init` 路径落单键**（中文写进 `en_US`、`zh_CN` 空）、**wizard 路径落双键**（两键均中文）。差别在 `env.lang` 有无 UI 语境，**源码读不出来** |

**第 1 条的连带**：缺陷 #1 **仍须修（R37-T4 已修）但严重度降一档** —— wizard 文案写死「小企业会计准则 / chart cn」却允许填入 `cn_large_bis` 且不拦不警告 = **文案是门控、代码不是**；但**默认公司字段为空、不带入当前公司**，⇒ **需人工误操作才触发，非默认即错**。

🔴 **对客户的说法据此改**：不得说「装我们模块会污染别的公司」，实情是「**wizard 允许你发到不该发的公司**」。

### 🟢 本轮新立两条惯例（note 侧代拍，已入 `background v16 §8`）

- **惯例 21**：`observed` 的采纳门槛**随结论方向不对称** —— 指向我方缺陷/更保守说法的可先行采纳；指向我方免责/更有利说法的须等 `verified`。
- **惯例 22**：**待办条目必须自带可执行内容**，只有指针没有内容的条目不得进本档。

### 下一个窗口的待办

| # | 待办 | 谁做 | 大小 |
|---|---|---|---|
| **1** | **T4 启用路径 UI 逐步点验**（惯例 1 走查，记点击路径与步数）—— 🔴 **须在 rebuild 之前，用当前已装库做**，否则要重装一遍 | 开发 | 极小 |
| ~~**2**~~ | 🟢 **已销号（R45-T1 用另一条路解决，2026-08-15）** —— **不再需要 Safi 建 build，此条撤销。**<br>🔴 **前提变了**：产品上线前**没有任何客户库在用** ⇒ 「装过 2.x、库里躺着连号科目」的人群**是空集**，原定「验它能响」是在验一件不会发生的事。<br>🔴 **而且方向是反的**：中国实务主流编码就是连号 —— 拿金蝶星辰真实科目表实测，**34 条命中旧判据第一分支**（`2221001 应交增值税`、`2221011 应交所得税` 等一整套规规矩矩的明细）⇒ 唯一可能被这条告警打到的，是**从金蝶迁过来的正常客户**。<br>⇒ **判据 `或`→`且` 收窄**（只对我方确实发行过的库告警），两半 live 均通过：无台账**不触发**、有台账**触发**、告警原文实捕、**不删不改**。`coa` 3.0.1，25 tests 0 failed。<br>🟡 **只剩一格 `observed` 不再追**：升级时刻是否会动旧科目 —— R43 已裁定不写合并 migration、脚本内无 `account.account` 写操作，**读源码即可，不专门开轮**；真要活体，等 `develop-6` 下次反正要重建时顺手做。 | —— | 🟢 **已结** |
| ~~**2-旧**~~ | ~~🔴 **卡平台侧（Safi）**：T1 本轮未跑。~~开发侧在容器内建不出 2.4.1 分支（`odoosh-push` 硬锁只推 `develop-6`）⇒ **须 Safi 在 odoo.sh 从 commit `59234dc`（2.4.1）建一个 throwaway build**，建好通知开发侧，他在那个库的会话里按七步跑完。**不要退回 `develop-6`**（保持 3.0.0 现状，新建更干净）。<br>**地基已核**（`observed`，源码/git，非活体）：`59234dc` 的 `assbe_chart_data.py` 中 `2221` 明细是**纯连号**（`2221001…`），R43 才转点分；`_detect_legacy_lianhao` 判据（无 `.`、位数 > 4、`2221` 纯数字/有台账痕迹）**对连号码必命中** ⇒ **地基成立，唯缺活体一跑**。<br>🔴 **判据 3/5（响 + 不动）未构造，标「交接待跑」，不挂 `observed` 待验** —— 区别于惯例 24 的构造性关闭：**那是永远造不出来，这是造得出来但本轮没条件造**。<br>原任务描述：验 `_detect_legacy_lianhao` 能触发：rebuild → 装 2.4.1 → 发行留下连号 → `-u` 升 3.0.0 → 验告警触发 + 文案可读 + **确实不删不改**。当前只验过「不误报」（干净库 0/0），**那是预期结果非证据**（惯例 14）；这条告警是「无自动迁移」边界**唯一的用户可见面**，不响则边界是空的<br>🔴 **2026-08-15 R44 结单复核：本条是 R44 唯一未结项，等的仍是这一次 build。** 卡的不是开发侧能力，是权限 —— **Safi 不动手，这条永远停在这里。** | 开发（**前置在 Safi**） | 小，**高** |
| **3** | **纳税人身份分档实效重估** —— 两档终态同为 166，分档对库里最终有什么科目**零影响**（官方 `cn` chart 无条件发全套 `2221.01.0x`）。三条路：(a) 删选项 / (b) 小规模档时归档官方那 11 条三级明细 / (c) 保留但只影响呈现。倾向 (b)，**但涉归档官方科目 ⇒ 与 §5 第一梯队 #4 合并评估，不单独排期** | 待评估 | —— |
| ~~**4**~~ | ~~ASBE 未执行版 form 开工~~ | 🔴 **已判定不做**（见 §5 第一梯队 #1）。`standard_version` 保留枚举值、不提供 form | —— |
| ~~**5**~~ | 🟢 **十三项取 0 提示 —— R44-T2 全绿结案**（`verified` live，`suite_cn_statement` **1.19.0**，22 tests 0 failed）。8 条前缀直取项的口径披露**双载体**落地：① 通用路径 `account.report.line`（`suite_cn_statement.cn_asbe_bs_prefix_disclosure`，挂官方 `l10n_cn_reports.l10n_cn_asbe_bs`、seq 9000、无表达式、位列末行、两列 `no_format=None`）；② **中式版式 XLSX 页脚** `footer_note`（`translate=False`、中性格式非红字，与 A48 红字口径告警共存）。**通用 XLSX + 中式版式 XLSX 各真导一次**，定稿串同时命中、两载体文案 `==` 定稿串、被否旧版两者均 0 命中。<br>🟢 **原「残一件小的」（真跑 XLSX）已跑、销号 —— 而且跑出东西了**：中式版式**不含 `account.report.line`**，只加报表行的话，披露在法定主力导出件上会消失。**承重假设两半都跑，这是第二次兑现价值**（首次＝撞号哨兵）。<br>**文案定稿与双载体裁定见 `项目档 v34 §4.5.7.1 (iii)(iv)`**（初版「0 表示未设科目或未用该编码」已否，**不得回退**）。🔴 **通用 PDF 未字面导出**（同走 `_get_lines`，`observed`，不再跑）。 | —— | 🟢 **已结** |
| **6** | 🔴 **叫二姐（三条，🟢 第 ② 条已降级为旁证）**：① 凭证上一级科目单独使用时，接受「2221 应交税费」这种带编码全名吗？② ~~小企业会计准则的客户还用工程施工/工程结算，还是改合同履约成本了？~~ 🟢 **不再是前置** —— 金蝶星辰小准则表实证：`4401`/`4403` 在、新准则那批一条没有（`项目档 v35 §4.5.21`），**结构面已定**；她的回答现在只作实务旁证，答不答都不阻塞③ 🆕 **资产负债表（中式版式）页脚多一条「哪几个项目是按科目编码前缀取数」的说明，报送或给外部看时会不会有人挑？**（同一页脚已有一条红字口径提示在跑，本条是黑字中性说明）—— 低风险确认题，一句话即可。**提问按惯例 25：写字段作用、不出现字段名** | **Safi** | —— |

**不做**：任何 v20 适配。§10 只做清单。

## 2. 已交付

| 项 | 模块 | 状态 |
|---|---|---|
| **自发 ASSBE 科目表（发行方）** | `suite_cn_coa` 2.0.0 | R33-A。68 一级 + 32 明细；`post_init` 发通用 88 + wizard 按纳税人身份补税费档；认领优先/幂等/纯 ORM 不写 `ir.model.data`/发行台账；建表校验「每公司必有 code」；成对科目删除告警（`dc_notice`）。**不碰分级树**。🔴 **R35 暴露三条缺陷**：#1 wizard 无 chart_template 门控（可跨准则发行）/ #3 认领分支不写台账（认领不可审计）/ #4 `create` 依赖 `env.lang`（中文写进 `en_US`） |
| **E-7 报表国标口径覆盖** | `suite_cn_coa` 2.0.0 | 对官方 ASSBE 报表 **8 条**外科覆盖（平表见 `项目档 v28 §4.5.12-E7`），先存原值、`uninstall_hook` 写回。🟢 **R36 已修（缺陷 #2 闭合）**：台账拆两张 —— 官方 **5 条覆盖**（`labels` 收窄为 `('balance',)`）+ 我方 **3 条源头写对**（XML `noupdate="0"`，随 `-u` 重载）。🔴 **`19.0.2.2.0` 带 `post-migration.py`** 显式重放官方 5 条，否则升级后两列脱钩破 `30=53`（实测差 500）。🟢 **逐码 diff 已升 `verified`**；🟢 二姐证言确认 **E-7 是双重正确**（行11 改读 `4001` = 在产品真实载体、`1406` 挪进行12 = 发出商品归位） |
| 🆕 **E-7 基线哨兵** | `suite_cn_coa`，`71d8308` | R34-T1。覆盖前逐条比对官方当前值与代码常量基线；漂移则跳过 + ERROR + 登记 `suite.cn.coa.baseline.drift`。**11 tests 绿**。🟢 **副产品：`项目档 §8` 补查项 V-1 自动化** |
| 🆕 **voucher 一级科目串三档规则** | `suite_cn_voucher` **19.0.1.1.0** | R34。`_l10n_cn_account_path` 顺序判定：① 含 `.` → `split('.',1)[0]`；② 无点 且 `len>4` 且 **`code[:4]` 本公司真实存在** → 取 `code[:4]`；③ 否则 `display_name` 全名**不猜**。🟢 **边界落实**：档 1/2 共用同一次「一级存在性」查询，`code[:4]` 查不到即落档 3 ⇒ **边界是一次 search 天然守住**，我方发行表恒成立、客户怪表自然失效，**不违 R28-T2-7**。`test_account_path_serial_tiers` **12 绿**。🟢 **R35-B4 三档全部实测符合设计**：`2221.01.01` → 截第一个点前；`2221010` → 截前 4 位；`1001`（`len==4` 不满足 `>4`）→ 落档 3 全名兜底，**是两段结构对单级科目不适用的必然结果、非缺陷**。⚠️ 一级科目单用时是否接受带编码全名，**呈现偏好待二姐**。🟢 **缺陷 #5 已交付**：override `_convert_to_amount_in_word`，币种守卫仅 CNY，10 用例全过，voucher **16 tests 0 failed**；🔴 **负数按绝对值转换、形态待二姐**（实务用红字非负号） | 🟢 **R42 后定案（负数形态销号）**：从业者口径「红字是手工登记账的标识／现在没有手工帐啦／没红字」⇒ **取绝对值、方向由借贷栏承载、不做红字**，与既有实现一致、零代码改动。⇒ 连带 `background v17 §7` 的「借方红字存成贷方蓝字」降为**不适用**。🔴 **新增缺陷 #8**：档 1 对官方点分科目取一级后拼明细名，而官方明细名自带父名前缀 ⇒ 凭证上重复（仅官方科目触发）。
| 🆕 🔴 **中式总分类账（月汇总形态）** | `suite_cn_statement` **1.23.0** | R49（63 tests 0 failed，14 新无回归）。新建 `cn_general_ledger`（挂官方 GL 变体）+ **我方自带** `suite.cn.general.ledger.report.handler`。**正面陈述：官方 GL 记录 / 官方 `account_report.py` / 官方任何 handler 三个都没碰。**<br>**形态**：一科目一段、**行 = 月份**（期初行 → 各月「本月合计」→「本年累计」行），列 = 摘要 / 借方发生额 / 贷方发生额 / **方向** / 余额。**只出一级科目**（`detail_leak` 确认为 0）。<br>🔴 **方向 = 余额实际符号**（`>0 借 / <0 贷 / ==0 平`），**余额栏恒取正数** —— 与 R48 余额表的「属性落栏」是**两条规则、两种载体**，边界见 `项目档 v39 §4.5.22`（唯一归宿）。<br>**取数**：复用 R47 roll-up + R48 地基，只多 `date_trunc('month')` 一层；跨年期初经 `to_beginning_of_fiscalyear`。**行 = 月份不吃列式 `date_scope`** ⇒ 用 `_dynamic_lines_generator`。<br>**判据全绿**：逐月连续（断裂 0 + 注入断月能检出）／每月全段借 == 贷（不平 0 + 注入能检出）／本年累计 == Σ 各月（不成立 0）／与八栏余额表本年累计对账（不一致 0 + 注入能检出）／方向含反向情形（`3103` 属性贷、实际借 ⇒ 方向「借」）。<br>**两条渲染路径**：通用 `_get_lines`（**扁平行**，规避框架 `totals_below_sections` 的噪声合计行）+ 中式版式 XLSX。<br>🟢 **受众 = 会计 / 审计（账簿非报送件）** ⇒ 表内口径提示**允许**，与 R48 撤下 BS 页脚不冲突。§58 页码 + 三签章栏位只留栏位。<br>🔴 **两条补课未清，见 §3**：判据未给实数（惯例 17）／「平」态无用例 |
| 🆕 🔴 **余额方向落栏 + 毛额负数 + 页脚撤下** | `suite_cn_statement` **1.22.0** | R48（49 tests 0 failed，12 新无回归）。**余额落哪一栏由科目自身「余额方向」属性定、不随符号迁移；反向时在原栏写负数**（`account.account.cn_balance_direction`，在 `statement` 定义）。值来源三层：**我方 `coa ASSBE_CHART` 方向列 = 唯一 home**；星辰 CSV **仅底稿、不上运行时路径**；`account_type` 仅兜底 + **条件告警**（给条数与编码、不写「见日志」）。<br>**毛额不取绝对值**（红冲负数原样体现，符号截断处确认为 0）。<br>**中式版式页脚已撤**、通用披露行保留、新增工具栏「取数口径说明」导出件 + **文案一致性钉**。<br>🔴 **口径依赖**：红冲负毛额**只有启用 `storno` 的库才保留**（手工负 `debit` 会被 ORM 翻正成 `credit`）⇒ **与金蝶红冲呈现的一致性以客户启用 storno 为前提**，未启用时两边形态本就不同、**非我方缺陷** |
| 🆕 🔴 **严格六 / 八栏中式科目余额表** | `suite_cn_statement` **1.21.0** | R47。新建 `cn_trial_balance`（挂官方 TB 变体）+ **我方自带** `suite.cn.trial.balance.report.handler`；取数 SQL 与官方 `_report_custom_engine_trial_balance` **逐字同款范式**，仅在我方 handler 内加派生键 `bal_debit`/`bal_credit`。**正面陈述：官方 TB 记录 / 官方 `account_report.py` / 官方任何 handler 三个都没碰。**<br>**列**：期初借 / 贷（`to_beginning_of_period`）· 本期发生额借 / 贷（`strict_range`，**毛额**）· 本年累计借 / 贷（`from_fiscalyear`，**毛额**）· 期末借 / 贷。**六 / 八栏 = 一张报表两列集**，开关控「本年累计」显隐。<br>**判据全绿**：三 / 四组合计借 == 贷；逐行勾稽 5 行 / 0 不成立；一级 roll-up 2 行 / 0 不成立 / **未归入任何一级的叶子 0 行**。<br>**一级 roll-up = 单动态行 + 我方后处理器**（键：有点取首段 / 无点取前 4 位）⇒ 点分与连号混合编码实测归一。**父行名无锚时查我方标准名再回落前缀**（命中金蝶迁入的连号客户）；**单叶子一级组折叠成一行**；**中式版式整链 `lang='zh_CN'`，法定件不出英文科目名**。<br>🔴 **两条渲染路径各验一次**（惯例 29）：通用 `_get_lines` + 中式版式 XLSX（表头带单位 / 期间 / 金额单位，页脚连续编号，末尾三签章栏位 —— §58/§60，**只留栏位不做流程**） |
| 🆕 **测试基建：空绿修复 + 正向哨兵** | `coa` **3.0.2** / `statement` **1.20.1** / `voucher` **1.3.1** | R46。全量 77 用例逐条打桩（把被测方法体换成 `return`/`[]`/`None`，还绿即空绿），**查出 4 条、全部修完、复验 0**。修法照惯例 30：断言块前加**正向哨兵**（`created 台账 > 0` / `我方 created 2221.51 在库` / `取号 = 1`）；`test_no_dangling_expressions` 改两半（注入 `ZZ_NO_SUCH_CODE.balance` 证有牙 + 真数据证无悬空）；`test_v1_v2_accounts_survive_restore` 的**被测对象由官方 `1122` 改为我方 `created 2221.51`**。🔴 **零业务逻辑改动**；两条被它们背书过的历史结论已降级，见 `项目档 v36 §6.7 第二十四 / 二十五条` |
| 🆕 **连号检测判据收窄（`或`→`且`）** | `suite_cn_coa` **3.0.1** | R45-T1。`_detect_legacy_lianhao` 只对**我方确实发行过**的库告警（原第一分支 `2221`+纯数字**不要求台账痕迹**，会把从金蝶迁来的正常连号客户当成旧版残留）。🔴 **两半 live 均通过**：无台账**不触发** / 有台账**触发** / 告警原文实捕 / **不删不改**（惯例 14 的两半都跑）。形式论证：真·残留必由 2.x `create` 建、`create` 必写台账 ⇒ `A∧B = A`，**收窄不丢真阳、只丢「客户自有连号」这类假阳**。夹具 = 金蝶星辰真实科目表 |
| 🆕 **十三项取 0 口径披露（双载体）** | `suite_cn_statement` **1.20.0** | R44-T2。8 条前缀直取项（`1101`/`148`/`1504`/`1505`/`1513`/`2204`/`2245`/`251`）的**静态口径声明** —— 恒真、**不走双通道、不配日志**（声明不是告警）。**双载体**：通用路径 `account.report.line`（挂官方 `l10n_cn_reports.l10n_cn_asbe_bs`）+ 中式版式 XLSX 页脚 `footer_note`（`translate=False`、中性格式），**文案一字一致**。🔴 **不替 0 下结论**（取 0 有第三种合法可能：科目建了、编码对，期末余额恰为 0）。🔴 **未碰官方 generic 引擎**：我方记录落共享表、FK 挂官方报表，官方 `-u` 按 `ir_model_data.module` 键**不删不改**（`background v21 §7 B-77`）。**组合式 5 条 / 残差式 1 条不加**（0 可多项相抵 / 几乎不可能为 0，加了是噪声） |
| 🆕 **`nonneg` 存货断言** | `suite_cn_statement` | R37-T2。ASBE BS + ASSBE BS 各加「行9 ≥ 0」，双通道曝光、告警非拦截。**自推·无原文**已标注。🟢 **不占偏离额度**（改我方 form）。⚠️ **T1 阶段 B 后该场景不再响是正常的** —— 它兜的是**未来其他成因**造成的负存货，尤其 `5401`–`5403` 这类已知未修项，**不得因 T1 修好而撤掉** |
| 🆕 **overrides 生效自检**（形态 b+a） | `suite_cn_coa` | R37-T3。`_check_overrides_effective` 只读任意库可调 + server action；`_apply_all` 末尾自查 → ERROR + 登记漂移台账。🔴 **诚实边界**：本轮无存量库 ⇒ fresh 只验**不误报 + 逻辑正确**，真正抓「`-u` 无 migration 漏放」要等第一个客户装过、发过第二版 |
| 🆕 **跨准则发行门控 + 认领留痕 + 污染扫描** | `suite_cn_coa` | R37-T4（缺陷 #1+#3）。门控以目标公司 `chart_template` 为判据、非 `cn` 拒绝 + 可读原因；认领写台账（前 name/type 快照 + 同名标）；`_scan_foreign_publications` 只读**只报不修**。🔴 **两处诚实盲区**：旧版认领无痕 ⇒ 历史误认领扫不到；非 `cn` 公司编码与官方 ASBE 大量重合 ⇒ 不产噪声启发式清单 |
| 五张报送 form（ASBE BS/PL、ASSBE BS/PL、现金流量表），192 表达式 | `suite_cn_statement` / `suite_cn_cashflow` | ✅ 中式版式导出可用 |
| 双列取数（期末/年初、本年累计/本月），`date_scope` 静态双列 | 同上 | ✅ |
| 上期列走原生 comparison，filter = `same_last_year` | 同上 | ✅ 税局「上期」= 上年同期 |
| 报送 form 三维（`period_scope` × `standard_version`） | 同上 | ✅ R26 建好；R30 第二次兑现 |
| `_CN_CROSSFOOT` 表内勾稽守卫 + 双通道曝光（WARNING + 导出件红字） | 同上 | ✅ R24 起；R30 换原文断言 |
| 不等式断言类型 `op='gte'` + 第三类破口「内含项超出」 | 同上 | ✅ R30-P2 |
| ASSBE 往来四行方向分流（B-58 装配式） | 同上 | ✅ R30-T2a　🔴 **机制在 v20 已被官方删除，见 §10 M-2** |
| ASBE 应付/预付 明细级 D/C 方向分流 | 同上 | ✅ R30-T2b　🟢 D/C 在 v20 保留 |
| 行19 累计折旧符号修正（`_CN_NEGATE`） | 同上 | ✅ R30-P3 |
| M3a 中式记账凭证呈现层 | `suite_cn_voucher` | ✅ R29-T1，commit `3e740ef`　🟢 宿主 `account_voucher_cn` 在 v20 仍在 |

**勾稽守卫当前条数**：ASBE BS 9 / PL 6（**自推，永无原文可换**，见 `项目档 §4.5.12 八`）；ASSBE BS 11 / PL 9（原文）；CF 5。

**报表挂点（R34-T3，`verified`）**：BS → 27/28（root=4）、PL → 23/25（root=7）、CF → 29（root=5，**我方独立报表**）。**4 张官方 variant + 1 张独立。**

🔴 **全部已交付物带两条风险，都不区分模块、不区分 form**：
① 多公司共享账户 + 本公司 code 缺失的前缀吃入（§6 首行，R32 坐实为静默错报）；
② 🆕 E-7 覆盖在 `-u` 升级路径上**不重放**（§6，`observed`）。

---

## 3. 待下发 / 进行中

> 🔴 **s-20 整节重写。** 旧版累积了 13 行，其中大半是**已交付却没销号**的（第 5 项「十四行取到 0 时出口径提示」R44 已交付、第 11 项「R35 note 回写」是十轮前的事），违反本档「每轮整体重写、不留历史」的规则。**本节自此只放真正开着的。**

| # | 项 | 归属 | 状态 |
|---|---|---|---|
| 1 | 🔴 **v20 影响面评估（不是适配，是评估）** | 开发 / VPS | **建议下一轮就做**。20.0 约 2026-09 上旬发布，只剩不到一个月。核心问题：`account.account` 加 `parent_id`、`account.group` 删除，对 `account_codes` 引擎（**全部报表 form 的地基**）的影响面。🔴 **这是只读源码活，不需要库**；`20.0` 分支未切出 ⇒ 目标是 `master`，**取证必须带 commit SHA + 日期**（源码取证的坐标是 SHA，同惯例 26 精神）。须同时看 community `account` 与 enterprise `account_reports`（`account_codes` 引擎在后者） |
| ~~**0**~~ | 🟢 **已销号（R48-T0）** —— 三库已升齐：`coa` **19.0.3.0.2** / `voucher` **19.0.1.3.1** / `statement` **19.0.1.22.0**，R46 哨兵补丁现已在库里生效 | 开发 | R47 登记：被测库 `statement` 已升 1.21.0，但 **`coa` / `voucher` 的库仍停在 3.0.1 / 1.3.0，工作树已是 3.0.2 / 1.3.1** ⇒ **R46 那批哨兵补丁在库里没生效**。本轮只碰 statement 故不影响判断，但**不留 B-76 同形状**（推了代码没 upgrade，那次耗掉一整个阻断项） |
| 2 | **中式账簿：~~三件~~ ~~剩两件~~ 剩一件** —— **明细账**（多栏 / 数量金额式）<br>🟢 **科目余额表 R47 已销号**；🟢 **总分类账 R49 已销号** | 开发 | 🟢 **不受脱钩工程影响**（解法都是新建报表、不覆写官方）⇒ 现在做不会白做。**这两件是「客户能不能关掉金蝶」的判据本身**，`#11` 档记「Odoo 无对应形态，**真缺口**」。<br>🟢 **R47 已把取数骨架跑通**（期初 / 发生额毛额 / 期末 / 余额方向 / 一级 roll-up），另两件是在它上面换呈现形态。⚠️ **R47 未预留任何接口** —— 开工时按实际需要定，不要去找"当初留的口子"。<br>🔴 **R50 起只剩明细账。开工前先做一件事（惯例 32 的直接后果）：把 `l10n_cn_kingdee_material.md` §3.6 关于明细账的三份模板绑定读一遍再下单** —— 三栏式明细账（`month`/`day`/**`vchnum` 凭证字号**/`explanation`/`debit`/`credit`/`dc`/`endbal`）、多栏明细账 `KZ-J107`（借方版与贷方版**是两个独立模板**、分析栏 66 个 `DataColumn` 运行时生成）、数量金额明细账 `KZ-J105`（借/贷/余三组各三栏）。⇒ **「多栏式 vs 数量金额式的版式争议未决」这句话本身要重估** —— 材料里是**两种并存的独立模板**，不是二选一。<br>🔴 **明细账另有一条形态事实（§7.2）**：它是**装订册**（封面 / 目录 / 正文三模板并存，目录含每科目起始页码），不是单张报表。下单时须先裁「我方做不做册」。<br>原记（s-24）开工次序三条因由：① **总分类账的取数与余额表同源** —— 行=月份、列=借/贷/方向/余额，**直接复用 R47 的 roll-up 与 R48 的 `cn_balance_direction`**，新增的只有「按月分组」这一层；② **明细账多一层未定的形态争议** —— 多栏式（按对方科目或明细项目分列）与数量金额式（加数量 / 单价列）是两种不同版式，**我方尚无实务原件对位**，先做会把版式争议和取数一起压上来；③ **总分类账能验死** —— 每月发生额合计 == 该月全部凭证借贷合计、逐月余额连续（上月期末 == 本月期初），都是机械判据；明细账的验收里相当部分是版式感受。 |
| 3 | **官方点分明细归档 wizard**（85 条零分录） | 开发 | 判据 = 零分录 + 点分明细（官方体系）+ 其一级已被我方认领，**不写「同义」**。🔴 须与「纳税人身份分档实效重估」**合并评估**（两档终态同为 166、分档零影响，倾向「小规模档归档官方那 11 条三级明细」） |
| 4 | **凭证号断号检查**（按 公司 × 期间 × 凭证字 列断号区间） | 开发 | **一条 SQL**，告警非拦截。R29-T1 的配套可发现性工具 |
| 5 | **报表面三件**：ASBE 现金流量表正表（35 数据行）/ 间接法补充资料（25 数据行）/ 银行余额调节表 | 开发 | 材料全部闭合，纯落地活 |
| 6 | **结账向导**（D 选①：写 lock date + 原生留痕） | 开发 | (b) 下结账是法定动作（《会计基础工作规范》§63）。流程层，**须过三护栏** |
| 7 | 🆕 **`suite_cn_cashflow_statement` 补测试** | 开发 | R46 发现：**该模块零测试用例** —— 一个模块没有测试是 **100% 空绿**，连打桩都不必做 |
| ~~8~~ | 🟢 **s-28 销号 —— 二姐已答，全部四条 + 这一格均已关闭。** 问：`2221` 展开级次后父行显示什么？答：**「没有借贷混合科目，属于贷方科目余额就是默认贷方」**。⇒ **父行方向 = 父科目属性（`2221` = 贷），不因子目有借有贷而变**，与 `项目档 §4.5.22` 属性制一致，**我方无需为父行另设规则**。<br>🔴 **附带收获**：这是属性制的**第一条非金蝶来源**，`§4.5.22 三` 的「单一厂商孤证」警示据此放宽。<br>⚠️ **她答的是方向，未答金额形态** —— 「父行落单侧 ⇒ 显示净额」是**我方推论**（惯例 33），已挂级，不追问。<br>原记（s-27）：🔴 **那一格仍未答 —— 2026-08-18 收到的是【科目表】截图，不是余额表 / 总账。** 该截图证实了 `2221` 确为**借贷混合父科目**（`2221 03/04/05/07` 属性借、其余 19 条属性贷），这是**前提坐实**，但**它没有金额列** ⇒ 回答不了「父行在子目有借有贷时**显示什么数**」。<br>**要的仍是同一格**：请二姐在**科目余额表或总账**里**展开 `2221` 的级次**，看**父行**那一行的借 / 贷 / 方向 / 余额各显示什么。<br>原记（s-26）：🟢 **总账样本不必要了，只剩一格。** 金蝶总账导出件 + 打印模板已把列集、方向规则、行标签字面全部覆盖（`项目档 v40 §4.5.22`）。<br>🔴 **只剩这一格**：请二姐**展开 `2221` 的级次**，看**父行在子目有借有贷时显示什么**（是父方向净额单侧，还是借贷两栏都出数）。这是 R48 判据 4 唯一无原件对位的一格 —— 已知 `2221` 确为借贷混合父科目（`2221 03/04/05/07` 属性借、其余贷），但总账导出件里它恰好全期为零、方向「平」，**没暴露该情形**。<br>原记（s-25）：🔴 **s-25 重写：向二姐要的东西已收窄，别再要整张表。** 🟢 **总分类账的「列集」不必再问** —— 金蝶 `KZ-J101` 模板绑定已在库（`项目档 v39 §4.5.22`）。**现在只剩两格**：① 请她**展开 `2221` 的级次**，看**父行在子目有借有贷时显示什么**（R48 判据 4 唯一无原件对位格）；② 一份总分类账样本，**只看两件事**：**行标签怎么写**（「本月合计」/「本年累计」/期初行的字面）与**期初行怎么排**（单独一行还是并进首月）。<br>原记（s-24）：🟢 **~~叫二姐（两条 + 一件实物）~~ 四条全部已答、销号**（2026-08-17）：① 凭证上一级科目带编码全名 **接受**；② 页脚 **对内可、对外撤**（R48-T3 已撤）；④ 科目余额表 **八栏**（与我方默认列集一致）；实物 = 客户真实账套截图**已到位**，成为我方首份余额表版式锚。<br>🔴 **下次要样本时指名要这一格**：请她**展开 `2221` 的级次**，看**父行在子目有借有贷时显示什么** —— 这是 R48 判据 4 唯一无原件对位的一格，比再要一整张表精准。 | **Safi** | 🟢 **四条已闭**；🟡 **只剩下次要样本时那一格**（`2221` 展开级次）。**提问按惯例 25：写字段 / 概念的作用，不出现技术字段名** |
| 9 | **T4 归档路径真人浏览器点一遍** | **Safi** | 🟡 五分钟。机制已 `verified`，缺的只是真人手感（惯例 1 的走查那一半） |
| 10 | 🆕 🔴 **R50 = R49 返工（一轮做完，不再拆）** | 开发 | 🔴 **(A) 方向 / 余额规则改回属性制** —— `余额==0 → 方向「平」、余额栏空`；`≠0 → 方向 = 科目余额方向属性`（复用 R48 已有的 `cn_balance_direction` 与 `_cn_resolve_direction` 三层解析），**余额栏带符号、可为负**。⇒ R49 的 `_cn_gl_direction` 纯符号实现作废。规则与证据见 `项目档 v40 §4.5.22`（唯一归宿）。<br>🔴 **(B) 每科目每期出两行** —— 「本期合计」+「本年累计」，原件即如此；R49 是每月一行 + 段末累计一次。<br>🔴 **(C) 「期间」列替「月份」列**，值形如 `202607`；**(D) 科目编码 / 科目名称各一列**（跨行合并），非段头承载。<br>🔴 **(E) 「平」补用例** —— 原件里 9 个科目有 4 个是平，**高频态不是边角**；须造零余额、断言方向 ==「平」且余额栏空。<br>🔴 **(F) 判据补实数**（惯例 17）—— R49 四条判据全写成「参与 `>0`」，哨兵满足、计数没给；R47 是给了的，属表述退化。要「参与月数 / 断裂月数」「参与科目数 / 不一致数」两数分列。<br>**载体锚 = 导出件**（非打印模板），理由见 `项目档 §4.5.22 五`。 |
| ~~10b~~ | 🟢 **已并入第 10 项（原 R49 补课两条）** | 开发 | **(i) 判据 1/2/3/4 补实数**（惯例 17）—— R49 回报把四条判据全写成「参与 `>0` / 不成立 0」，**哨兵满足了、计数没给**；同一批人在 R47 是给了实数的（参与 5 行 / 不成立 0 行），属**表述退化**。要「参与月数 / 断裂月数」「参与科目数 / 不一致数」两个数分列。<br>**(ii) 「平」态补用例** —— 方向栏取值域是**借/贷/平**三态（§60(七) 明文 + 金蝶 `balancedc` 恒有三值），R49 判据 5 只抽了反向一例，**「平」无用例钉着**；且在「无发生额月不出行」的裁决下，零余额行可能在真实数据上**根本不出现** ⇒ 须造零余额月、断言方向 == 「平」。 |
| 12 | 🆕 🔴 **我方 ASSBE BS form 对官方 17号逐条核（可与 R50 同轮，独立于总账返工）** | 开发 | 锚 = `项目档 v41 §4.5.12 十`（**不是金蝶导出件**）。三件事：**(i) 列序** —— 官方是「**期末余额 → 年初余额**」，核我方 form 的两列顺序；**(ii) 行次** —— 货币资金 = **1**（不是 2），资产侧 1–30、负债权益侧 **31–53**，**负债侧首项是「短期借款」= 31**；节标题行**不占行次**；**(iii) 固定资产三行** —— 原价 18 / 减：累计折旧 19（**正数列示**）/ 固定资产账面价值 20，**非流动资产合计（29）只吃行 20**。<br>⚠️ **不得拿金蝶那份 xlsx 当参照**（演示账套、被改过、行次 +1、列序反），判读规则见 `l10n_cn_kingdee_material.md v4 §0.1`。 |
| 11 | 🆕 🟡 **判据 4 的证据级已下调，登记一条候选（不排期）** | note 侧 / 开发 | 总分类账与余额表**共用同一套取数**（构造必等）⇒ 现有交叉对账是**同源一致性检验**、不是互证。**真互证**需另写一条**独立取数路径**（直接从 `account.move.line` 聚合的对照断言）。**不急**：现在两表已一致、且各自有自证；等第三张账表（明细账）落地时**一并做一条三表对照**，边际成本最低。 |

## 4. 冻结中

| 项 | 冻结原因 | 解冻条件 |
|---|---|---|
| **M3b 凭证签章流程层** | 需真实实施场景才能定工作流形态 | 等客户信号。法源：《会计基础工作规范》§53 四角色 |
| **合同结算叠加**（ASBE 合同资产/负债第二路径） | 附件2 第22条是可选自设科目，财政部未给编码 | 由客户配置项给编码（默认空）。直接路径 `1481`/`2204` 现已正确 |
| 凭证规则引擎（design §11.3） | 第二条记账路径，已否决 | 不解冻 |
| 跨 journal 重排 | 超出 M3a 边界 | 不解冻 |
| 分数编号法 | 首版声明不支持（§51(二) 法规允许，**但文档不写"法规不允许"**） | 客户信号 |
| 「按同一合同净额」列示（ASBE 已执行版 第22条） | Odoo 无合同维度对应 `account.account`，且落点需按流动性二次分流 | 🔴 **范围风险**，仍不碰 |
| 发票 / 金税 / 全电 | 🔴 **不在范围内**（不是"冻结"） | 见 `项目档 §6.7 第九条` |
| 国标导出 GB/T 24589 | 归档 | 待客群信号 |
| 十四行新增科目的可配置化（设置项） | 零客户，形态未知 | 第一个客户，且其科目表非我方生成 |
| **CAS 30（财会〔2026〕11号）适配** | 表样格式通知未发布 | 财政部发布新表样。目标客群施行日 2030-01-01（可能因集团口径提前）。R31 在 v20 官方 CoA 增删中**未发现**任何可归因于 CAS 30 的变动 |
| **全部 v20 适配工程** | v20 未发版、`20.0` 分支尚未切出、靶子在动 | **20.0 正式发布**（约 2026-09 上旬）。清单见 §10，**现在不得据此改任何代码** |
| 🆕 **官方报表脱钩工程** | 🔴 **排期已定 = 20.0 同轮**（与 M-2/M-3/M-4 合并重写，边际成本最低；且 v20 报表结构届时才确定） | 20.0 发布。**同轮必须一并处置 id4/28 可达性**，否则脱钩 = 增加错误答案的数量。度量与做法见 §8 第 8 项 |

---

## 5. 开工候选

**排序原则**：① 能碰到**外部真值**的优先（外部真值 = 电子税务局，由和界鸿源账号取得；dev 库只是内部真值）；② **修正已交付物的正确性，优先于新增交付物。**

### 第一梯队

| # | 项 | 说明 |
|---|---|---|
| 1 | ~~**ASBE 未执行版 form**~~ | 🔴 **2026-08-14 判定：不做**（论据＝**适用面**，见 `项目档 v32 §4.5.21`）。财会〔2019〕6号**附件1** 服务的是未执行新金融/新收入/新租赁准则的企业，而三项对**非上市 ASBE 企业均自 2021-01-01 施行**（三份修订通知原文，`observed`）⇒ 至 2026 年适用面≈0。**处置**：`standard_version` 保留「未执行版」枚举值（R26 三维已建、零迁移），**不提供 form**；解冻条件＝**遇到真实的未执行客户**。⇒ 行集平表**不再需要制备**。<br>⚠️ **两处论据订正**：**(i)** 曾以「CAS 30 修订」为由建议本项不做，**方向错了** —— `§4.5.20` 明写「未执行版 form 零影响、照排」，那句讲的是 CAS 30 对它没影响、不是它有客户；**(ii)** 「材料已到位」不成立 —— 产物在仓库 404、`index.md` 无此条，判定该文件**从未作为文件存在过**（分析在早前对话完成、结论被写成「已制备」），⇒ `background v19` 立**惯例 28**。原记（v29）： `l10n_cn_ASBE_unexecuted_rowset.md` 在 note 仓库返回 **404**，`index.md` 亦无此条。⚠️ **但制备工作确实做过**：下述统计数（BS 左栏 26 / 右栏 31、PL 37、BS 说明 15 条、PL 说明 8 条）经 v31 逐个回核附件1 原件，**全部准确** ⇒ 这是 **v29 的落档漏项：结论入档、产物未入库**，不是没做。**开工前须先确认该平表是否仍在 Safi 手上**：在则提交入库并补索引，不在则据 PDF 重做（有原件、有已核统计数，重做远快于首次）。<br>🔴 **另记一条判据订正**：v31 曾以「CAS 30 修订」为由建议本项不做，**方向错了** —— `项目档 §4.5.20` 明写「ASBE 未执行版 form 零影响、照排」。若要重议本项存废，正确论据是**适用面**（附件1 服务的是未执行新金融/新收入/新租赁准则的企业，三项对非上市 ASBE 企业的施行日须以三份**修订通知**原文为准，通知已由 Safi 取得、待入库），**与 CAS 30 无关**。<br>原记（v29）：note 侧据财会〔2019〕6号**附件1 原件**制备了行集合平表（BS 左栏 26 + 右栏 31 数据行、PL 约 37 行，**逐行标注有无原文说明背书**）。⚠️ **原「照抄 executed 版结构即可」是过度乐观**（R32-T2 只证明了已执行版照的是附件2，未证明未执行版可照抄），开发侧 R38 据此退回本项**是正确的拒绝**。<br>🔴 **三条硬约束**：**①** 行次号**原文根本没有「行次」列**（note 侧实读原件 `verified`）⇒ 留空标待验是**唯一正确处置且不必再找材料**，本条终局；**②** 有原文背书的是少数（BS 说明 **15 条**、PL 说明**仅 8 条**，对照附件2 是 17 条）⇒ 其余行取数是**科目余额直填的推演，须逐行标 `推演`**；**③** 其他综合收益两类别原文各以「……」结尾 = **开放式枚举**，我方取几条是**我方的选择**，须显式写「本 form 取 N 条、超出不支持」，**不得写成「照原文建全」**。同轮补自推 `_CN_CROSSFOOT`（附件1「勾稽」二字出现 0 次），标「无原文背书」。 |
| 2 | **结账向导**（D 选①：写 lock date + 原生留痕） | (b) 下结账是法定动作（《会计基础工作规范》§63）。流程层，须过三护栏。小 |
| 3 | **凭证号断号检查**（按 公司 × 期间 × 凭证字 列断号区间） | R29-T1 的配套可发现性工具，**告警非拦截**。一条 SQL |
| 4 | 🟢 **官方点分明细归档 wizard —— R35 结论：值得做** | 候选 **85 条全部零分录**（R34-T5）。<br>🔴 **判据 = 零分录 + 点分明细（官方体系）+ 其一级已被我方认领**，**不写「同义」** —— T5 的「按名同义 = 0」是 dev demo **英文名**造成的假象；真实客户库装 zh_CN 后是「**同一级下两套中文明细**」，**比现在更难分辨** ⇒ **归档价值被 demo 环境低估，不是高估**。<br>**形态**：零分录默认勾选、**有分录不进可勾选集合（只列出告知）**、`active=False` 可逆、动作写发行台账。<br>**分组参考**：`2221`×27 / `5403`×12 / `1601`·`1602`·`1606` 各×6 / `5711`×5 / `5301`·`5603`×4…<br>🟢 **R35 实测补三条**：① 小企业库 85 条 / 大企业库 108 条（7 位 83 + 10 位 25），**全部零分录**；② 官方 `cn` 存在**三级点分**（`2221`→`2221.01`→`2221.01.01`），**非两级**；③ 🔴 **不只是观感** —— 同父级下 `2221.11` 因字符串排序排在 `2221001` 之前、两套体系视觉交错且语义重叠，**且三级连号 10 位在点分体系中无对应深度 ⇒ 按编码前缀做的任何汇总（自制报表、税局辅助资料导出）会分叉**。<br>**Safi 观感（`observed`）**：我方知道成因、用起来无碍；**客户不知道会疑惑**，功能不受影响但统一格式比较友好 |

### 第二梯队（呈现层，材料齐）

| # | 项 | 备注 |
|---|---|---|
| 5 | **ASBE 现金流量表正表** | 35 数据行 + 3 节标题 |
| 6 | **现金流量表间接法补充资料** | 25 数据行 + 3 节标题，两列无行次。按科目取数、不依赖流量项目维度 |
| 7 | **银行余额调节表** | 材料闭合（官方版式 + 法定归档件 + 12号 §23 + §62(三)） |
| 8 | **路线甲 / Outstanding 清算户口径** | CF 唯一的数值覆盖面缺口 |
| 9 | **A2-方案：CF 必填约束对技术行过度触发** | 小，影响开箱体验 |

### 第三梯队（(b) 才需要，工作量最大，全是新建报表、零 L2 覆写）

| # | 项 | 备注 |
|---|---|---|
| 10 | **中式明细账（三栏式 / 多栏式 / 数量金额式）** | 🟢 **s-26：版式锚已闭合两种，材料不再是瓶颈**（`l10n_cn_kingdee_material.md v3 §3.6b`）。**三栏式** = 模板 8 列与导出件逐列一致（含 `vchnum` 凭证字号，形如 `记-3`，与我方 voucher 形态相合）⇒ **可直接开工**。**数量金额式 KZ-J105** = 逐字表头 +两行表头合并结构原值在手（⚠️ 原件「贷方（付出））」多一右括号，**我方不抄**、加 PIN）。**多栏式 KZ-J107** = 借贷**两个独立模板**已坐实，分析栏 66 列全无绑定、运行时按对方科目展开、**单侧展开**；5 个并存网格的分档依据 `unknown`。<br>🔴 **裁语：多栏式与数量金额式不是「二选一的版式争议」，是两条不同产品线** —— 原「争议未决」的表述作废，可分轮做、互不阻塞。**开工次序建议：三栏式 → 数量金额式 → 多栏式**（后者最重且依赖对方科目展开）。<br>原记（s-25）：🔴 下单前必读金蝶材料档 §3.6 / §7.2 —— 三栏式 / `KZ-J107` 多栏 / `KZ-J105` 数量金额是**三份并存的独立模板**（多栏账还分借方版与贷方版两个模板），且明细账是**装订册**（封面 / 目录 / 正文）。见 §3 第 2 项 |
| ~~11~~ | ~~**中式总分类账（月汇总形态）**~~ | 🟢 **R49 已交付销号**（`suite_cn_statement` 1.23.0）。法源 §63(二) + §60(七)；形态与证据见 `项目档 v39 §4.5.22` |
| 12 | **严格六 / 八栏科目余额表** | 官方 TB 期初期末不拆借贷、列动态生成 → 须新建报表 |
| 13 | **M4 期末结转** | 流程层，须过三护栏。机制已验；残留 = 报表日记账筛选默认全选 |
| 14 | **电子凭证会计数据标准适配** | 财政部有免费基础工具包。过渡期终点 2027-12-31 |

### 候选但不排期

会企04表所有者权益变动表 / 企业所得税季度预缴（A200000）/ 现金流「未分类项目」兜底清单界面 / 跨境数据境内备份方案（**是服务不是模块，可以卖**）/ 中式内部账表版式（等客户信号）/ 平行账边界的重新表态。

> ⚠️ **ASBE 段的处置**：行集合层面追加安全（有权威源），**但取值与版式的外部验证仍无载体**（ASSBE 是唯一载体）。第 1、5 项交付时须标明「未经税局验证」。

---

## 6. 收尾未清

| 项 | 内容 |
|---|---|
| 🔴 **多公司共享账户 + 本公司 code 缺失的前缀吃入 = 静默错报** | R32 坐实（`verified`），详见 `background v15 §7 B-63`。触发面：**仅经导入/建表模板/load（`defer_account_code_checks`）路径可造**，普通建账户造不出；一旦触发，该账户发生额被跨公司兜底吃到另一公司的前缀行，`_CN_CROSSFOOT` **静默**。**处置已定 ①，R33-A 实现**，剩余 defer/import 路径兜底属实施期告知。**它不是 v20 问题，是 19.0 今天就带着的。** |
| 🟢 **E-7 覆盖 `post_init` 不随 `-u` 重放 —— 🟢🟢 R41 全链闭环，本条可结** | R34 附带（`observed`，`background v15 §7 B-67`）。旧版已装 + 仅升级的客户库**拿不到覆盖且无提示**。⇒ 排查覆盖类缺陷时**先答「这个库上覆盖到底跑没跑过」**。🟢 **R36 升 `verified`**（活体首装 → `-u` 观测）：我方 3 条走 XML **随 `-u` 重放**、官方 5 条走 `post_init` **不重放**；`19.0.2.2.0` 已带 migration 补齐。🔴 **但引入了一个靠记性守不住的模式** ——「每个触及官方 5 条的版本各带一次 migration」，某版忘带就**再次不重放且静默**。⇒ 自检已由 R37-T3 交付。🟢🟢 **R41 真实存量库实测闭环（`verified`）**：全新库装 **2.1.0** → 推代码 `-u` 升 **2.4.0**（跨三个 migration），**八条公式同步到位、无半应用**、漂移台账 0、覆盖台账 8、发行台账未被升级动过。⇒ **「每个触及官方条目的版本各带一次 migration」这个模式已被一次真实跨版升级验证过**；R37-T3 那条「要等第一个客户装过、发过第二版」的诚实边界**解除**。**本条自此只剩纪律要求，不再是未验风险。** |
| 🔴 **翻译字段语言键（B-68，R37 整条改写）** | 🟢 **机制已 `verified`**（`background v15 §7`）：未激活语言 context **硬报错**、不静默回落；**真正暴露面 = 以默认 context 建记录只落 `en_US` 单键**，若该串是中文则界面回落显示正常、**肉眼查不出** ⇒ **验收必须查 jsonb，看界面等于没查**。修法口径已定（§8 第 14 项）。🆕 **R41 把暴露面拆细了（`verified`）**：**`post_init` 路径落单键**（中文写进 `en_US`、`zh_CN` 空）、**wizard 路径落双键**（两键均中文）——差别在 `env.lang` 有无 UI 语境，**源码读不出来、只有活体能得**。⇒ 验收不能只抽查「我方发的科目」，须**按发行路径分别抽查**。🔴 **但顺序性症状仍 `unknown`**（R41 只验到**正序**：先装中文 → 后装科目表 → 官方账户双键正常；按惯例 20 **不能用正序推反序**） —— 本库 `zh_CN` 停不掉、无法原样复现；且存在「官方走模块 i18n / 我方走运行时用户数据」的**可能分野**，若成立则**官方并不中招、我方独中**，与原判断方向相反。复现法与影响见 §1 |
| 🆕 **统计口径本身会制造漏项（记一条防再犯）** | `l10n_cn_reports` 在 `ir_model_data` 里只有 **15 条**表达式，却定义了 ASSBE BS 62 行 + PL 38 行 —— 官方表达式多为 XML **内联定义、无独立 xmlid**。🔴 **不可用 `ir_model_data` 统计官方表达式总量**。与 B-65 / 惯例 17 同族 |
| 🆕 **变体可达性的残余风险（存档，按令不修）** | `availability_condition` 挡的是 UI，**程序化强制渲染仍可绕过**。若强制渲染 report 28 于 ASBE 库，行11 在产品公式 `4001` 会读到**实收资本**（本库 `4001` 余额 0 故示值 0）⇒ **结构性风险非当前故障**。见 `background v15 §7 B-70` |
| **生产库 `base_vat` / USCI 校验位** | R32-T3 附带发现：dev 库未装 `base_vat`，占位 vat 未经校验位校验。**税局导入器认不认校验位错的统一社会信用代码，`unknown`。** 装不装 `base_vat`、真实 USCI 是否合规，接客户前须确认。不阻塞当前 |
| ~~**T3-5 税局上传外部验证**~~ | 🟢 **销号 —— 订正：R33-A 已做且导入通过**（Safi，2026-08-16）。本条被本节长期记为未做，属**落档遗漏**：结论其实早已写在 `项目档 §4.5.21`（`.xlsx` **被接受**、**逐格落位全中**、两列未串，均 `verified`），当次唯一的问题是**日期字面格式**（所属期起止须 `YYYY-MM-DD`），已一并记在同节第 10 条。<br>⚠️ **残留一条，不专门开轮**：「行19 改填正数后是否被导入器接受」当次**未单独构造** ⇒ 仍 `unknown`，下次真实报送时顺带看一眼。<br>🔴 **教训同惯例 13 自用面**：结论落了项目档，本档却仍挂「未做」—— **单一 home 的另一半是「其他档要指过去，不是各记各的」**。 |
| 🆕 🟡 **余额表：判据 4（父子方向不一致）无原件对位** | R48。设计为「父行 = Σ子行 signed 余额，再按**父行自身**方向属性落栏、反向写负数」，实测 `D221` 组自洽且跨栏勾稽平，**与实务规则同构**。但截图里 `2221` 本期与期末恰好为空、**未暴露该情形** ⇒ **待验**。下次要样本时指名要这一格（见 §3 第 8 项） |
| ~~**余额表：无版式锚**~~ | 🟢 **R48 销号** —— 二姐客户真实账套截图（金蝶，2026年07期，八栏）已到位，成为**首份余额表版式锚**。**逐条对上**：列结构（编码 / 名称 / 期初借贷 / 本期发生额借贷 / 本年累计发生额借贷 / 期末借贷）、合计行在最下且标签为「合计」、四组各自借贷相等、**无发生额无余额的科目不出行**、**空侧留空不是 0**、零余额时**两侧全空**（`1122` 期初借 23,250 / 本期贷 23,250 / 期末两侧空）。<br>🆕 **另两条可抄项**：期间选择器按「**期数**」而非日期（`2026年07期 ~ 2026年07期`）；旁置「**展开所有级次**」勾选框。<br>⚠️ 样本量 = **一家软件 / 一个客户 / 一位会计**，`observed` 不升 `verified` |
| 🆕 **余额表：大企业一级名回落仍可能光秃** | R47。父行名无锚时查的是我方 `ASSBE_CHART`，**大企业侧独有的一级科目查不到 ⇒ 回落裸前缀**。🟢 **低危不动**：大企业走官方点分表，`2221` 这类一级锚基本恒在，回落几乎跑不到 |
| 🆕 **余额表：期初是否含未过账未专测** | R47-T2 第 4 问。受报表 `all_entries` 选项控制、默认仅已过账（`observed`，未逐一实测该开关） |
| 🆕 🔴 **`suite_cn_cashflow_statement` 零测试用例** | R46 发现。**一个模块没有测试 = 100% 空绿**，连打桩都不必做。已入 §3 第 7 项待下发 |
| **备案报告「核算软件」栏** | **比版式更前置** |
| **元数据四格的「值」格式** | 🟢 R33-A 外部验证已取（`observed`）：识别号 18 位无分隔符、所属期起/止 `YYYY-MM-DD`。🔴 网页渲染非 `.xls` 单元格，与夹具冲突时**夹具赢** |
| **`.xls` vs `.xlsx` 接受度** | 🟢 R33-A 销号（`.xlsx` 被接受） |
| **季报「上期金额」口径升 `verified`** | 机制侧 `same_last_year` 已 `verified`；准则依据与税局列头文字仍 `observed` |
| **T2-C 三处「部分满足」** | 核算方法变更留痕 / 结账前完整性预检 / 审计日志报表。**是实施项不是模块项** |
| ✅ **ASSBE 报表在 v20 的公式改动未核** | 🟢 **R34-T1 哨兵自动化**：官方任何一条公式变动，测试直接报出 line/label。手工 grep（`项目档 §8` V-1）降为可选 |

**观察，不开工**：官方 `cn_nep` = 利润总额 − 所得税 − 以前年度损益调整。ASBE 下前期差错更正是追溯重述，不进当期利润表 —— 是 `l10n_cn_reports` 上游的候选缺陷，不在本线范围。

---

## 7. 材料缺口

🟢 **法规材料侧基本清零**（索引见 `项目档 §6.6`）。

🆕 **v29 补一条投送规矩**：`legal documents/` 目录在 note 仓库，**开发分支搜不到**（§6.6 v20 已记过一次，R38 又发生一次）。⇒ **凡施工单要用原件，随单附 raw 链接**（`index.md` 已补登记该目录）。**这不是材料缺口，是材料投送缺口，两者的处置完全不同。**

剩余为操作项：

| 缺 | 取法 | 优先级 |
|---|---|---|
| 🆕 🔴 **`4001` 下是否需预置直接材料 / 直接人工 / 间接费用；`1403` 下是否需原材料 / 辅助材料 / 周转材料** | 二姐已给工业路径（`观察`）；须核我方 `l10n_cn_assbe_chart_R33A.csv` 现状 | **高** —— 若无，工业客户装完第一件事就是自己建，**正是指标 3 的原始素材** | 🆕 **2026-08-14 补量化锚**：二姐在小企业库 181 条中归档 **49 条（27%）**，全为通用件、无一行业垂直科目 ⇒ 「通用科目表既不垂直又有多余」由观感变为**可引用的数字**。二姐并给出机制口径：「账套在建账是选择行业、准则，带出来的连锁配套:科目体系、结转逻辑」——**Odoo 按国家切、中国实务按行业切，是切分维度不同，不是实现质量问题**。🔴 **此条指向行业包形态，零客户下严禁开工**，只入 §5「候选但不排期」与销售叙事。
| 🆕 🔴 **制造费用（`4101`/`5101`）实务用不用** | 二姐把**间接费用直接挂 `4001` 下** ⇒ 若普遍如此，`项目档 §4.5.12-E7` 的 **B-60-lite 留字适用面须重估** | 中 |
| ~~`1012 其他货币资金` 的现金口径（缺陷 #6）~~ | 🟢 **已结（2026-08-14）**：二姐口径 —— 编现金流量表时**算现金及现金等价物、外埠存款与银行汇票保证金不剔除** ⇒ **整户 `asset_cash`、不做明细级区分**。原记「保证金类可能受限、可能需明细级区分」的疑虑**撤销**；我方 wizard 告警「应为 `asset_cash`」**内容正确、保留**。🔴 **残留张力如实记**：CAS 31 文义为库存现金 + **可随时支付**的存款，与「受限保证金不剔除」有张力；本口径系实务惯例、**单人样本 `observed`**，遇大额受限保证金客户须复核，**不作为对外权威表述**。🔴 **提问方式教训**：首次按字段名问得到方向相反的答案，见 `background v17 §8 惯例 25` | 🟢 **已结** |
| 🆕 **一级科目单用时凭证上是否接受带编码全名**（B4 档 3） | 问二姐 | 低 |
| ASSBE 三处符号分流的实务口径 | 问二姐 | 中，不阻塞 |
| 凭证字实务上用几个 | 问二姐 | 低，不阻塞（§50 明文允许通用「记」字） |
| `res.company.vat` 的真实值 | 待电子税务局侧 | 低 |
| 星云（ASBE）是否有凭证引入入口 | —— | 低 |
| 《电子凭证会计数据标准》标准文本 + 基础工具包 | 财政部会计司门户，免费 | 低 |

---

## 8. 开放项 / 待拍

| # | 项 | 谁定 |
|---|---|---|
| 1 | 🟢 **父科目记账守卫 —— 已定 ③ 只告警，R33-A 实现**。19.0 `account.account` 扁平 ⇒ 实为 **no-op**，只加断言锚不加代码。🔴 **v20 挂 `parent_id` 后须重估**（M-1） | **已定 ③** |
| 2 | 🟢 **推荐编码风格 —— 已定「统一点分」并已实施（R43，`suite_cn_coa` 3.0.0）**（原「已定:连号」作废） | **已实施**。缺陷 #7 消除:两档新建均 6、认领 15/25、整库 159→166、连号残留 0。🔴 **无自动迁移**（只 `_detect_legacy_lianhao` 告警、不删不改，守「发行的科目留库」承诺），版本跳 3.0 让断裂显式。<br>**依据（按强度）**：① 🔴 **成本决定性** —— 小企业库官方点分 **85** / 我方连号 **21**；大企业库官方点分 **108** / 我方 **31**。统一到点分只需改我方 21/31 条；统一到连号需新建 85/108 条并归档等量官方件，**约 4:1**，且后者把我方发行件从「68 一级 + 明细补缺」被迫扩成「接管全表明细层」。② 🟢 **一举两得** —— 改点分后 `code` 判据自然命中，缺陷 #7 的 15/25 条同义重复走认领分支自动消失（`background v17 §7 B-73`）；**格式统一与重复消除是同一个动作**，只修重复则修完仍不统一。③ **可读性反直觉但成立** —— 金蝶星辰/精斗云本体是连号，却**必须在界面上加空格分段**（`2202 003`、`2221 001 001`）才能读 ⇒ 裸连号需要呈现层补救；点分 `2221.01.01` 天然可读。**二姐的连号手感来自那些靠分段显示救回来的软件。** ④ **v20 迁移次要利好** —— M-1 要改挂 `account.account.parent_id`，点分可直接 split 出父子、与官方一致。<br>🔴 **代价与损失如实记**：**(i) 号段撞车** —— 官方已用到 `2221.16`，我方独有 6 条若接 `.17`+ 则官方下版新增即撞（B-59 同族）⇒ **约定我方走 `.51`+ 并配撞号哨兵**（形状同 E-7 原值哨兵）。**(ii) `code` 改写 21/31 条** —— 我方发行件**纯 ORM、不写 `ir.model.data`** ⇒ 无 xmlid 迁移问题，一个 migration 即可；**零客户的现在是最便宜也是最后的窗口**。**(iii) voucher 档 2 不会变死代码** —— 它从「服务我方发行件」变为「服务客户自建的连号科目」，三档规则本就为兼容各种客户表而设，我方退出反而更干净。**(iv) 必测**：192 表达式有无写死位数处，须实测不得推演。**(v) 🔴 措辞边界** —— 中国实务编码传统确实是连号（财政部只给 4 位一级；金蝶星辰/精斗云/用友主流连号）⇒ **统一到点分 = 统一到 Odoo 的形态，不是统一到中国标准的形态，对客户不得说「我们按国标」**。<br>🟢 **原提「国标导出编码格式要求」这个拍板前置无效**：GB/T 24589 非本线对象、早已归档（`项目档 §4.5.13 / §6.7 第十条`、`background §9`、本档 §4）；现行锚点是电子凭证会计数据标准，元素清单已在 `项目档 §4.5.14`，自查成本低、不阻塞。**教训**：提「未核前置」之前先在自己的主题索引里搜一遍（惯例 13 的自用面）。 |
| 3 | 结账前完整性预检的判据集合（§63(一) + 12号 §24） | 待 D 落地时定 |
| 4 | 🟢 **`account_codes` 在 v20 的语义 —— 主体已销**（R31/R32/R33-T0 三轮收口）：前缀匹配语义与正则逐字未变、D/C 后缀保留、`date_scope` 枚举逐字未变、`code_path` 零消费方；「code 是否公司无关单值」已销号（**从来就不是**）。🔴 **剩余唯一项**：`sum_if_pos` 删除的迁移（§10 M-2） | **主体已销** |
| 5 | **Q-34 字体回退**：阿语 RTL + CJK（paper-muncher） | 待 20.0 发版 |
| 6 | 🟢 **`4001 生产成本` 类型缺陷 —— 已销（R33-A）**。问题不在 type，在报表取数，已由 E-7 覆盖修掉 | **已销** |
| 7 | 🔴 客户是否需要提前执行 CAS 30 | **Safi + 二姐 + 第一个客户**。实务问题非排期问题。**只影响 ASBE 已执行版 PL 在候选清单里排第几，不影响任何一行现有代码** ⇒ 已并入客户装机反馈清单，**不单列待办** |
| 8 | **`l10n_cn` 官方报表脱钩到什么程度** | 🟡 **方向已表态 + 排期已定，具体做法待 20.0**。<br>**Safi 表态**：倾向不改写官方逻辑 —— 我方报表错了随时改无代价，改写官方则卸载与出错时波及原生。<br>**note 侧分层**：① 科目表层**实质已脱钩**（E-7 二选一）；② 官方报表行层**排期至 20.0 同轮**（与 M-2/M-3/M-4 合并，边际成本最低）；③ 凭证报表层不动。<br>🔴 **R34 补入两条必读**：**(i)** 官方按型通用版 **id4 仍可达**（variant 下拉可切）⇒「一个库里两张表两个答案」**今天就存在**；脱钩会变成**三个**（id4 错、28 退回错、只有新报表对）⇒ **同轮必须一并处置 id4/28 可达性**（倾向：对装我方科目表的公司隐藏或关闭，做法待 20.0 结构确定）。**(ii)** **191 是资产度量不是工作量度量**，真实工作量 = 自建 204 line + 重写 204 主列 + 重挂 191。<br>🔴 **在此之前的纪律**：偏离数**封顶 9 处**，再加任何一处先讨论。🟢 **R36 后官方覆盖由 8 降至 5**（我方 3 条转为自有 XML，不再算偏离）；**ASBE backport 后的新总数待测量重算** |
| 9 | 🟢 **自己发一套科目表 —— 已交付（R33-A）** | **已销** |
| 10 | 🟢 **F3 静默错报的处置 —— 已定 ①，R33-A 实现** | **已定** |
| 11 | **`suite_cn_coa` 的升级路径策略** —— 发行不写 `ir.model.data`（为避 R22-T2 那种级联删）⇒ 无 `noupdate` 官方升级语义。🔴 **R34 加重**：`post_init` 不随 `-u` 重放（B-67）⇒ 不只是「骨架改版时判不了哪些是我方发的」，是**升级路径上覆盖根本不施加**。**两件同族，宜同轮定** | **Safi 拍**，已从「不急」升为**下一轮定** |
| 13 | 🟢 **缺陷 #2 修复方案 —— 已定并已交付（R36）** | **已销**。原文留此备查：开发侧与 note 侧建议一致 = **源头 XML 生成时即正确、`REPORT_OVERRIDES` 缩回官方 5 条**。理由比「时序不巧」更强一层：**我方自己的静态 XML 绕道用另一模块的 hook 覆盖自己，本身就是设计错误**。「伞加 hook」「statement depends coa」两方案**违 §4.5 依赖铁律已排除**。🔴 动手时**四处同步改**：`REPORT_OVERRIDES`、T1 基线常量、`test_e7_baseline_drift` 断言条数、`uninstall_hook` 写回范围 + `项目档 §4.5.12-E7` 台账表拆两张。**漏一处测试即红**。🟢 五处全落地、验收五项全过；🔴 **实施时另撞出施工单漏掉的 migration 债**，见 §1 |
| 15 | 🟢 **ASBE 侧存货口径 backport —— 阶段 A 已确认，进阶段 B（2026-08-13）** | **已定**。测量结论：**两个吸收口**（`5\(53)` + 裸前缀 `-5`）、**病根 = 存货行不含任何 5xxx**、**必须三处成对摘**（存货 + 营业成本 + 权益）、归属 = 官方原生 **3** + 我方源头 XML **2**、**偏离数 5+3=8 未超封顶 9**。🔴 **`5401`–`5403` 本轮不做、登记已知偏离**（完工百分比法下 `5401`/`5402` 是净额列报，当存货加总可能是错的会计；v20 官方也只补三个；`nonneg` 断言已兜住）。解冻 = 建筑业客户 + 二姐列报判断。🔴 **验收按绝对终态：存货由 −6000 回到 `0`**，不写「+6000」。详见 `项目档 v28 §4.5.12` | <br>🔴 **2026-08-14 严重度降级**：二姐口径 —— 老制度工程施工/工程结算**入存货**；**新收入准则改用合同履约成本、合同结算，借方合同资产、贷方合同负债**（教育侧佐证:「今年学习考试都是新的科目啦」）。实测印证 `cn_large_bis` 有 `1481 合同资产`/`2204 合同负债`、**无「合同履约成本」「合同结算」**，正面印证「可选自设科目、财政部未给编码」。⇒ `5401`–`5403` 由「**存货漏计的已知偏离**」降为「**历史科目，现行准则客户不适用**」（同族见 `background v17 §7 B-74`）；解冻条件由「等建筑业客户 + 二姐列报判断」改为「**先核该客户执行哪一版收入准则**」。🟢 **2026-08-14 `unknown` 销号，升 `observed`（原文级）**：财会〔2017〕22号 修订通知原文 ——「执行本准则的企业，不再执行……财会〔2006〕3号 中的《企业会计准则第14号——收入》和**《企业会计准则第15号——建造合同》**」，且**非上市 ASBE 企业自 2021-01-01 施行**。⇒ `5401 工程施工`/`5402 工程结算` **不是「推测无人使用」，而是准则本身已废止**；与二姐口径（改用合同履约成本/合同结算，借方合同资产、贷方合同负债）原文级吻合。详见 `项目档 v32 §4.5.21`。🔴 **两条红线不动**：① **`nonneg` 断言不得撤**（它兜的是未来其他成因造成的负存货，与 `5401`–`5403` 存废无关）；② **ASSBE 侧未核** —— 小企业会计准则未跟随新收入准则，`4401`/工程结算可能仍在用，**ASBE 与 ASSBE 是两条线，不得合并判断**。
| 14 | 🟢 **#4/#7 语言键修法口径 —— 已定（2026-08-13）：前置校验** | **已定**。🔴 原倾向「统一只写 `zh_CN`」**被实测否掉** —— 未激活语言的 context **硬报错**（`Invalid language code`），写双键也得先激活。**新口径**：建记录前确保 `zh_CN` 已激活且在 context；**未激活则拒绝发行 + 可读原因**（同 T4-a 门控形状，**不静默继续**）。**残余接受**：双键同中文、英文界面显示中文科目名（ASSBE 科目无权威英文译名，硬造是伪精确）；**关键是显式取 `zh_CN` 处不再空**。配套：装机顺序「先装语言、再建公司/装科目表」写进实施手册。⚠️ **顺序性症状本身仍 `unknown`**，见 §1 |
| 16 | 🟢🟢 **R35 库污染 —— 已结案（R41）** | **已结**。🟢 **重建实测坐实**（`verified`）：wizard 手动填入 `cn_large_bis` → **43 条新建 + 56 条认领**，台账**只记 43 条新建**；台账表**无 `action` 列** ⇒ 「旧版认领无痕」是**结构性事实**不是推断。🔴 **成因订正**：不是 `post_init` 误发（它只搜 `chart_template='cn'`，天然不跨公司），是 **wizard 路径**。旧库在不在已不重要。原文留此备查：零客户 + staging 库，写回收工具不划算。**14 条认领盲区**（旧版不写台账、无痕）登记为「#3 修复前历史安装」的**永久限制**，无真实暴露面。🔴 **但须在真实 R35 库上跑一次 `scan` 验证机制命中 43 条** —— 干净库只能用合成污染行验 |
| 12 | 🆕 **官方点分明细归档的形态**：wizard / notice / 纯实施说明 | **Safi 拍**。候选 85 条全零分录，量级已清；判据与形态倾向见 §5 第一梯队 #4 |

---

## 9. 校验计划三指标（design §12.3）—— **指标 2 已有外部锚**

1. 装机人工步骤数　2. 中国会计首次可识别度　3. 每客户重复劳动是否归零

🔴 **真正的瓶颈不在 §5 任何一条：是找到第一个愿意让本模块在他库上装一次的中国客户。**
在 (b) 定位下这是**唯一前置**，(b) 是三条路里最重的一条，却建立在最少的外部验证上。

🟢 **指标 2 已拿到外部锚（2026-08-11，`verified`）**：我方导出的 `.xlsx` 在电子税务局被接受、**逐格落位全中**（指纹数据行1–8 全部落对行，两列未串）。十条结论见 `项目档 v28 §4.5.21`。

**二姐给出的可操作判据**：**报表项目名称对得上 + 与系统备案的财务会计制度一致 + 数据不能瞎弄**。前两条已过；第三条约束的是**测试方法**不是产品。

🔴 **和界鸿源账号上禁止造数** —— 数据异常会进「我的提醒」，与**信用失分**同池。后续外部验证一律用真实数据，或网页试算后点「重置」。**按 `项目档 §4.5.2`：走到数据落位为止，不点「提交申报」。**

🟢 **产品叙事的四条实证（措辞要说准，混着说会被懂行的人当场拆掉）**

| # | 实证 | 责任归属 |
|---|---|---|
| 1 | **生产成本户类型错致在产品成本静默进损益** | **Odoo 的问题** |
| 2 | **累计折旧符号与编制说明相反** | **Odoo 的问题** |
| 3 | **2017 后新增科目各家一套、碰撞会静默取错数** | **行业现状**（财政部未重发完整科目表，金蝶也在猜、只是猜得不一样） |
| 4 | **科目表缺明细会造成填列缺陷**（R33-A 实测：某小规模客户未设「预付账款」明细，借方净额 **−37,948** 挂 BS 行33 并计入行41 合计；二姐认「分流填列更专业」） | **行业现状** |

🔴 **第 1 条对外定型措辞（R34-T4 升 `verified`）**：

> 官方那张资产负债表**只在客户完全按 Odoo 的西式 WIP 模型做账时才对**（在产品记 `1406`）。中国会计的默认动作是记 `4001` —— 那一刻官方表静默错报（未分配 −6000，`53=30` 照样自平），我方表正确。

**不说「Odoo 是错的」**（近 24 月官方对华 8+14 次提交，不是无维护，一查 commit 就被拆）。要说「**Odoo 对的前提在中国不成立**」。矩阵见 `项目档 v28 §4.5.12-E7`。

🆕 **第 1 条在 v20 得到侧面印证**：官方自己在 ASBE 报表侧加了 `5001+5101+5201`。

🔴 **第 1 条又拿到一条独立从业者证言（2026-08-13，`observed`）**：二姐被问 `1406` 时答**「不是发出商品吗」「一般这个科目都不用」**。

**这条把风险方向反转了，销售叙事要跟着改**：既然没有中国会计往 `1406` 记在产品，官方报表行11 **不是「可能取不到」，是几乎必然取不到** —— 工业企业 WIP 一律走 `4001`。⇒ **对客户可以把这条从「有个坑」升级成「你现在的表就是错的，只是它不报错」**，而且这是**工业客户 100% 命中**、不需要任何前提假设的一条。

**同时对内的含义**：E-7 覆盖不是可选优化，是**工业客户的必需品**；B-67 与缺陷 #2 的严重度同步上升（#2 已于 R36 修复）。

🆕 🔴 **ASBE 侧是另一种症状，销售叙事要分开讲（R36-T4，`verified`）**：ASSBE 是**存货漏计**；ASBE 是**存货变负数 + 成本高估**，而且**资产与权益精确抵消、`30=53` 不破、守卫抓不到**。

⇒ 对 ASBE 客户可以说得更重：**「你的资产负债表上存货是负数，而这张表照样配平」**。同时叙事要留住干净的那一半 —— **v20 官方自己已经加了 `5001+5101+5201`，我们做的是把上游自己的修法提前，不是发明口径。**

---

## 10. v20 迁移面（**清单，不排期，不开工**）

> **锚（s-21 更新）**：`odoo/odoo@af5442cef5c4` / `odoo/enterprise@059764e93470`（**2026-08-15**）。前锚 `a0b6c0a376cc` / `d2368c8b6fe`（2026-08-10），五天漂移 299 / 212 个提交，其中触及 `l10n_cn` 2 个、`l10n_cn_reports` **0** 个。**全部 `observed`，无 `verified`。**
> **口径**：20.0 未发版、`20.0` 分支尚未切出。**每条都要在 20.0 发布后重核，现在不得据此改任何代码。**
> 🔴 **本节只登记「我方哪些交付件会被砸到」，不登记 Odoo 事实本身** —— 事实在 VPS 侧分册（`odoo20_change_directions.md` 及 `A-` / `G-` / `V-` / `P-` / `AI-` 各册），本节只存指针。**避免两处各记各的，同惯例 13。**

| # | 打到什么 | 严重度 | 有无迁移路径 |
|---|---|---|---|
| **M-1** | **`account.group` 模型已删除**、53 个 `account.group-*.csv` 全删、无兼容层 | **致命** | 改挂 `account.account.parent_id`；纯分组节点用 `active=False`。🔴 官方脚本 `COA_TO_MIGRATE` **不含 `cn`** ⇒ **我们的树是自己建的，迁移得自己写**。🟢 R33-A 已把冲击面减半（定位改「科目表发行方」、不碰分级树）；但发行侧新增一笔债 = §8 第 11 项 |
| **M-2** | **ASSBE 往来四行的 8 个表达式全部报错**（`sum_if_pos` / `-sum_if_neg` 已删），且 `0` 值语义反转 | **高** | 官方范式 = 拆两个表达式 + `aggregation` + `if_above(CNY(0))`。**0 值边界逐条人工判定，不可机械替换** |
| **M-3** | ~~中式竖式 XLSX **取数逻辑**失效~~ 🟢 **s-21 收窄**：**取数路径未变**（`_get_lines` 仍是导出取数入口，A-93-a；数据对象层只改「行被渲染成什么」dict→dataclass，未改「行从哪来」，A-91-c）。真实失效面 = **消费侧的属性访问写法**：`column_group_key`(str) → `column_group_index`(int)、dict 下标 → dataclass 属性 | ~~高~~ **中高** | 无官方脚本，人工改写全部 dict 式访问。**范围比原判小一圈：改的是读法，不是取数** |
| **M-4** | 凡 override 过 `_compute_formula_batch_with_engine_*` 的代码失效（改名 + 掉 3 参） | **高** | 机械改名 + 签名裁剪；用到 `offset`/`limit`/`next_groupby` 的需重设计 |
| **M-5** | **全部 `suite_*` 模块的 `security/ir.model.access.csv` 失效**（`ir.model.access` / `ir.rule` 双双删除，无兼容层） | **高** | 有：`odoo/upgrade_code/19.4-00-ir-access.py`。🔴 **与 l10n_cn 线无关、影响 12 个已发布模块 + 5 个内部模块，应单独立项** |
| **M-6** | `suite_cn_coa` 差异表要重算（官方 CN CoA 层级化 + 增删科目） | 中高 | 无脚本，人工对齐。🟢 R33-A 后基线已有：`l10n_cn_assbe_chart_R33A.csv` 101 行 + 19.0 逐码 diff（同码同义 29 / 同码异义 1 / 官方独有 5 / 我方独有 38），按同一四分类跑一遍即可 |
| **M-7** | ASBE/ASSBE 差异表要重算（存货/成本口径公式改动 + `groupby` 上提 + `foldable` 替换） | 中高 | `foldable` 有脚本；公式口径无 |
| **M-8** | 导出向导二进制赋值 `base64.b64encode` → `BinaryBytes` | 中 | 有脚本 |
| **M-9** | `account.report.expression` 字段变化（`formula`/`subformula` Char→Text、engine 新增 `text`/`reference`） | 低 | 向后兼容 |
| **M-10** | 官方 XLSX 按钮 `always_show` 默认翻转 | 低 | 我方显式带 `always_show: True` 即可 |
| 🆕 **M-11** | **脱钩工程 + id4/28 可达性处置** —— 🔴 **不是 v20 打进来的**，是**主动排期到此轮**以复用 M-2/M-3/M-4 的取数层重写。列此仅为排期归位，勿按「v20 破坏面」读 | —— | 度量与做法见 §8 第 8 项 |
| 🆕 **M-12** | **`property_*` 配置整批迁到 `res.company` 模板并改名** —— 收款 / 付款 / 存货计价 / 生产成本四键去 `property_` 前缀（`property_account_receivable_id` → `receivable_account_id` 等）；`use_storno_accounting` 与两个 `property_account_*_categ_id` 从 `template_data` 消失。**我方若在 chart template 层写过这些键，v20 上静默失效**（不报错、只是不生效） | **高** | 无脚本。机械改名 + 换层级，但**须逐键确认新归属**。⚠️ 官方提交名含 `l10n_*`，属跨全部本地化的横扫改动，不是 CN 专有。出处：VPS `A-90(a)` |
| 🆕 **M-13** | **CN 在产品 / 制造费用科目改挂**：`account_production_wip_account_id` 从 `14xx`（存货段）改挂 `4001`/`5001`（成本段），overhead 键改挂 `4101`/`5101`（`cn` 与 `cn_large_bis` 各两键） | **高** | 无脚本。🔴 **这是 M-7「存货 / 成本口径公式改动」的成因，不是两件事** —— 官方把 BS 存货公式加了 `+ 5001 + 5101 + 5201`、PL 排除项由 `5\(53)` 改成 `5\(5001, 5101, 5201, 53)`，正是为了把挪到成本段的在产品拉回来。**差异表重算必须先接受「在产品挂 4001/5001」这个前提，否则两边永远对不上。** 出处：VPS `A-90(b)` |
| 🆕 **M-14** | 🔴 **`1012` 段两个明细子科目被删除** —— master 的 `cn` 表上无 `1012.01`/`1012.02`，职能改挂新增的 `1004`/`1005`（`transfer_account_code_prefix` 由 `1012` 改为 `1005`、suspense 改 `1004`） | **中高** | 无脚本，人工对齐。🔴 **连带**：R43 对 `1012 其他货币资金` 的裁定（「采纳时告警、不自动改 `account_type`」）**前提是官方保留 `1012` 明细** —— 该前提在 v20 不成立，**这条裁定到 v20 要重做**，见 `项目档 §4.5` 相关条。出处：VPS `A-90(c)` / `A-96(c)` |
| 🆕 **M-15** | **两张中式资产负债表会继承 `enable_snapshots=True`** —— `l10n_cn_asbe_bs` / `l10n_cn_assbe_bs` 的 root 是 `balance_sheet`（全仓唯一显式开快照的报表），而 `l10n_cn_reports` 未显式覆盖 ⇒ 按 `_compute_report_option_filter` 的 root 继承规则会跟着开。两张利润表 root 为 `profit_and_loss`，不开 | **中** | 🟢 有覆盖办法：CN 报表 XML 上显式 `<field name="enable_snapshots" eval="False"/>`。<br>⚠️ **这是读继承规则推出的，非直读赋值** —— VPS 侧已显式标为推断并转实测项 **Q-51**（`precompute+store` 的 compute 字段最终落库值须装库读表确认）。**不得当作既成事实处理。**<br>🟢 **三条已排除**：我方无表达式披露行不进快照（不触发引擎）、我方改过表达式会因 `serialized_formulas_dict` 变化而重算（拿不到陈旧值）、引擎版本升级自动清快照。出处：VPS `A-91(d)` |
| 🆕 **M-16** | **`ir.sequence` 两条改动**：① 日期区间子序列基准由 `fields.Date.today()`（服务器 UTC）改为 `context_today()`（用户时区）⇒ **跨日边界会落到不同的 `date_range` 子序列**，对「按期间分段取号」的中式凭证字号有实际影响；② 前缀 / 后缀含非 ASCII 触发 `@api.onchange` 告警（官方理由：Code128 条码只支持 ASCII）—— 中文字号「记 / 收 / 付 / 转」正中 | **①中 / ②低** | ① 无脚本，须确认我方取号是否依赖 UTC 日界。<br>② 🔴 **不动** —— 它是 onchange warning，**不阻断写入、不影响 XML/ORM 建记录**，只在 UI 编辑时提示。为消除告警改用 ASCII 前缀与中式字号习惯冲突，不值得。实测确认转 **Q-53**。出处：VPS `A-94(c)` |

**已排除、明确不构成风险的**：D/C 方向后缀、前缀匹配语义与正则、`date_scope` 枚举、条件类 subformula 白名单、comparison 多 column_group 机制、`_init_options_buttons` / `dispatch_report_action` / `export_file` / `export_to_xlsx` / `root_report_id` / custom_handler 回落、`account_voucher_cn`、B-57 的 code 邻近继承。

🆕 **s-21 新增排除（VPS `A-88`~`A-96`，这几条是「地基没塌」的具体内容）**：

| 排除项 | 出处 | 对我方的意义 |
|---|---|---|
| `account_codes` 引擎语义**全线保留**；删的是 `domain` 引擎的三个 subformula | `A-88` | 我方 192 条表达式的引擎地基在 |
| 🔴 前缀匹配**仍且只能基于 `code`** —— `code_path` 在企业版 master 全树 `.py` 命中 **0**、社区侧仅 3 处定义/SQL 生成、业务侧零消费 | `A-88` | 「按 code 前缀取数」这个前提没被层级化取代 |
| `l10n_cn_reports` 模块与 **`l10n_cn_asbe_bs` xmlid 原地保留**；三个 `chart_template` 键（`cn` / `cn_common` / `cn_large_bis`）无更名无合并 | `A-89` | **我方披露行与 `bal_begin` 列的 FK 挂载安全**（B-77 说的「唯一耦合」这次没断） |
| `account.report.line` **无新增必填字段**；`report_id` / `hierarchy_level` / `sequence` 逐字未变。唯一变化 `foldable`(Bool) → `foldability`(Selection)，有官方脚本 | `A-91(b)` | **我方那条 `sequence=9000` 无表达式披露行，v20 上建得出来** |
| 数据对象层**没有绕过** `account.report.line` —— `_get_lines()` 仍 `for line in self.line_ids` | `A-91(c)` | **外挂行仍会出现在输出里**（这是 M-3 得以收窄的依据） |
| `account_type` **19 个取值零增删**，`asset_cash` 仍在；`parent_id` **非必填**；`name` 翻译 JSON 形态逐字未变 | `A-92` | 纯 ORM 新建科目路径不受影响；现金流量口径判据未变 |
| 导出取数仍走 `_get_lines`；报表 **默认不走 Paper Muncher** | `A-93` | 中式版式导出这条路的入口没换 |
| `account.move._order` / `sequence.mixin` / `_sequence_index` / 五个 regex **逐字未变**；`review_state` 在 `_post()` 内**零命中**（不改过账与编号时点） | `A-94` | 凭证编号机制整体未变 |
| 三个 module hook 签名与 `-u` 重跑约束**全部未变**；`_register` 保留属性两分支各 8 处未变 | `G-95` | B-67 那条架构约束在 v20 仍成立 |
| 现金流量表 handler 机制与 `asset_cash` 判据未变 | `A-96` | —— |

⚠️ **一条口径订正（s-21）**：VPS 分册 `A-50` 表内「`account_report.py` 5,045」是 **`git diff --stat` 的改动行数、不是文件规模**（判据：同表 `account_partner_ledger.py` 记 1,200，而该文件 master 总行数仅 534 —— 总行数不可能小于自身）。新锚点精确值：**改动 `+2,731 / −2,406 = 5,137` 行，文件 `8,163 → 8,488` 行**，改动占比按插入侧 ≈ 32%、按增删合计 ≈ 60%（后者会把「一行被改」重复计成一增一删）。**引擎本体被大改这个结论不变，反而更重。**

**阻塞**：DB 级迁移脚本在闭源 `odoo/upgrade#9357`，`unknown`。

**转出的待实测项**（VPS 侧登记为 Q-51 ~ Q-53，均须等 20.0 可用）：① 两张中式 BS 的 `enable_snapshots` 实际落库值（M-15）；② 快照存在时我方外挂披露行在锁定期报表上的呈现；③ `ir.sequence` 中文前缀告警的实际 UI 呈现与是否影响保存（M-16②）。

**重启条件**：20.0 正式发布 / master 版本号跳到 20.1 / 2026-09-24~26 Odoo Experience 之后 / 业务侧出现具体触发（某 `suite_*` 要提前适配、或客户提出升级时间表）。
