<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="cn_tb_export_form" model="ir.ui.view">
        <field name="name">suite.cn.tb.export.form</field>
        <field name="model">suite.cn.tb.export</field>
        <field name="arch" type="xml">
            <form string="科目余额表 中式版式导出">
                <group>
                    <field name="report_id" invisible="1"/>
                    <field name="options_json" invisible="1"/>
                    <field name="columns_mode" widget="radio"/>
                </group>
                <p class="text-muted">
                    六 / 八栏是同一张报表的两种列集。八栏含「本年累计发生额·借/贷」两列，
                    六栏不含。默认八栏。
                </p>
                <footer>
                    <button name="action_export" type="object" string="导出"
                            class="btn-primary"/>
                    <button string="取消" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
